---
doc: gate-e-verdict
owner: "@qa"
signoff: pending
---
# [F-002] Gate E — verdict

> **เจ้าของเอกสารนี้ = qa** · เกณฑ์ = [test-plan.md §17](test-plan.md) + [WEB_TEAM.md §4](../../../WEB_TEAM.md) + skill `quality-gate`
>
> ไฟล์นี้มี **verdict สองฉบับ** และฉบับเก่า**ไม่ถูกลบ**:
>
> | | วันที่ | commit | ผล |
> |---|---|---|---|
> | **v2 — ฉบับที่ใช้จริง** (อยู่ด้านล่างนี้) | **2026-09-20** | `65df986` | ⚠️ **GO WITH KNOWN RISK** |
> | v1 — ฉบับแรก (เก็บไว้ทั้งฉบับที่ [ภาคผนวก A](#ภาคผนวก-a--verdict-v1-2026-09-05--เก็บไว้ทั้งฉบับ-ไม่แก้ไข)) | 2026-09-05 | `df37104` | ❌ NOT DONE |
>
> **v2 ไม่ได้ยกเลิก v1 และไม่ได้แปลว่า v1 ตัดสินผิด** — ข้อเท็จจริงทางเทคนิคของ v1 **ยังจริงทุกข้อวันนี้**
> สิ่งที่เปลี่ยนคือ **เจ้าของความเสี่ยงตัดสินใจเดินต่อ** ไม่ใช่ qa เปลี่ยนใจ · อ่าน §0.1 ก่อนอย่างอื่น

---

# ══════ VERDICT v2 — 2026-09-20 · บน `65df986` ══════

> ตัวเลขทุกชุดใน v2 **ผมรันเอง 2026-09-20** ไม่ได้ลอกจาก PM / `tasks.md` / `manual-pass-results.md` / `release-gate-f.md`
> — เอกสารพวกนั้นเป็น*หลักฐานของคนอื่น* ไฟล์นี้เป็น*คำตัดสิน* และคำตัดสินต้องยืนบนของที่ผมเห็นกับตา
> · ของที่ผมรันเองไม่ได้ในรอบนี้ ผมเขียนไว้ตรง ๆ ว่ารันไม่ได้ (§2.3) **ไม่กลบเป็น "ตรวจแล้ว"**

## 0. คำตอบสั้น ๆ ก่อน

**Gate E = ⚠️ GO WITH KNOWN RISK** · ไม่ใช่ ✅ DONE · ไม่ใช่ ❌ NOT DONE

| | |
|---|---|
| เกณฑ์ §17.6 ผ่านหรือยัง | **ยังไม่ผ่าน** — manual §12.2 ยังไม่ครบ (M-01) · **สถานะทางเทคนิคเหมือน v1 เป๊ะ** |
| แล้วทำไมไม่ใช่ ❌ | **user ตัดสินใจรับความเสี่ยงข้อนี้ไว้เอง เมื่อ 2026-09-20** — ปิด F-002 โดยยกให้ M-01 ไปทำทีหลัง |
| ความเสี่ยงที่ถูกรับไว้ชื่ออะไร | **M-01** (ลิงก์คำเชิญผ่าน LINE จริง + คนเดินจนจบบนเบราว์เซอร์มือถือ) และ **M-07ค ฝั่ง iOS** |
| defect ระดับ blocker ในโค้ด | **0** — และผมตรวจซ้ำแล้วในรอบนี้ (§2, §5, §6) |
| เลนอัตโนมัติ | **เขียวครบ** · CI `35491463371` 9/9 job บน headSha = HEAD จริง · ผมรันซ้ำในเครื่องได้ 7 เลน (§2.1) |
| B-19 | **ปิดแล้วจริง** ผมตรวจโค้ด+เทสต์เอง (§5) · **AC ครบ 36/36** (35 → 36 จาก `84a3277`) และ AC-4.2 หลักฐาน*แข็งขึ้น* (§4) |

### 0.1 ⚠️ ความต่างที่เป็นทั้งหมดของความซื่อตรงของ verdict นี้

ตอน Gate 2 ผมเขียนกติกาของตัวเองไว้ใน `test-plan.md §17` ว่า
**"ข้อใดไม่ครบ = แดง ไม่มีเขียวแบบมีเงื่อนไข"** · กติกานั้น**ยังอยู่และผมไม่แก้**

> **ผมไม่ได้ให้ M-01 ผ่าน · ผมไม่ได้บอกว่า E-05 พิสูจน์แทน M-01 ได้ · ผมไม่ได้ลด M-01 ลงเป็น non-blocking**
> **§17.6 ยังเป็น ❌ ในตาราง §3.0 ข้างล่าง และจะเป็น ❌ ต่อไปจนกว่าจะมีคนเดินมันจริง**

สิ่งที่เกิดขึ้นคนละเรื่องกัน และต้องแยกให้ขาด:

| ถ้า **qa** เปลี่ยน ❌ เป็น ✅ เอง | ถ้า **เจ้าของความเสี่ยง** ตัดสินใจเดินต่อ |
|---|---|
| = แก้เกณฑ์ตอนกำลังถูกตัดสิน | = รับรู้เกณฑ์ที่ไม่ผ่าน **แล้วเลือกจ่ายราคานั้น** |
| หลักฐานที่ขาดหายไป **ถูกกลบ** | หลักฐานที่ขาดหายไป **ยังถูกเขียนไว้ว่าขาด** |
| ครั้งหน้าไม่มีใครรู้ว่าเคยขาด | ครั้งหน้าเปิดไฟล์นี้แล้วเห็นทันทีว่าขาดอะไร (§3.1) |
| **qa ทำแบบนี้ไม่ได้** | **user ทำได้ และทำไปแล้ว 2026-09-20** |

⇒ `go-with-known-risk` คือรูปที่ `release-gate-f.md §7` รออยู่พอดี
· **ห้ามใครอ้างไฟล์นี้ว่า "qa-green"** — เขียนว่า *"qa = go-with-known-risk · M-01 และ M-07ค-iOS ยังไม่ถูกพิสูจน์"*

---

## 1. Verdict block

```
⚠️ QUALITY GATE — F-002 Organization · License · Membership  (65df986)

A Requirement: pass — AC 35 ข้อใน F-002-organization-license-membership.md §2 · gate1-approved ·
                      Platform: both · size: full · D-012/D-027/D-028/D-029/D-030 ผูกกับ AC แล้ว
B Design:      pass — architecture/data-model/api-spec/ux-wireframe/ui/test-plan ครบ + signoff:approved ·
                      security-review 3 ไฟล์ + advisory review ของ B-19 · AC→case ครบ 36/36 (§4 · remap §4.2)
C Domain&Data: pass — cross-tenant: org-leak.kit 20/20 + tenancy.db 19/19 (DB จริง, CI) ·
                      tx+lock: concurrency-matrix 15/15 · ledger/Decimal: n/a โดยประกาศ (F-002 ไม่แตะ
                      StockMovement/เงิน) · ⚠️ ทั้งบรรทัดนี้ = หลักฐานจาก CI อย่างเดียว (Docker ดับ §2.3)
D Experience:  pass — 4 state ครบ+มี unit ประกบ · copy lint E-11 6/6 (สแกนทั้ง tree ⇒ copy ใหม่ของ B-19
                      อยู่ในขอบเขตอัตโนมัติ) · design token 4/4 · **ตรวจใหม่ด้วยเกณฑ์ reachability ที่เพิ่ง
                      เพิ่มเข้า Gate D หลัง v1** (90f0b21) — ผ่าน ไม่ได้ยกเว้นให้ตัวเอง · §6.5
E Quality:     ⚠️ MET-WITH-ACCEPTED-RISK
                      เขียว: 9/9 job บน CI run 35491463371 (headSha = 65df986 = HEAD) · playwright
                      expected=29 unexpected=0 flaky=0 skipped=0 · int 212/212 skip 0 · db 185/185 skip 0 ·
                      mobile-e2e 4 · ผมรันซ้ำเอง 9 คำสั่งในเครื่อง (§2.1) ตรงกับ CI ทุกตัว
                      ไม่ผ่าน: **§17.6 manual §12.2 ยังไม่ครบ — M-01 ยังไม่มีใครเดิน** (เกณฑ์เดิม ไม่ถูกแก้)
                      ที่ทำให้ไม่ใช่ FAIL: **user รับความเสี่ยง M-01 ไว้เอง 2026-09-20** (ไม่ใช่ qa ปล่อยผ่าน)
F Release:     n-a  — ไม่ใช่ขอบเขตของ Gate E · @release ตัดสินบน release-gate-f.md
                      · ข้อความถึง release/product อยู่ที่ v2 §8

VERDICT: GO WITH KNOWN RISK
Accepted risk (user, 2026-09-20): M-01 · M-07ค-iOS — รายละเอียดว่าอะไรพิสูจน์แล้ว/ยังไม่ ที่ §3
Blocking: 0 (หลังการรับความเสี่ยง) · ถ้าถอดการรับความเสี่ยงออก = กลับเป็น NOT DONE ทันทีด้วยเหตุผลเดิม
Non-blocking ใหม่ในรอบนี้: §6 — N-1 ยังเปิด (มีทะเบียน+เจ้าของ) · N-2 ปิด · N-3 ปิด (AC ใหม่) · N-4 บันทึกไว้
Revisit triggers (verdict นี้เป็นโมฆะเมื่อ): §7
COVERS SHA: 69b6960 (merge `54a1c20` รวมอยู่แล้ว) · ตรวจครบ 6+1 ข้อที่ §7.3 บน CI run 35523016859 (9/9)
AC: 36/36 (35 → 36 จาก `84a3277` · remap ที่ §4.2)
```

> **verdict block ตัดสินครั้งแรกบน `65df986` · วันนี้ครอบถึง `69b6960`** และนั่น**ไม่ใช่ความขัดแย้ง**:
> commit ที่ลงหลังจากนั้นถูกจัดชนิดทีละตัวตามกติกา §7.1 — **A1 สี่ตัว · B หนึ่งตัว · A2 หนึ่งตัว · D (merge) หนึ่งตัว**
> ⇒ ตัว D ทำให้โมฆะจริงตามกติกา **และผมตรวจครบแล้วที่ §7.3 (6+1 ข้อ ผ่านหมด) ⇒ verdict กลับมาครอบ `69b6960`**
> · ผมรันเลนในเครื่องซ้ำบน `6422142` (web **399** · api ไม่ขยับ · เคสเบราว์เซอร์ใหม่ **2/2 ผมรันเอง**)
> และเทียบกับ node-ci บน `69b6960` แล้วตรงกันทุกตัว · **คำตัดสินและช่องแดงช่องเดียวไม่เปลี่ยน**
> · **N-2 ปิด** (§7.1) · **N-3 ปิด** — product เติม AC ⇒ **35 → 36** (§4.2) · **N-1 ยังเปิด แต่มีทะเบียนและเจ้าของแล้ว**

---

## 2. หลักฐานเป็นตัวเลข — ผมรันเอง 2026-09-20

เครื่อง: node **v22.23.1** · pnpm **9.15.9** · `git rev-parse HEAD` = `65df98675ee4068cada55fdc3b58cc632d6fef14`

### 2.1 สิ่งที่ผมรันเองในเครื่องรอบนี้ (ทุกคำสั่ง exit 0)

| คำสั่ง | ผล | exit |
|---|---|---|
| `pnpm --filter web test` | **52 files · 397 passed · 0 failed · 0 skipped** | 0 |
| `pnpm --filter api test` | **56 passed \| 11 skipped files · 704 passed \| 210 skipped (914)** | 0 |
| `pnpm --filter @omnistock/core-domain test` | **23 files · 384 passed** | 0 |
| `pnpm --filter @omnistock/db test` | **8 passed \| 2 skipped files · 161 passed \| 24 skipped (185)** | 0 |
| `pnpm --filter @omnistock/config test` | **2 files · 63 passed** | 0 |
| `pnpm --filter @omnistock/contracts test` | **3 files · 12 passed** | 0 |
| `fvm flutter test` (apps/mobile) | **`03:18 +451: All tests passed!`** | 0 |
| `fvm flutter analyze` | **`No issues found! (ran in 4.4s)`** | 0 |
| `fvm dart run tool/check_boundaries.dart` | **`OK — 85 files scanned, no violations`** | 0 |

**web 366 → 397 (+31)** คือของใหม่ของรอบนี้ · api/core-domain/db/config/contracts/mobile **ไม่ขยับเลย**
⇒ สอดคล้องกับ diff: B-19 + NO_TOKEN + ปุ่มเชิญใหม่ + Referrer-Policy **เป็นงานฝั่งเว็บล้วน ไม่มีอะไรแตะ API**
(ผมเช็ค `git diff --stat df37104..65df986` เองแล้ว — ไฟล์ `apps/api/` ที่ถูกแตะมีไฟล์เดียวคือ `test/build-defects.ts` = ทะเบียน defect)

### 2.2 CI run `35491463371` — ผมเปิด log อ่านเอง ไม่ได้ดูแค่ ✅

`gh run view 35491463371 --json headSha` → **`65df98675ee4068cada55fdc3b58cc632d6fef14`**
⇒ **ตรงกับ `git rev-parse HEAD` ของ branch เป๊ะ** ⇒ **ไม่ใช่ run ของคอมมิตเก่า** (นี่คือข้อที่ PM ขอให้ยืนยัน และผมยืนยันแล้ว)
9 job · `conclusion = success` ทั้งหมด: `node-ci` · `integration-api` · `db-migrate` · `e2e-web` · `mobile-e2e` · `flutter-ci` · `contracts-drift` · `oasdiff` · `depcruise`

| job | บรรทัดใน log ที่ผม grep เอง | ตัวเลข |
|---|---|---|
| `e2e-web` | `Running 29 tests using 1 worker` → `29 passed (30.1s)` → `playwright: expected=29 unexpected=0 flaky=0 skipped=0` → `the browser lane ran 29 passing case(s) (floor 29).` | **29 · skip 0 · flaky 0** |
| `integration-api` | `[lane-enabled guard] … total: 212 passed / 0 failed / 0 skipped (of 212)` → `OK — the required suites really ran.` | **212 · skip 0** |
| `db-migrate` | `[lane-enabled guard] … total: 185 passed / 0 failed / 0 skipped (of 185)` → `OK — the required suites really ran.` | **185 · skip 0** |
| `mobile-e2e` | `00:08 +4: All tests passed!` → `mobile integration: 4 passing case(s) (floor 4)` | **4** |
| `flutter-ci` | `🎉 451 tests passed.` · `No issues found!` · `check_boundaries: OK — 85 files scanned` | **451** |
| `node-ci` | `Tasks: 33 successful, 33 total` · web `397` · api `704 \| 210` · core-domain `384` · db `161 \| 24` · config `63` · contracts `12` | **ตรงกับเครื่องผมทุกตัว** |

**PM ขอให้ยืนยันว่า `integration-api` ผ่านจริงไม่ใช่เดา — ยืนยันแล้ว** และผมไล่ถึงชั้นที่สอง:
เลน int แตกราย **13 ไฟล์** (`invitations 23 · redeem 16 · org-leak 20 · concurrency 15 · refresh-token 8 ·
rate-limit 6 · perf-smoke 5 · openapi-parity 5 · header-policy 4` + orgs/members/auth.e2e/seed-kit)
· **ทุกไฟล์ `0 skipped`** ⇒ ไม่มีไฟล์ไหน `describe.skip` ทั้งก้อนแล้วเขียวลอย ๆ (I-37)
· `grep -rE "\.only\(|xit\(|xdescribe\(" apps packages` = **0 hit ทั้ง tree**

### 2.3 ⚠️ สิ่งที่ผม **รันเองไม่ได้** ในรอบนี้ — พูดตรง ๆ ว่าไม่ได้ตรวจกับตา

**Docker บนเครื่องนี้ดับ** (`docker info` → exit != 0 · daemon ไม่ตอบ) ⇒ ไม่มี Postgres/Redis ในเครื่อง
⇒ **ผมรันสี่เลนนี้เองไม่ได้เลยในรอบนี้:**

| เลนที่รันเองไม่ได้ | หลักฐานเดียวที่มีวันนี้ |
|---|---|
| Playwright browser (29 เคส รวม **E-05 ที่ถูกเขียนใหม่เพราะ B-19** และ E-12★) | **CI เท่านั้น** (run 35491463371) |
| `integration-api` (212 · DB+Redis จริง) | **CI เท่านั้น** |
| `db-migrate` (185 · tenancy proof บน DB จริง) | **CI เท่านั้น** |
| `mobile-e2e` (4 · emulator + API จริง) | **CI เท่านั้น** |

**ผมไม่แก้ Docker และไม่อ้างว่าตรวจแล้ว** · ต่างกับ v1 ที่ผมรัน Playwright เองบนสแตกจริงในเครื่อง —
**รอบนี้ทำไม่ได้** ⇒ คุณภาพของหลักฐานสำหรับ 4 เลนนี้ **ต่ำกว่ารอบที่แล้วหนึ่งขั้น** (CI ชั้นเดียว ไม่ใช่สองชั้น)
· ผมยังรับว่าเพียงพอ เพราะ CI รันบน headSha เดียวกับที่ผมตัดสิน และมี I-37 guard บังคับว่าเลนที่ไม่ได้รันจริง = แดง
· **แต่มันคือชั้นเดียว และไฟล์นี้ต้องเขียนแบบนั้น** ([[green-locally-not-tested]] ทำงานทั้งสองทิศ:
"เขียวในเครื่อง ≠ ทดสอบแล้ว" และวันนี้คือ "เขียวบน CI = ทั้งหมดที่ผมมี")

---

## 3. สิ่งที่ยัง **ไม่ถูกพิสูจน์** — เขียนแบบไม่อ้อม

### 3.0 `test-plan.md §17` ไล่ซ้ำ — **เฉพาะแถวที่ขยับจาก v1** (ที่เหลือ = ตาราง §4 ของภาคผนวก A ยังใช้ได้)

| § | เกณฑ์ (ของผมเอง ตอน Gate 2 — **ไม่ถูกแก้**) | v1 | v2 | หลักฐานวันนี้ |
|---|---|---|---|---|
| 17.1 | AC ทุกข้อมีเทสต์ที่รันจริงและผ่าน | ✅ (35) | ✅ (**36**) | §4 — ตรวจใหม่หลัง B-19 และหลัง AC ใหม่ของ `84a3277` ⇒ **36/36** (§4.2) |
| 17.2 | Track 1 เขียวบน **CI** · int lane พิสูจน์ว่าเปิดจริง (I-37) | ✅ | ✅ | 9/9 บน `35491463371` (headSha = HEAD) · int 212/212 · db 185/185 · **skip 0 ทั้งคู่** |
| **17.6** | **manual §12.2 ทำครบและบันทึกผล** | ❌ | **❌ ยังแดงเหมือนเดิม** | **M-01 ยังไม่มีใครเดิน** · M-02..M-07ข ครบ · **ช่องนี้ไม่ถูกทำให้เขียว — ถูก*รับความเสี่ยง*แทน (§0.1)** |
| 17.8 | ไม่มี test ถูก skip/ลด/ลบเพื่อให้ผ่าน | ✅ | ✅ | `grep -rE "\.only\(\|xit\(\|xdescribe\("` = **0 hit** · `.skip` ที่มีคือ `INT_LANE_ENABLED ? describe : describe.skip` ซึ่ง I-37 บน CI บังคับให้รัน · **ผมตรวจด้วยว่า E-05 ที่เขียนใหม่ไม่ได้*ลด*หลักฐาน — ดู §5 ย่อหน้าสุดท้าย** |
| 17.9 | defect ค้างถูกจัดชั้น · **blocker = 0** | ✅ | ✅ | `build-defects.ts` **B-1..B-19** (เพิ่ม 1 แถว) · B-19 มี 5 pin ครบ · ไม่มีแถวไหนไม่มีเทสต์ |
| 17.4 / 17.10 | §10 gate ครบ | ⚠️ บางส่วน | ⚠️ **บางส่วนเหมือนเดิม** | G-06/G-09/G-11/G-12 ยังไม่มี implementation — **แต่ยกไป `forward-commitments.md` พร้อม trigger แล้ว** (§6.6) ⇒ ไม่ใช่ของที่หายไปเงียบ ๆ อีกต่อไป |

⇒ **ช่องแดงยังเป็นช่องเดิม ช่องเดียว และผมไม่ได้แตะมัน**

### 3.1 M-01 — ลิงก์คำเชิญผ่าน LINE จริง บนโทรศัพท์จริง

**สถานะ: ยังไม่มีใครทำ** · §17.6 = ❌ · user รับความเสี่ยงไว้ 2026-09-20

**พิสูจน์แล้วโดยเครื่อง (ให้เครดิตตามจริง):**

| ของที่พิสูจน์แล้ว | หลักฐาน |
|---|---|
| เส้นทาง เปิดคำเชิญ → สมัคร → ล็อกอิน → **กลับมาที่คำเชิญเอง** → กดเข้าร่วม → เข้าร้าน | **E-05 เดินครบบนเบราว์เซอร์จริงทุกรอบ CI** (เขียนใหม่ตอนปิด B-19) |
| ระหว่างเดินทาง token **ไม่อยู่ใน URL / localStorage / sessionStorage / cookie / DOM** | E-05 assert กลางทางบนหน้า auth + **E-12★** |
| `/invite` **เรนเดอร์จริงบน Chrome ของ Android 13 เครื่องจริง** และ URL bar ไม่มี token | manual pass 2026-09-05 (`manual-pass-results.md` §M-01.3) — **ครั้งแรกที่มีใครเปิดหน้านี้บนเบราว์เซอร์มือถือจริง** |
| รูปร่างลิงก์ไม่ใช่ความเสี่ยงของ LINE | token = base64url 43 ตัว ไม่มี space/`+`/`/`/`=`/unicode · 1 path 1 query |

**ยัง ไม่ ได้พิสูจน์ — และนี่คือราคาที่ถูกรับไว้:**

| ยังไม่รู้ | จะกัดได้ยังไง |
|---|---|
| **LINE ทำอะไรกับลิงก์ของเรา** — ห่อ/ตัด/เติม tracking/เปิดใน in-app browser ของตัวเอง | ถ้า LINE ตัดหรือห่อ query string ⇒ **กลไกเชิญทั้งหมดของ MVP ตายในโลกจริง** ทั้งที่ทุกเลนเขียว (ไม่มี SMTP จนถึง F-081 ⇒ ไม่มีทางสำรอง) |
| **คนกรอกฟอร์มสมัครจบบนเบราว์เซอร์มือถือ** | คีย์บอร์ดบังปุ่ม · autofill · viewport · in-app browser ที่ไม่มี history API ครบ ⇒ **B-19 ทั้งตัวเกิดจากเส้นนี้พอดี** และมันรอดทุกเลนมาหลายสัปดาห์ |
| **in-app browser ของ LINE เก็บ module memory ข้าม client-side nav ไหม** | ถ้าไม่เก็บ ⇒ กลับมาเจอ `NO_TOKEN` (ซึ่งตอนนี้มีคำพูดที่ถูกต้องแล้ว — ไม่ใช่ "ลิงก์พัง") ⇒ คนต้องแตะลิงก์ใน LINE ซ้ำ · **degrade อย่างสุภาพ ไม่ใช่ตาย** แต่ไม่มีใครเคยเห็นของจริง |

> ⚠️ **`adb input` ของ PM ล้มเหลว = ไม่ใช่หลักฐานว่าฟีเจอร์พัง** และก็ **ไม่ใช่หลักฐานว่าฟีเจอร์ดี**
> มันคือ "เครื่องมือขับฟอร์มไม่สำเร็จ" ⇒ **ช่องนี้ว่างเปล่า ไม่ใช่เขียวและไม่ใช่แดง** · ผมนับเป็นว่าง

**และผมต้องแก้คำสั่งของตัวเองด้วย** — §8 ของ v1 เขียนว่า *"ตั้ง `WEB_APP_BASE_URL` เป็น LAN IP"*
**คำสั่งนั้นทำตามไม่ได้** ผมยืนยันเองแล้วที่ `packages/config/src/env.ts` (ฟังก์ชัน `isAcceptableWebAppBaseUrl`):
รับเฉพาะ `https:` หรือ `http:` บน `localhost`/`127.0.0.1`/`[::1]`/`::1` ⇒ **API จะไม่ยอมบูต**
· ทางที่ใช้ได้คือ `adb reverse tcp:3001 tcp:3001` (ต่อ USB) ซึ่ง**อยู่ใน runbook แล้ว** และ runbook
เขียนข้อจำกัดของมันไว้ตรง ๆ ด้วย: **ลิงก์ `localhost` ส่งข้ามเครื่องไม่ได้**
⇒ **M-01 เวอร์ชันที่ทำได้วันนี้ พิสูจน์ "LINE ห่อ/ตัดลิงก์ไหม" ได้ แต่ไม่ใช่ลิงก์ที่มีรูปร่างแบบ production**
(ต้องมี origin https จริง — `forward-commitments.md` ผูกไว้กับ "ก่อนเปิด external customer" แล้ว)
· **นี่แปลว่าแม้มีคนเดิน M-01 พรุ่งนี้ มันก็ยังไม่ปิดความเสี่ยงทั้งก้อน** — ผมระบุไว้เพื่อไม่ให้ใครนึกว่า 20 นาทีจบ

### 3.2 M-07ค ฝั่ง iOS — **สถานะเดิมไม่เปลี่ยน: `n/a — deferred, owned, triggered`**

ผมตรวจซ้ำในรอบนี้: `forward-commitments.md` บรรทัด 213 ยังอยู่ · มีเจ้าของ (**devops**) · มี trigger ผูกเหตุการณ์
(*"ก่อน mobile feature ถัดไปที่แตะ `AppDelegate.swift`/iOS-native (F-006) หรือก่อน first iOS release"*)
· ยังไม่มี job `macos`/`xcode` ใน CI ⇒ **`AppDelegate.swift` ยังไม่เคยถูก compile เลยสักครั้ง**
· ฝั่ง Android ปิดจริง (`mobile-e2e` ต้อง `assembleDebug` ก่อนรัน ⇒ `MainActivity.kt` compile ทุกรอบ)

**เหตุผลที่ยังไม่ใช่ red เหมือนเดิม:** ไม่มี iOS release ใน Phase 0 ⇒ ไม่มีผู้ใช้คนไหนแตะโค้ดบรรทัดนั้นได้
· **แต่ข้อผูกของผมยังอยู่ทุกตัวอักษร: ถ้าจะ ship iOS เมื่อไหร่ ข้อนี้กลับเป็นแดงทันที และ verdict นี้ไม่ครอบ iOS**

---

## 4. AC ↔ เคส — ตรวจใหม่ทั้งหมดหลัง B-19 / NO_TOKEN / ปุ่มเชิญใหม่

> **คำถาม:** ของใหม่ 4 ก้อนทำให้ AC ข้อไหนเปลี่ยนสถานะ หรือมี AC ข้อไหนที่เพิ่งมีเคสตอนนี้ทั้งที่เมื่อก่อนไม่มีไหม
> **คำตอบ: ไม่มีข้อเดิมข้อไหนเปลี่ยนสถานะ · AC-4.2 หลักฐาน "แข็งขึ้น" ไม่ใช่ "เพิ่งมี"**
> · **แต่จำนวน AC เปลี่ยน: 35 → 36** เพราะ `84a3277` เติม AC ของปุ่ม "เชิญใหม่อีกครั้ง" ย้อนหลัง (N-3) ⇒ ตาราง §4.2

| AC | เดิม (v1) | วันนี้ | อะไรเปลี่ยน |
|---|---|---|---|
| **AC-4.2** ยังไม่มีบัญชี → สมัคร email เดิม → ผูกอัตโนมัติ | ✅ ผ่าน แต่เคสเดินทาง **อ้อม** (เปิดลิงก์ซ้ำ) และมีคอมเมนต์ในเทสต์ประกาศตัวเองว่าเป็น *"deliberate deviation"* จากถ้อยคำของ §12.1 | ✅ **ผ่านด้วยเส้นทางจริงตามสเปก** | E-05 เดิน signup → login → **กลับมาที่คำเชิญเอง** → กดเข้าร่วม · **deviation ที่ค้างมาตั้งแต่ 2026-08-16 ถูกปิด ไม่ใช่ถูกอธิบาย** · + assert E-12 กลางทาง |
| **AC-3.4** ออกลิงก์ใหม่ → ลิงก์เดิมตาย · ห้ามคำว่า "คัดลอกลิงก์เดิม" | ✅ | ✅ **ไม่ขยับ** | ปุ่ม "เชิญใหม่อีกครั้ง" เป็นคนละเส้นกับ reissue (มันสร้าง**คำเชิญใหม่**บนแถวที่**ตายแล้ว** ไม่ใช่หมุน token ของแถวที่ยัง pending) ⇒ ไม่แตะเนื้อของ AC-3.4 · `copy-lint.test` สแกน **ทั้ง `apps/web/src` + `apps/mobile/lib` แบบเดินไดเรกทอรี** ⇒ **copy ใหม่ทุกบรรทัดของรอบนี้อยู่ใต้ lint อัตโนมัติแล้วโดยไม่ต้องเพิ่มอะไร** (ผมเปิดไฟล์ยืนยันเอง ไม่ได้เดา) |
| **AC-3.8 / AC-6.3** (D-028) Owner-only | ✅ | ✅ **ไม่ขยับ** | ปุ่มใหม่ **ไม่เปิดรูใหม่**: ถ้าแถวที่ตายเป็นคำเชิญระดับ Owner และคนกดไม่ใช่ Owner ⇒ `roleId` ถูกปล่อยว่าง (ไม่ pre-fill สิ่งที่ส่งไม่ได้) · และ**ด่านจริงยังอยู่ที่ server** ซึ่ง int test ยิงตรงอยู่แล้ว (`★ C-1/D-028: an Admin cannot invite somebody AS an Owner`) |
| อีก 32 ข้อ | ✅/🟡 | **เหมือนเดิมทุกข้อ** | diff รอบนี้ไม่แตะ API/โดเมน/สคีมาเลย (§2.1) ⇒ ไม่มีช่องทางที่สถานะจะเปลี่ยน |
| **AC-3.10 ใหม่** (เติมย้อนหลัง `84a3277`) ปุ่ม "เชิญใหม่อีกครั้ง" | — ยังไม่มี | ✅ **มีเคสครบทุกอนุประโยค แต่ชั้นหลักฐานอ่อนที่สุดในชุด** | ดู §4.2 |

**สรุป: 36/36 · เต็ม 33 · partial 3 (AC-1.2 · AC-5.4 · AC-7.3)** — **เปลี่ยนจาก 35/35 เพราะ `84a3277` เติม AC ใหม่ 1 ข้อ**
· การ remap เป็นสิทธิ์ของผมตามที่ product เขียนไว้เอง และนี่คือการใช้สิทธิ์นั้น: **ผมรับ AC-3.10 เข้าตารางและนับเป็น 36**
partial ทั้งสามยังเป็น *"พื้นผิวยังไม่เกิดใน Phase 0"* (F-080/F-082 · F-005/F-011 · F-007) ตามที่ @product รับไว้ใน D-029(4)

### 4.2 AC-3.10 — ไล่ทีละอนุประโยค (ผมเปิดเคสอ่านเอง ไม่ได้นับจากจำนวน)

| อนุประโยคของ AC | เคสที่รับ | ชั้นหลักฐาน |
|---|---|---|
| แถว `expired` / `cancelled` เชิญซ้ำได้จากแถวนั้น | `MembersScreen.reinvite.test`: *shown on an EXPIRED row* · *shown on a CANCELLED row* | unit |
| ⛔ ห้ามขึ้นบนแถว `accepted` | *⛔ NOT shown on an ACCEPTED row — that person is already in the shop* | unit |
| (ไม่ชนกับปุ่มเดิม) ไม่ขึ้นบนแถว `pending` | *NOT shown on a PENDING row — it already has its own two buttons* | unit |
| เติม email + สิทธิ์เดิมเป็นค่าตั้งต้นที่ยังแก้ได้ | *★ opens S7 with the row's email and role, both still editable* | unit |
| (D-028) แถว Owner + คนกดไม่ใช่ Owner ⇒ ไม่เลือกสิทธิ์ไว้เลย | *⛔ an Owner-role row, reopened by a non-Owner, leaves NO role selected* + **control**: *★ an Owner-role row, reopened by an Owner, still pre-fills Owner* | unit · **control คือสิ่งที่ทำให้เคสแรกมีความหมาย** |
| ผลลัพธ์เป็นคำเชิญ**ใบใหม่** ไม่ใช่หมุน token ใบเดิม | **ไม่มีเคสเดี่ยวที่ assert ประโยคนี้ตรง ๆ** — ได้มาจากโครงสร้าง: ปุ่มเปิด `InviteDialog` ซึ่งยิงเส้น *สร้างคำเชิญ* (int 23 เคส) ไม่ใช่เส้น *reissue* | **โดยองค์ประกอบ ไม่ใช่โดยเคส** — ผมระบุตรงนี้ไม่กลบ |
| ของเก่า (เป็นสมาชิกแล้ว / มี pending) ⇒ server ปฏิเสธ ใช้ข้อความฟอร์มเดิม | `invitations.e2e.int`: *409 ALREADY_MEMBER* · *409 INVITATION_PENDING carries the id* (เคสของ AC-3.6 เดิม) | **int (DB จริง)** |

**คำตัดสินของผม: AC-3.10 = ✅ "เต็ม" ตามเกณฑ์ §17.1** (*มีเทสต์ที่รันจริงและผ่าน*) **แต่ผมติดป้ายกำกับไว้ว่ามันคือ AC ที่หลักฐานอ่อนที่สุดใน 36 ข้อ**
· อนุประโยค (ก)–(ง) พึ่ง unit ล้วน **ไม่มีเคสเบราว์เซอร์** ⇒ ไม่มีอะไรพิสูจน์ว่า*คนกดจริงแล้วได้ฟอร์มที่เติมมาถูก* — เทสต์สร้าง component เอง
(นี่คือรูปที่เกณฑ์ reachability เพิ่งเตือนไว้เป๊ะ: *"เทสต์ที่ CONSTRUCTS จอ พิสูจน์ได้แค่ว่ามันทำงานเมื่อมีคนพามันขึ้นมา"*)
· **สิ่งที่กันไม่ให้เป็นแดง:** ตัวจอที่ปุ่มอยู่ (`MembersScreen`) **มี** เคสเบราว์เซอร์เดินถึงจริง (E-08/E-08b) และ
**ด่านสิทธิ์จริงอยู่ที่ server** ซึ่ง int ยิงตรงแล้ว ⇒ ต่อให้ UI พลาด คนก็ยังเชิญ Owner ไม่ได้
· **งานค้างของผม:** เพิ่มเคสเบราว์เซอร์ 1 ตัวให้ AC-3.10 · **trigger = ครั้งถัดไปที่แตะจอสมาชิก (F-003/F-004)** — ผมรับ trigger ที่ product เสนอ

### 4.1 ของใหม่ที่ **ไม่มี AC รองรับ** — ผมบันทึกไว้ ไม่ใช่ทำเป็นไม่เห็น

| ของใหม่ | ผูกกับอะไร | เคสที่ประกบ | ข้อสังเกตของผม |
|---|---|---|---|
| สถานะ `NO_TOKEN` (§11.5) | ux-wireframe §11.5 — **ไม่มี AC** | `InviteScreen.test` 3 เคส รวม **⛔ *NO_TOKEN copy never appears for a REAL 404 INVITATION_INVALID*** | เคส ⛔ คือตัวที่มีค่าที่สุด: มันกัน**การกลับด้าน**ที่ทำให้ของจริงถูกกลบด้วยของปลอม · ผมพอใจกับชั้นนี้ |
| ปุ่ม "เชิญใหม่อีกครั้ง" (§7) | ux-wireframe §7 — **ไม่มี AC** | `MembersScreen.reinvite.test.tsx` **8 เคส** (แสดงบน expired/cancelled · ⛔ ไม่แสดงบน accepted/pending · pre-fill 3 เคส) | **มีเทสต์ครบและตรรกะถูก แต่ไม่มีเคสเบราว์เซอร์ และไม่มี AC** ⇒ เป็น scope ที่ลงมาตอน QA · ดู **N-3** |
| `Referrer-Policy: no-referrer` บน `/invite` | security review (advisory) — ไม่มี AC | `response-headers.test.ts` 2 เคส (อ่าน `next.config.mjs` **ตัวจริง** + กันการขยายเป็น wildcard) | เทสต์**เขียนไว้เองอย่างซื่อสัตย์**ว่ามันพิสูจน์ไม่ได้ว่าเซิร์ฟเวอร์ส่งจริง ⇒ ดู **N-2** |

---

## 5. B-19 — ผมตรวจของจริง ไม่ได้รับคำของ implementer

สิ่งที่ผมเปิดอ่านเองในรอบนี้ (ไม่ใช่เชื่อ `tasks.md`):

| ที่ตรวจ | ผลที่ผมเห็นกับตา |
|---|---|
| `lib/session/pending-invite.ts` | module-scope variable ล้วน · **ไม่มี `localStorage`/`sessionStorage`/`document.cookie`/URL แม้แต่ตัวเดียวในไฟล์** ⇒ E-12 ทุกข้อยังยืนได้จริงโดยโครงสร้าง ไม่ใช่โดยวินัย |
| `features/invite/use-invite-token.ts` | ลิงก์ใหม่ใน URL **ชนะเสมอ** (`dropPendingInvite()` ก่อนตั้งค่า) · `?? ` กัน strict-mode double-invoke ไม่ให้ทิ้ง token · ยัง `replaceState` ไม่ใช่ `pushState` |
| `AppShell.tsx` | `session.endSession()` **มีผู้เรียกจริงแล้ว** ในปุ่มออกจากระบบ (ของเดิม: 0 ผู้เรียก) |
| `session-context.tsx` | `endSession` ทิ้ง hold · + effect ทิ้ง hold เมื่อ route ที่ mount ไม่อยู่ใน `{/invite,/login,/signup}` |
| `pending-invite.test.ts` (10 เคส) | **ชื่อเทสต์ตรงกับสิ่งที่ assert จริงทุกข้อ** — ผมไล่ทีละเคสเพราะ [[test-name-overclaims]] · `/login/help` ถูก assert ว่า **ไม่ใช่** `/login` (exact match ไม่ใช่ prefix) ตามที่รายงานไว้ |
| `build-defects.ts` | B-19 มี **5 pin** · pin หลักชี้ไปที่ `AppShell.logout.test.tsx` = **เทสต์ที่กดปุ่มจริง** ไม่ใช่เทสต์ที่เรียก `endSession()` ตรง ๆ ⇒ **ถูกต้องตามบทเรียนที่มันอ้างถึงเอง** |
| `e05-signup-through-invite.spec.ts` | เดิน signup → login → กลับมา `/invite` → กดเข้าร่วม · assert URL/storage/cookie/DOM **กลางทาง** |

**ข้อสังเกตหนึ่งข้อที่ผมตรวจแล้วและตัดสินว่าไม่เป็นไร:** E-05 เวอร์ชันเก่าพิสูจน์ *"สมัครด้วยอีเมลที่ถูกเชิญ
แล้วยังไม่ได้เป็นสมาชิกอะไรเลย"* ด้วยการไปยืนที่ `/select-org` แล้ว assert ว่า**ไม่มี**ชื่อร้าน · เวอร์ชันใหม่
ไม่ได้แวะ `/select-org` อีก ⇒ เปลี่ยนไปพิสูจน์ทางอ้อมว่า **ปุ่ม "เข้าร่วมร้านนี้" ยังอยู่และต้องกดเอง**
· ผมยอมรับ เพราะถ้าการสมัครเข้าร่วมให้เองจริง preview จะตอบ `ALREADY_MEMBER` และปุ่มนั้นจะไม่เรนเดอร์
⇒ **ครึ่งลบของ AC-4.2 ยังถูกคุ้ม แค่เปลี่ยนรูป** — บันทึกไว้เพราะเป็นการ*ลดรูป*หลักฐาน ไม่ใช่การเพิ่ม

---

## 6. ของใหม่ที่ผมเจอในรอบนี้ — ไม่บล็อก แต่มีเจ้าของ

### N-1 · หน้าต่าง 30 นาทีบนเครื่องที่ใช้ร่วมกัน — 🟡 **ยังเปิด แต่มีที่อยู่แล้ว** · owner: **@frontend + @security-reviewer**

> **อัปเดต `6422142`:** สิ่งที่ผมบ่นว่า *"อยู่ในคอมเมนต์อย่างเดียว"* ถูกแก้แล้ว — มีแถว **OPEN** ใน
> `build-defects.ts` พร้อมชื่อเจ้าของ และมีเทสต์ 2 ตัว · **แต่ตัวพฤติกรรมยังไม่ถูกแก้ ⇒ ผมไม่นับว่าปิด**
> · และพูดตามตรง: เทสต์ที่เดินสถานการณ์ assert สิ่งเดียวกับเคสนาฬิกาที่มีอยู่แล้ว ⇒ **ได้ "ชื่อ" ไม่ได้ coverage ใหม่**
> ตัวที่มีเขี้ยวคือ `HOLD_MS <= 30 min` ซึ่งวัดกับ*ขอบเขต* ⇒ ขยายหน้าต่างเมื่อไหร่แดง (§7.1)

`HOLD_MS = 30 นาที` · hold ถูกทิ้งเมื่อ: มีลิงก์ใหม่ใน URL · `endSession` (ออกจากระบบ) · mount route นอกเส้นทาง · หมดอายุ
**แต่ไม่มีข้อไหนคลุมกรณีนี้:** A เปิดคำเชิญ → แตะ "เข้าสู่ระบบ" → **เดินหนีไปเลยโดยไม่ล็อกอิน** (ยังค้างที่ `/login` ซึ่งอยู่ใน
เส้นทาง ⇒ ไม่ถูกทิ้ง · ไม่มี session ⇒ `endSession` ไม่เคยถูกเรียก) → **B มานั่งแท็บเดิมแล้วล็อกอินด้วยบัญชีตัวเอง**
⇒ `destinationAfterLogin()` = `/invite` ⇒ B เห็น **ชื่อร้าน + อีเมลที่ mask แล้ว** ของคำเชิญของ A

- **B เข้าร่วมไม่ได้** — server ปฏิเสธด้วย `INVITATION_EMAIL_MISMATCH` (int test ยิงอยู่แล้ว) ⇒ **ไม่ใช่ privilege escalation**
- ข้อมูลที่หลุดคือของที่ **ใครถือลิงก์ก็เห็นอยู่แล้วโดยออกแบบ** (`api-spec §3.14` — preview เป็น public-by-token)
- **ที่ยังแปลกคือ B ไม่เคยได้รับลิงก์นั้น** แต่ถูกพาไปเห็นเป็นเวลาสูงสุด 30 นาที
- **โค้ดรู้ตัวอยู่แล้ว** — คอมเมนต์ใน `pending-invite.ts` เขียนไว้เองว่า *"so an abandoned login on a shared
  computer does not route the next person to somebody else's invitation … but they should not be shown it"*

⇒ **ปัญหาไม่ใช่ว่าไม่มีใครคิด แต่คือมันอยู่ในคอมเมนต์อย่างเดียว**: ไม่มีแถวใน `build-defects.ts` · ไม่มีใน
`security-review-*.md` · ไม่มีเทสต์ที่ตั้งชื่อตามสถานการณ์นี้ (10 เคสของ `pending-invite.test` pin `HOLD_MS` ไว้จริง
แต่ไม่มีเคสไหน**พูดว่าทำไมมันต้องสั้น**) ⇒ วันหน้ามีคนขยาย `HOLD_MS` เพื่อความสะดวก **จะไม่มีอะไรแดง**
· `security-review-build-B.md:135` เขียนไว้เองว่า *เครื่องร่วม/เครื่องในร้าน = persona ของ OmniStock* ⇒ ไม่ใช่สถานการณ์สมมุติ
**ข้อเสนอ (ไม่ใช่คำสั่ง — ไม่ใช่โดเมนผม):** ลด `HOLD_MS` ให้พอดีกับการสมัคร 1 ครั้ง **หรือ** เพิ่มเทสต์ที่ชื่อบอกเหตุผล
**ไม่บล็อก** — blast radius = ชื่อร้าน + อีเมล mask ใน 30 นาที และมี server ปฏิเสธชั้นสุดท้าย

### N-2 · ~~`Referrer-Policy` ไม่มีใครพิสูจน์ว่าเซิร์ฟเวอร์ส่งจริง~~ — ✅ **ปิดแล้วใน `6422142`**

> **ปิดจริง และผมยืนยันเอง** (§7.1): เคสเบราว์เซอร์ 2 ตัวใน `e17-invite-response-headers.spec.ts` —
> ตัวบวกยิง `/invite` จริง ตัวคุม assert ว่า `/login` **ไม่มี** header (คุมการเผลอเหมารวมทั้งแอป) ·
> floor 29 → 31 · **ผมรันเอง 2/2 ผ่าน + `curl` ตรงกับเซิร์ฟเวอร์จริง** · ข้อความเดิมเก็บไว้ข้างล่างเป็นบันทึกว่าเจออย่างไร

#### (บันทึกเดิม) owner: **@devops + @qa (ผมเอง)**

`response-headers.test.ts` อ่าน `next.config.mjs` ตัวจริง (ดีกว่าสำเนา) **แต่เขียนไว้เองว่า**
*"vitest never boots a Next.js server, so it cannot see whether Next actually serves whatever `headers()` returns"*
⇒ หลักฐานเดียวว่า header ออกมาจริงคือ `curl -sI` ครั้งเดียวในรายงานของ devops — **ไม่มี regression guard**
· ผม grep แล้ว: `apps/web/e2e/` **ไม่มีเคสไหนแตะคำว่า `referrer` เลย** ทั้งที่เลนนั้น**บูต Next จริงอยู่แล้วทุกรอบ CI**
⇒ เพิ่ม assertion บน response header ของ `/invite` ใน Playwright = ไม่กี่บรรทัด ได้ guard ถาวร
**ไม่บล็อก** — header นี้เป็น defence-in-depth ชั้นที่สอง (ชั้นแรกคือ layout effect ที่ถอด token ออกจาก URL ก่อน paint ซึ่ง E-12 คุมอยู่)

### N-3 · ~~ปุ่ม "เชิญใหม่อีกครั้ง" ไม่มี AC~~ — ✅ **ปิดแล้วใน `84a3277`** (product เคาะ)

> product เลือกทาง *เติม AC ย้อนหลัง* (ไม่ใช่ถอดปุ่มออก และไม่ใช่ประกาศว่าไม่ต้องมี AC) ⇒ **AC 35 → 36**
> · product เขียนเองว่า *"การ remap ตาราง AC↔เคสเป็นสิทธิ์ของ @qa"* — ถูกต้อง และผม remap แล้วที่ **§4.2**
> · product ยัง**ไม่กลบ**จุดอ่อน: บันทึกเองว่าหลักฐานเป็น unit-only ⇒ ผมยืนยันด้วยการไล่ทีละอนุประโยค (§4.2)
> · **เหลือ:** เคสเบราว์เซอร์ 1 ตัว · trigger = ครั้งถัดไปที่แตะจอสมาชิก · **ไม่บล็อก**

#### (บันทึกเดิม) owner: **@product (รับทราบ) + @qa**

เป็นของที่**ผู้ใช้เห็นและกดได้** ลงมาตอน QA · มี 8 unit case · server บังคับสิทธิ์อยู่แล้ว · **ไม่มี AC และไม่มีเคสเบราว์เซอร์**
· ผมไม่นับเป็นแดง (§17.1 บังคับว่า *AC ต้องมีเทสต์* ไม่ได้ห้าม *เทสต์ที่ไม่มี AC*) และไม่ขอให้ถอดออก
**แต่บันทึกไว้เพราะรูปนี้คุ้นเกินไป**: ของที่ผู้ใช้เห็น ลงมาหลังเอกสารถูกเคาะ ⇒ ไม่มีใครเคยตกลงว่า "ถูก" แปลว่าอะไร
⇒ **@product เคาะว่าจะเติม AC ย้อนหลัง หรือประกาศว่าเป็น UX affordance ที่ไม่ต้องมี AC** — เลือกอย่างใดอย่างหนึ่ง ก่อน F-003

### N-4 · diff ของ branch แตะ protected path 2 ไฟล์ — owner: **@PM (ยืนยัน) · qa บันทึก**

Gate E ของ skill `quality-gate` มีข้อ *"diff does not touch protected paths (`.claude/**`, `WEB_TEAM.md`, …)"*
· `git diff --name-only main...HEAD` แสดงว่า branch นี้**แตะ**: `.claude/skills/quality-gate/SKILL.md` · `WEB_TEAM.md`
(commit `90f0b21`) และ `docs/01-data-model.md` · `docs/02-architecture.md` · `docs/DECISIONS.md` (commit `3570f2d` = Gate 2 sync-back ซึ่ง**คาดหมายอยู่แล้ว**)

- `90f0b21` เขียนใน body ว่า *"Approved by the user"* — **ผมยืนยันข้ออ้างนั้นจากตรงนี้ไม่ได้** ⇒ ผมบันทึกว่าเป็น
  **"คำกล่าวอ้างว่าได้รับอนุมัติ" ไม่ใช่ "อนุมัติที่ผมตรวจแล้ว"**
- **สิ่งที่ผมตรวจได้และตรวจแล้ว: มันเป็นการ*เพิ่ม*เกณฑ์ ไม่ใช่*ลด*** (reachability เข้า Gate D) ⇒ **เป็นไปไม่ได้ที่จะถูกแก้เพื่อให้ F-002 ผ่าน** ซึ่งคือทิศทางเดียวที่ผมจะต้องคัดค้าน
- **และผมเอาเกณฑ์ใหม่นั้นมาใช้กับ F-002 จริง ไม่ได้ยกเว้นให้ตัวเอง** → §6.5

### 6.5 · Gate D ตรวจใหม่ด้วยเกณฑ์ reachability (เกณฑ์นี้ยังไม่มีตอน v1)

| พื้นผิว | มีเส้นทางไปถึงไหม | เทสต์ที่**เดินเส้นนั้น** (ไม่ใช่แค่สร้างจอ) |
|---|---|---|
| `/invite` (รวม NO_TOKEN) | ✅ แตะลิงก์จากแชท/เมล | **E-05 · E-12 เปิด URL จริงบนเบราว์เซอร์** + เปิดจริงบน Android 13 Chrome ด้วยมือ |
| หน้าสมาชิก (ที่ปุ่ม "เชิญใหม่อีกครั้ง" อยู่) | ✅ | **E-08 / E-08b** เดินเข้าไปจริงบนเบราว์เซอร์ · mobile **E-10** + nav ชั่วคราวที่ปิด B-18 |
| ปุ่มออกจากระบบ (ที่เพิ่งได้ `endSession`) | ✅ | **E-16** (เบราว์เซอร์) + `AppShell.logout.test.tsx` **กดปุ่มจริง** |
| `/` | ✅ | **E-15 / E-15b** (ปิด B-14) |

⇒ **Gate D ผ่านภายใต้เกณฑ์ใหม่ด้วย** · ของที่เคยเป็นรู (B-14/B-16/B-18) ถูกปิดไปแล้วทั้งสามและมีเคสเดินเส้นจริง

### 6.6 · ของค้างจาก v1 — ผมตรวจซ้ำแล้ว **ทั้งสามข้อถูกยกไป `forward-commitments.md` พร้อม trigger จริง**

| ค้างจาก v1 | วันนี้ |
|---|---|
| §10 gate 4 ตัวไม่มี implementation (G-06/G-09/G-11/G-12) | ✅ **อยู่ใน `forward-commitments.md` บรรทัด 303** · trigger = *ก่อน F-003 เปิด role CRUD* · owner = qa + devops — **ตรงกับที่ผมสั่งตัวเองไว้ใน v1 §6.1** |
| `--passWithNoTests` อีก 2 workspace | ✅ บรรทัด 304 · trigger = *commit แรกที่มีโค้ดจริง* · owner = devops |
| G-08 ถูกละเมิดตามตัวอักษรตอน B-10 | ✅ บรรทัด 305 · trigger = *ครั้งถัดไปที่แตะ `ledger-guard.ts`* · owner = backend-api · **ผม verify ซ้ำ: `git diff --name-only main...HEAD` ยังแตะไฟล์ ledger แค่ไฟล์เดียวคือ `ledger-guard.ts` และเป็น import path** |
| doc drift B-18 ใน `manual-pass-results.md` | ✅ แก้แล้ว (หัวข้อ §B-18 เขียน "ปิดแล้ว" ตรงกับหัวตารางแล้ว) |
| ป้ายเคส E-10/E-11 ชนกับชื่อไฟล์ | ✅ rename เป็น `e15-` / `e16-` แล้ว · `build-defects.ts` pin ตามไปแล้ว · เลนยัง 29/29 |

---

## 7. verdict นี้จะ **ผิด** เมื่อไหร่ — เงื่อนไขที่ทำให้ต้องกลับมาตัดสินใหม่

**เขียนเป็นเงื่อนไข ไม่ใช่คำเตือนลอย ๆ** · ข้อใดข้อหนึ่งเกิด ⇒ `go-with-known-risk` ฉบับนี้ **เป็นโมฆะ** และต้องออก verdict ใหม่:

| # | เงื่อนไข | ทำไมมันทำให้ verdict นี้ผิด | ต้องทำอะไร |
|---|---|---|---|
| **R-1** | **เปิดให้ผู้ใช้ภายนอก (ไม่ใช่ dogfood) แตะ F-002** | ความเสี่ยงที่ถูกรับไว้คิดบนสมมติฐาน *"คนที่เจอปัญหาคือพวกเราเอง และบอกกันได้ทันที"* · ลูกค้าจริงที่คำเชิญไม่ทำงาน **ไม่มีทางสำรอง** (ไม่มี SMTP จนถึง F-081) | **M-01 กลับมาเป็น blocking** — และต้องเป็นเวอร์ชันที่มี **origin https จริง** ไม่ใช่ `adb reverse` |
| **R-2** | **ship iOS** (หรือแตะ `AppDelegate.swift` ใน F-006) | `AppDelegate.swift` ยังไม่เคยถูก compile — privacy overlay + `localOnly` ของคลิปบอร์ด อาจ degrade เป็น no-op โดยไม่มีเลนไหนแดง | **M-07ค-iOS กลับเป็นแดงทันที** · verdict นี้ **ไม่ครอบ iOS** ตั้งแต่ต้น |
| **R-3** | **มี commit ใหม่เข้า branch นี้** หลัง `65df986` — **แต่ไม่ใช่ทุก commit · ดู §7.1** | ตัวเลขทุกตัวใน §2 ผูกกับ headSha นี้ | **แยกตามชนิดของ commit ที่ §7.1** ไม่ใช่นับจำนวน commit |
| **R-4** | **มีคนเดิน M-01 แล้ว "ไม่ผ่าน"** | verdict นี้รับความเสี่ยงไว้บนสมมติฐานว่า*ยังไม่รู้* ไม่ใช่ว่า*รู้แล้วว่าพัง* — `release-gate-f §4` แยกไว้เองว่า **"ของพัง" คนละกลุ่มกับ "ยังพิสูจน์ไม่ได้"** | กลับเป็น **NOT DONE** ทันที ไม่ต้องรอใครอนุมัติ |
| **R-5** | **F-081 (SMTP/onboarding) หรือ F-003 (role CRUD) เริ่ม** | F-081 เปลี่ยนสมมติฐาน "ไม่มีทางสำรอง" · F-003 ทำให้ G-12 ที่ไม่มีตัวตนกลายเป็นของจำเป็น (`forward-commitments` บรรทัด 303) | ทบทวน §3.1 และปิด §10 gate ทั้ง 4 ตัวก่อน |

### 7.1 R-3 ฉบับที่ใช้ได้จริง — **ผมเขียน R-3 ผิดเอง และนี่คือการแก้**

> **@product พูดถูก และ `release-gate-f.md §7.4` ตั้งคำถามถูก** (2026-09-20)
> R-3 ฉบับแรกเขียนว่า *"มี commit ใหม่ = โมฆะ"* ซึ่ง**ทำลายตัวเอง**: ลายเซ็นรับความเสี่ยงคือ commit ·
> ตัว verdict นี้เองก็คือ commit (`a89560a`) · merge commit ก็คือ commit ⇒ อ่านตามตัวอักษรแล้ว
> **ปิด F-002 ไม่ได้เลยตลอดกาล** · **product ปฏิเสธที่จะตีความแทนผมและเขียนข้อโต้แย้งไว้ — ถูกต้องแล้ว**
> ของที่ผมหมายถึงคือ **"หลักฐานใน §2 ยังพูดถึงต้นไม้ก้อนเดียวกับที่ผมตัดสินอยู่หรือเปล่า"**
> ไม่ใช่ *"มีคนพิมพ์ `git commit` กี่ครั้ง"* · เกณฑ์ที่ถูกจึงเป็น **"commit นั้นแตะอะไร"**

**แยกตาม*สิ่งที่ถูกแตะ* ตัดสินด้วย `git diff --name-only <base> <sha>` — ไม่ใช่ด้วยข้อความ commit**
(ข้อความ commit เป็น*คำกล่าวอ้าง* · รายชื่อไฟล์เป็น*ของจริง* — หลักเดียวกับ N-4)

| ชนิด | นิยามที่ตรวจด้วยเครื่องได้ | โมฆะไหม | ต้องทำอะไรก่อน merge |
|---|---|---|---|
| **A1 · docs-only, ไม่มีข้อกำหนด** | ทุกไฟล์อยู่ใต้ `docs/**` หรือ `*.md` ที่ root · **และไม่มีไฟล์ไหนเป็นสเปกที่ถือ AC** | **ไม่โมฆะ · ไม่ต้องทำอะไร** | ไม่มี artifact ที่รันได้เปลี่ยน ⇒ ทุกเลนกำลังทดสอบโค้ดชุดเดิมเป๊ะ · ครอบคลุม: ลายเซ็นของ product · ตัว verdict นี้ · `forward-commitments.md` · `RETRO.md` ⇒ **ลายเซ็นไม่ทำให้ตัวเองเป็นโมฆะ** |
| **A2 · docs-only แต่แตะ *สเปกที่ถือ AC*** ⚠️ | ดิฟเป็น docs ล้วน **แต่มี `docs/features/F-XXX*.md` (ตัวที่มีรายการ AC) อยู่ด้วย** | **ไม่โมฆะ** *(เลนไม่ต้องรันใหม่)* **แต่ต้อง remap §4 ก่อน** | **ผมเขียน A ไว้กว้างเกินไปรอบแรก และ `84a3277` คือเคสที่พิสูจน์ว่าผิด** — เอกสารไม่ได้ *inert* เสมอไป: สเปกคือ**ที่อยู่ของ AC** ซึ่งเป็น*ตัวตั้ง*ของ §4 และ §17.1 ⇒ เติม/ลบ AC ได้โดยไม่แตะโค้ดเลย · ไม่ต้องรันเลนใหม่ (ไม่มีอะไรรันได้เปลี่ยน) แต่ **qa ต้องไล่ AC↔เคสใหม่แล้วบันทึกตัวเลขที่ถูก** |
| **B · test/CI-only** | แตะได้เฉพาะ `**/*.test.*` · `apps/web/e2e/**` · `apps/*/integration_test/**` · `apps/api/test/**` · `.github/workflows/**` · `tool/ci/**` · **ไม่มีไฟล์ใดใต้ `src/`, `prisma/`, `packages/contracts/openapi/**` เลย** | **ไม่โมฆะ** *(มีเงื่อนไข 2 ข้อ)* | (1) **CI เขียวครบบน sha ใหม่** — เทสต์ที่เพิ่มเข้ามาอาจเปิดโปงของที่พังอยู่แล้ว และนั่นคือของจริง ต้องขึ้นมา ห้าม skip (2) **พื้น (floor) ของทุกเลนห้ามลดลง** — เลน B ทำร้าย verdict ได้ทางเดียวคือ*ทำให้เลนอ่อนลงเงียบ ๆ* (ลด floor / ลบเคส) |
| **C · production** | แตะไฟล์ใดก็ตามใต้ `apps/*/src/**` · `packages/*/src/**` · `prisma/**` · `packages/contracts/openapi/**` · generated client · `next.config.mjs` · `package.json` (dependency) | **โมฆะ** | ตรวจใหม่เต็ม: CI 9/9 บน sha นั้น + §4 (AC↔เคส) ถ้าแตะ `/invite`/auth/สิทธิ์/สคีมา |
| **D · merge commit** | ดึงเนื้อของ base เข้ามา ⇒ **เป็น C เสมอโดยนิยาม** แม้ฝั่งเราจะไม่แก้อะไรเลย เพราะโค้ดที่รันจริงหลัง merge ไม่ใช่โค้ดที่ผมตัดสิน | **โมฆะ** | **§7.2** |

⇒ **ตอบ @product โดยตรง: ลายเซ็นรับความเสี่ยงเป็นชนิด A · ไม่ทำให้ verdict เป็นโมฆะ · เซ็นได้เลย**

**ตรวจย้อนกับ 2 commit ที่ลงไปแล้วหลัง `65df986` (ผมตรวจรายชื่อไฟล์เอง ไม่ได้อ่านแต่ข้อความ commit):**

| sha | ไฟล์ที่แตะ | ชนิด | ผล |
|---|---|---|---|
| `a89560a` | `docs/features/F-002/gate-e-verdict.md` (+401 −0) ไฟล์เดียว | **A1** | ไม่โมฆะ |
| `84a3277` | `F-002-organization-license-membership.md` · `release-gate-f.md` · `forward-commitments.md` | **A2** ⚠️ | ไม่โมฆะ · **แต่เติม AC ใหม่ 1 ข้อ ⇒ ผม remap §4 แล้ว: 35 → 36** (§4.2) |
| `20d8c80` | `gate-e-verdict.md` ไฟล์เดียว | **A1** | ไม่โมฆะ |
| `54a1c20` | merge commit | **D** | โมฆะ ⇒ ตรวจครบตาม §7.2 แล้ว |
| `69b6960` | `docs/RETRO.md` ไฟล์เดียว | **A1** | ไม่โมฆะ |
| `6422142` | `apps/web/e2e/e17-*.spec.ts` · `apps/web/src/lib/session/pending-invite.test.ts` · `apps/api/test/build-defects.ts` · `.github/workflows/ci.yml` · **ไม่มีไฟล์ production แม้แต่ไฟล์เดียว** | **B** | ไม่โมฆะ · เงื่อนไข (2) ผ่าน: floor **ขึ้น** 29 → 31 ไม่ได้ลง · เงื่อนไข (1) รอ CI บน sha ใหม่ |

**ผมรันเองบน `6422142` (2026-09-20):**

| คำสั่ง | ผล | exit |
|---|---|---|
| `pnpm --filter web test` | **52 files · 399 passed · 0 skipped** (397 → 399 = เคส N-1 สองตัวพอดี · `pending-invite.test.ts` 10 → 12) | 0 |
| `pnpm --filter api test` | **704 passed \| 210 skipped** (ไม่ขยับ) · `build-defects.test.ts` **6/6** ⇒ แถว N-1 ผ่าน gate ของทะเบียนเอง | 0 |
| `curl -D - http://localhost:3001/invite?token=probe` | **`Referrer-Policy: no-referrer`** | — |
| `curl -D - http://localhost:3001/login` | **ไม่มี header** ⇒ scope แคบจริง | — |
| `npx playwright test e17-invite-response-headers --project=chromium --no-deps` | **`2 passed (4.9s)`** | 0 |

> **N-2 ปิดแล้วและผมยืนยันด้วยตาตัวเอง** — เคสสองตัวนี้ไม่ต้องใช้ DB (ตรวจแค่ response header ของ document
> load บน route สาธารณะ) ⇒ **Docker ดับไม่ได้ขวางข้อนี้** และผมรันผ่านทั้ง `curl` และ Playwright จริง
> · สิ่งที่ผมยัง**ไม่ได้**ทำคือรันมันในเลนเต็ม (ต้องมี `setup` project ที่ต้องใช้ DB) ⇒ **floor 31 ยังรอ CI พิสูจน์**
>
> **N-1 ลดชั้นจาก "ไม่มีที่อยู่" → "มีทะเบียน + มีเจ้าของ"** · ข้อสังเกตที่ต้องพูดตามตรง: เคส
> *"an abandoned hold does not route the NEXT person…"* assert สิ่งเดียวกับเคสนาฬิกาที่มีอยู่แล้ว
> ⇒ **มูลค่าของมันคือ "ตั้งชื่อให้สถานการณ์" ไม่ใช่ coverage ใหม่** · ตัวที่มีเขี้ยวจริงคือ
> `expect(HOLD_MS).toBeLessThanOrEqual(30 * 60_000)` ซึ่งวัดกับ*ขอบเขต*ไม่ใช่กับ*ค่าคงที่* ⇒ ขยายหน้าต่างเมื่อไหร่ **แดง**
> · **พฤติกรรมเองยังไม่ถูกแก้ และแถวในทะเบียนเขียนว่า OPEN ซึ่งถูกต้อง** — ผมไม่นับว่า N-1 ถูกปิด

### 7.2 merge commit — ต้องพิสูจน์อะไรก่อน verdict นี้จะครอบ sha ที่ merge แล้ว

**สมมติฐานของ @PM ถูกต้อง ผมยืนยัน** (CI เขียวเต็มบน merge commit เอง · `contracts-drift` มีน้ำหนักพิเศษ · floor 31 พิสูจน์ว่าเลนโต) **และขอเพิ่มอีก 3 ข้อ:**

| # | ต้องผ่าน | ทำไม |
|---|---|---|
| 1 | **CI 9/9 เขียวบน merge commit เอง** — ไม่ใช่ของ parent ฝั่งใดฝั่งหนึ่ง | โค้ดที่รันหลัง merge ไม่เคยมีอยู่ในต้นไม้ไหนมาก่อน · run `35491463371` อยู่บน `65df986` ซึ่ง**เก่ากว่าทั้ง merge และ 2 commit ข้างบน** |
| 2 | **`contracts-drift` เขียว** | ✅ เห็นด้วยเต็มที่ — merge ดึง generated code เข้ามา · job นี้ regen แล้ว fail ถ้ามี diff ค้าง **รวม untracked** ⇒ เป็นตัวเดียวที่จับ "merge ทำให้ client กับ spec คนละตัว" ได้ |
| 3 | **floor ต้องพิสูจน์ว่าเลนโต ไม่ใช่เงียบ ๆ หาย** — web **≥31** · mobile-e2e **≥4** · `integration-api` **212 · skip 0** · `db-migrate` **185 · skip 0** | ✅ เห็นด้วย · เพิ่ม: **สามเลนหลังต้อง `skip 0` ด้วย ไม่ใช่แค่ผ่าน** — merge ที่ทำ env ของ job หายไปจะทำให้ int lane skip เงียบแล้วเขียว (I-37 / [[green-locally-not-tested]]) |
| **4 ใหม่** | **ไฟล์ต้องไม่หาย: `comm -23 <(git ls-tree -r --name-only <parent>) <(git ls-tree -r --name-only <merge>)` = ว่าง ทั้งสอง parent** | ผมตรวจเองกับ `fbcd5d8` แล้ว: ต่างกัน **8 ไฟล์ และทั้ง 8 เป็นการ*ย้ายที่* ไม่ใช่การหาย** (`components/auth/**` → `features/auth/components/**` · `i18n/auth.ts` → `features/auth/i18n.ts` — ผมยืนยันว่าปลายทางมีอยู่จริงใน HEAD) ⇒ **"0 deleted" ของ @PM ถูกต้อง** · แต่ต้องตรวจซ้ำบน*ผล merge* เพราะการ resolve เป็นงานที่ทำมือได้ |
| **5 ใหม่ ⚠️** | **`extractTraceId` ต้อง *ไม่* กลับมาเป็น export ใน `apps/api/src/common/index.ts`** | นี่คือกับดักเฉพาะของ merge นี้ · ฝั่ง `fbcd5d8` **มี** `export { …, extractTraceId }` · ฝั่งเรา**ถอดออกโดยตั้งใจ** (NEW-7: ของเดิมสะท้อน `X-Request-Id` ของ client กลับไปใน response — ปิดไปแล้วใน `security-review-build-B.md:315`) และทิ้งคอมเมนต์อธิบายไว้ที่บรรทัดเดิม ⇒ **การ resolve แบบ "เอาทั้งสองฝั่ง" จะเปิด finding ด้านความปลอดภัยที่ปิดไปแล้วกลับมาเงียบ ๆ** · ผมตรวจแล้วว่าการถอดออกเป็นของตั้งใจจริง (อ้างใน `architecture.md §15` แถว 2 + security-review 2 ไฟล์) |
| **6 ใหม่** | ถ้า merge แตะไฟล์ใต้ `apps/*/src/**` เกินกว่าการ resolve conflict เชิงกลไก ⇒ **ตรวจ §4 (AC↔เคส) ใหม่ด้วย** | merge เป็นชนิด C · §4 ผูกกับพฤติกรรม ไม่ใช่กับ sha |

**ผมขอตรวจ sha สุดท้ายเองก่อน @release merge — ใช่ครับ ขอ** และเหตุผลไม่ใช่พิธีการ:
ข้อ 5 เป็นของที่ **CI จับไม่ได้** (โค้ดที่ compile ผ่านและเทสต์เขียว แต่เปิดรูที่ปิดไปแล้ว) ⇒ ต้องมีคนอ่านผล resolve
· **สิ่งที่ผมต้องการมีแค่: sha ของ merge commit + เลข CI run บน sha นั้น** · ผมจะตอบเป็น "ครอบ / ไม่ครอบ"
แล้วเติมบรรทัดเดียวใน §1 ว่า verdict นี้ครอบ sha ไหน ⇒ **ไม่ต้องออก verdict ใหม่ทั้งฉบับ ถ้าทั้ง 6 ข้อผ่าน**

### 7.3 ✅ ผลการตรวจ sha สุดท้าย — `69b6960` · CI run `35523016859` (qa, 2026-09-20)

**ผ่านครบ 6 ข้อ · ผมรันทุกคำสั่งเอง ไม่ได้รับตัวเลขจาก @PM** · `git status` สะอาด · `gh run view … -q .headSha`
= `69b6960a3708eeb88ca43f914e75ca3d1590e48e` ⇒ **ตรงกับ sha ที่จะ merge** · **9/9 job success**
(⚠️ ผมไม่ใช้ `35491463371` และ `35522799246` เป็นหลักฐาน — ตัวแรกเก่ากว่า merge ตัวหลังถูก cancel · **@PM ไม่เสนอมันมาด้วย ซึ่งถูกต้อง**)

| # | เกณฑ์ | ผลที่ผมเห็นเอง | ✓ |
|---|---|---|---|
| 1 | CI 9/9 บน merge commit เอง | `headSha` ตรง · ทั้ง 9 job `success` | ✅ |
| 2 | `contracts-drift` เขียว | success บน merge ที่ดึง generated code เข้ามา | ✅ |
| 3 | floor โตไม่ใช่หด · เลน DB ต้อง `skip 0` | `playwright: expected=31 unexpected=0 flaky=0 skipped=0` → *ran 31 (floor 31)* · int **212/0/0** + guard OK · db **185/0/0** + guard OK · mobile **4** · flutter **451** · node-ci: web **399** api **704\|210** core-domain **384** db **161\|24** — **ตรงกับที่ผมรันเองบน `6422142` ทุกตัว** | ✅ |
| 4 | ไม่มีไฟล์หาย เทียบ**ทั้งสอง** parent | `comm -23` กับ parent ฝั่งเรา (`84a3277`) = **ว่าง** · กับ `fbcd5d8` = **8 path และทั้ง 8 คือการย้ายที่** · ผมยืนยันปลายทางครบทั้ง 8 ด้วย `git ls-tree` | ✅ |
| 5 | `extractTraceId` ห้ามกลับมา | `grep -rn "extractTraceId\|TRACE_ID_PATTERN" apps/api/src packages/*/src apps/web/src` → **1 hit เดียว คือคอมเมนต์ที่ `common/index.ts:15`** ไม่มี export ไม่มี implementation | ✅ |
| 6 | production เกินกว่า resolve เชิงกลไก | `git diff --stat 84a3277 54a1c20` = **ว่างสนิท** ⇒ merge ไม่ได้เพิ่มอะไรเลย · `84a3277..69b6960` เหลือแต่ `RETRO.md` + ไฟล์นี้ ⇒ **§4 ไม่ถูกแตะโดย merge** | ✅ |

**ข้อ 7 — ผมเพิ่มเอง และมันคือข้อที่เกือบพลาด:** `-X ours` อันตรายสองทาง · ข้อ 4/5 กันทาง *"ของเราหาย"*
แต่ไม่มีข้อไหนกันทาง ***"ของ base หาย"*** — `-X ours` **ทิ้งฝั่ง base เงียบ ๆ** ในไฟล์ที่แก้ทั้งสองฝั่ง
· ผมตรวจเอง: ไฟล์ที่ **base แตะแต่เราไม่แตะ** ตั้งแต่ merge-base `25ae7f3` มี **8 ไฟล์ = ชุดเดียวกับที่ถูก rename**
· แล้วผม diff เนื้อของ base กับสำเนาที่ย้ายที่แล้วทีละไฟล์: **7 ไฟล์ต่างกัน*เฉพาะบรรทัด import* — 0 บรรทัดที่เป็นเนื้อหา**
(กรองบรรทัด `import`/`vi.mock` ออกแล้วเหลือ **ศูนย์**) · ไฟล์ที่ 8 (`i18n/auth.ts`) ต่างจริงและ**ต่างถูกทาง**:
ฝั่งเราคือ copy ของ **M-02** ที่เลิกชี้ไปหาจอที่ไม่มีอยู่ ⇒ ของ base คือของเก่าที่ต้องถูกแทน
⇒ **ไม่มีงานของ base หายไปแม้บรรทัดเดียว** · @PM สรุปถูกในสาระ · **ตัวเลขที่ถูกคือ "1 ไฟล์เหมือนเป๊ะ · 6 ไฟล์ต่างแค่ import · 1 ไฟล์ถูกแทนโดยตั้งใจ"** ไม่ใช่ "หกไฟล์เหมือนเป๊ะ สองไฟล์ต่างที่ import"

**ของที่ @PM เขียนคลาดไป 1 จุด (ไม่กระทบผล):** `response-header-policy.int.test.ts` **ไม่ใช่ไฟล์ใหม่** —
ผม diff breakdown ของ int lane ระหว่าง run เก่ากับ run ใหม่แล้ว **เหมือนกันทุกบรรทัด 13 ไฟล์**
· จุดนี้ควรถูกทักเพราะ*ทิศตรงข้ามน่ากลัว*: ถ้ามีไฟล์ใหม่โผล่จริงแต่ยอดรวมยังเป็น 212 แปลว่า**เคสหายไปที่อื่น** ⇒ ตรวจแล้ว ไม่ใช่กรณีนั้น

> **สรุปการเซ็น:** ✅ **verdict `go-with-known-risk` ฉบับนี้ครอบ `69b6960`** · @release merge ได้ในแง่ของ Gate E
> · **ไม่ต้องออก verdict ใหม่** · ช่องแดงยังเป็นช่องเดิมช่องเดียว (§17.6 / M-01) และไม่มีอะไรในรอบนี้แตะมัน

> ⛔ **ข้อที่ห้ามเข้าใจผิด:** §7.1/§7.2 **ไม่ได้ผ่อนอะไรของ M-01 เลยสักนิด** — มันตอบแค่คำถาม
> *"หลักฐานใน §2 ยังพูดถึงต้นไม้ก้อนเดียวกับที่ merge หรือเปล่า"* · **M-01 ยังไม่ถูกพิสูจน์เหมือนเดิมทุกตัวอักษร**
> และ verdict ยังเป็น `go-with-known-risk` ไม่ว่าจะ merge บน sha ไหน

---

## 8. ข้อความถึง @release และ @product (ผมไม่แก้ `release-gate-f.md` — เป็นเอกสารของ release)

**ถึง @release:**

1. `release-gate-f.md §4.1` แถว *"qa เขียว (Gate E)"* วันนี้ควรอ่านว่า
   **`⚠️ go-with-known-risk (qa, 2026-09-20, บน 65df986)`** — **ไม่ใช่ `qa-green`**
   · §7 ของเอกสารคุณมีแถวที่รอรูปนี้อยู่แล้ว: *"ถ้า verdict เป็น go-with-known-risk: เซ็นรับความเสี่ยง … product"*
2. **ห้ามเขียนที่ไหนว่า M-01 ผ่าน** · ถ้อยคำที่ถูกคือ *"M-01 ยังไม่ถูกพิสูจน์ · user รับความเสี่ยงไว้ 2026-09-20"*
3. §4.1 แถวเดิมเขียนว่า M-01 ใช้ *"~20 นาที"* — **ตัวเลขนั้นมาจากผมเองใน v1 และวันนี้ผมแก้มัน**:
   ทางที่ใช้ได้ต้องต่อ USB + `adb reverse` (คำสั่ง LAN IP เดิม**บูตไม่ขึ้น** §3.1) และ**ยังพิสูจน์ลิงก์รูปร่าง production ไม่ได้**
   ⇒ อย่าวางแผนบนสมมติฐาน "20 นาทีแล้วเขียว"
4. verdict นี้ **ไม่ครอบ iOS** และไม่ครอบการเปิดให้ผู้ใช้ภายนอก — ดู §7 (R-1/R-2)

**ถึง @product:**

1. ลายเซ็นรับความเสี่ยงของคุณใน `release-gate-f.md §7` **ยังว่างอยู่** — verdict นี้ไม่ได้เซ็นแทนคุณ
   · สิ่งที่คุณกำลังเซ็นรับคือ §3.1 ตารางล่าง (*"ยังไม่รู้ / จะกัดได้ยังไง"*) ไม่ใช่แค่คำว่า "M-01"
2. **M-02 ยังไม่มีคำตอบ** (ยกมาจาก v1 §7.2 ไม่ใช่ของใหม่): *ไม่มีจอไหนเรียก `adminResetPassword` เลย*
   ⇒ ราคาที่เคาะไว้ใน D-030 ยังไม่เคยมีใครเห็นบนจอ · พฤติกรรม API ถูกและมีเทสต์ ⇒ **ไม่บล็อก** แต่คุณควรรู้ก่อนเซ็น
3. **N-3** — ปุ่ม "เชิญใหม่อีกครั้ง" เป็นของที่ผู้ใช้กดได้แต่ไม่มี AC · เคาะว่าจะเติม AC ย้อนหลังหรือประกาศว่าไม่ต้องมี

---

## 9. ถ้าจะทำให้เป็น ✅ DONE จริง ๆ (ไม่ใช่ go-with-known-risk)

```
เหลืองานเดียวเหมือนเดิม: M-01 — แต่คำสั่งเก่าของผมใช้ไม่ได้ นี่คือฉบับที่ถูกต้อง
  1. ต่อโทรศัพท์จริงด้วย USB + เปิด USB debugging
  2. adb reverse tcp:3001 tcp:3001 && adb reverse tcp:3000 tcp:3000 && adb reverse --list
     ⛔ ห้ามตั้ง WEB_APP_BASE_URL เป็น LAN IP — config ปฏิเสธ API จะไม่บูต (packages/config/src/env.ts)
     ⇒ WEB_APP_BASE_URL คงเป็น http://localhost:3001 ตามเดิม
  3. เชิญสมาชิก → คัดลอกลิงก์ → ส่งทาง LINE → แตะลิงก์ในแอป LINE บนเครื่องนั้น
  4. เดินให้จบ: การ์ดคำเชิญ → สมัคร → ล็อกอิน → ต้องกลับมาที่คำเชิญเอง (B-19) → กดเข้าร่วม → เข้าร้าน
  5. บันทึกลง manual-pass-results.md แล้วกลับมาแก้ §1 verdict block + §3 ของไฟล์นี้
  ⚠️ แม้ทำครบ 5 ข้อ ก็ยัง "ไม่ครอบลิงก์รูปร่าง production" (ลิงก์เป็น localhost) —
     ส่วนนั้นปิดได้เมื่อมี staging origin https จริง (forward-commitments.md บรรทัด 260)
```

> **ห้ามทำ:** แก้ §12.2 ให้ M-01 หายไป · แทน M-01 ด้วย E-05 · หรือเปลี่ยน ⚠️ เป็น ✅ โดยไม่มีคนเดิน
> ถ้าจะเปลี่ยนเกณฑ์นี้จริง ๆ ต้องมี **D-XXX** และเป็นการตัดสินของ user ไม่ใช่ของ qa ไม่ใช่ของคนที่อยากให้ feature ผ่าน

---
---

# ภาคผนวก A — verdict v1 (2026-09-05) · เก็บไว้ทั้งฉบับ ไม่แก้ไข

> ⚠️ **ข้างล่างนี้คือ verdict ฉบับวันที่ 2026-09-05 บน `df37104` ตามตัวอักษรเดิมทุกบรรทัด**
> เก็บไว้เพราะ **ประวัติของคำตัดสินคือหลักฐานชนิดหนึ่ง** — ถ้าลบทิ้ง จะไม่มีใครรู้ว่าเคยมีอะไรแดงและแดงเพราะอะไร
> · ตัวเลข/ลิงก์/สถานะในภาคผนวกนี้เป็นของ `df37104` **ไม่ใช่ของวันนี้** · ที่ล้าสมัยไปแล้วผมชี้ไว้ที่ §4 และ §6.6 ของ v2
> · โดยเฉพาะ **§8 ของ v1 (คำสั่ง `WEB_APP_BASE_URL` = LAN IP) ผิด** — ฉบับที่ถูกอยู่ที่ **§9 ของ v2**
>
> ⚠️ **เลขหัวข้อ §0–§8 ที่เห็นต่อจากบรรทัดนี้เป็นเลขของ v1** ซ้ำกับ §0–§9 ของ v2 ด้านบนโดยตั้งใจ
> (ผมไม่แก้เลขหัวข้อของเอกสารเก่า เพราะมีเอกสารอื่นอ้างถึงมันอยู่ เช่น `tasks.md` และ `release-gate-f.md`)
> · **เวลาอ้างอิงจากนี้ไป ให้ระบุว่า "v1 §x" หรือ "v2 §x" เสมอ**

# [F-002] Gate E — verdict

> **เจ้าของเอกสารนี้ = qa** · เกณฑ์ = [test-plan.md §17](test-plan.md) + [WEB_TEAM.md §4](../../../WEB_TEAM.md) + skill `quality-gate`
> · ตัดสินบน `df37104` (HEAD ของ `claude/thai-language-output-4ea998`)
> · ตัวเลขทุกชุดในไฟล์นี้ **ผมรันเอง 2026-09-05** ไม่ได้ลอกจาก `tasks.md` / `manual-pass-results.md` / `release-gate-f.md`
>   — เอกสารพวกนั้นเป็น*หลักฐานของคนอื่น* ไฟล์นี้เป็น*คำตัดสิน* และคำตัดสินต้องยืนบนของที่ผมเห็นกับตา

---

## 0. คำตอบสั้น ๆ ก่อน (สำหรับคนที่อ่านบรรทัดเดียว)

**Gate E = ❌ NOT DONE** · ช่องแดง **1 ช่อง** และมันไม่ใช่โค้ด

| | |
|---|---|
| แดงเพราะ | **M-01** — ส่งลิงก์คำเชิญจริงทาง LINE บนมือถือจริง **ยังไม่มีใครทำ** ⇒ §17.6 ("manual §12.2 ทำครบ") ไม่ครบ |
| ต้องการอะไรถึงจะเขียว | คน 1 คน + โทรศัพท์ที่มี LINE + ~20 นาที · **ไม่ต้องแก้โค้ดแม้แต่บรรทัดเดียว** |
| **M-07ค ฝั่ง iOS บล็อกไหม** | **ไม่บล็อก** — เหตุผลเต็มอยู่ที่ §5.2 (สรุป: ไม่มี iOS release ใน Phase 0 · devops ตัดสินเป็นทางการแล้ว 2026-09-05 ให้เป็น forward-commitment ที่มี trigger ⇒ **n/a ไม่ใช่ red**) |
| defect ระดับ blocker ในโค้ด | **0** |

**ผมไม่เขียนว่า "เกือบผ่าน"** — §17 ของแผนที่ผมเขียนเองบอกว่า *"ข้อใดไม่ครบ = แดง ไม่มีเขียวแบบมีเงื่อนไข"*
และ §12.2 เขียนกำกับ M-01 ไว้ตรง ๆ ว่า **"D-012 คือ flow หลักของ MVP — ห้ามพิสูจน์แค่ใน CI"**
ผมเขียนประโยคนั้นตอน Gate 2 เพื่อกันนาทีนี้พอดี ⇒ ผมจะไม่มาแก้เกณฑ์ของตัวเองตอนตัดสินเพื่อให้มันผ่าน

---

## 1. Verdict block

```
❌ QUALITY GATE — F-002 Organization · License · Membership  (df37104)

A Requirement: pass — AC 35 ข้อใน F-002-organization-license-membership.md §2 · gate1-approved ·
                      Platform: both · size: full · D-012/D-027/D-028/D-029/D-030 ผูกกับ AC แล้ว
B Design:      pass — architecture/data-model/api-spec/ux-wireframe/ui/test-plan ครบ + signoff:approved ·
                      security-review 3 ไฟล์ (spec + build-A + build-B) · test plan map AC→case ครบ 35/35 (§3)
C Domain&Data: pass — cross-tenant proof รันจริง: org-leak.kit 20/20 + tenancy.db 19/19 (DB จริง) ·
                      tx+lock: concurrency-matrix 15/15 (Postgres+Redis จริง) · ledger: n/a โดยประกาศ
                      (F-002 ไม่แตะ StockMovement/เงิน — ยืนยันด้วย diff ที่ §6.3) · Decimal/int: n/a เหตุผลเดียวกัน
D Experience:  pass — 4 state ครบและมี unit ประกบ (SessionList/TaxProfileCard/MembersScreen) · copy lint E-11
                      6/6 · design token contract 4/4 · web+mobile ใช้ token ชุดเดียว · manual M-05: "องค์กร" 0 จุด
E Quality:     FAIL — เลนอัตโนมัติเขียวหมดและผมรันเองครบทุกเลน (§2) · manual §12.2 **ไม่ครบ: M-01 ยังไม่ได้ทำ**
F Release:     n-a  — ไม่ใช่ขอบเขตของ Gate E · @release ตัดสินบน release-gate-f.md (ห้ามอ้าง qa-green จากไฟล์นี้)

VERDICT: NOT DONE
Blocking: M-01 (manual §12.2 · owner = คนถือเครื่อง, dispatch โดย PM) — ข้อเดียว
Non-blocking (บันทึกไว้ มีเจ้าของ): §6 — 4 static gate ที่ไม่มีตัวตน · G-08 ถูกแตะ · doc drift 1 จุด
```

---

## 2. หลักฐานเป็นตัวเลข — ผมรันเองทั้งหมด (2026-09-05)

เครื่อง: node **v22.23.1** · pnpm **9.15.9** · สแตกจริงในเครื่อง (API :3000 · web :3001 · PG :55432 · Redis :56379)

### 2.1 unit / lint ในเครื่อง

| คำสั่ง | ผล | exit |
|---|---|---|
| `pnpm --filter web test` | **48 files · 366 passed · 0 failed · 0 skipped** (27.72s) | 0 |
| `pnpm --filter api test` | **56 passed \| 11 skipped files · 704 passed \| 210 skipped (914)** | 0 |
| `pnpm --filter @omnistock/core-domain test` | **23 files · 384 passed** | 0 |
| `pnpm --filter @omnistock/db test` | **8 passed \| 2 skipped files · 161 passed \| 24 skipped (185)** | 0 |
| `pnpm --filter @omnistock/config test` | **2 files · 63 passed** | 0 |
| `pnpm --filter @omnistock/contracts test` | **3 files · 12 passed** | 0 |
| `fvm flutter test` (apps/mobile) | **`00:54 +451: All tests passed!`** | 0 |
| `fvm flutter analyze` | **`No issues found! (ran in 2.4s)`** | 0 |
| `fvm dart run tool/check_boundaries.dart` | **`OK — 85 files scanned, no violations`** | 0 |

> **210/24 ที่ skip ในเครื่อง คือ int lane ที่ไม่มี `TEST_DATABASE_URL` — ไม่ใช่ skip ที่ซ่อนอะไร**
> พิสูจน์ที่ §2.3: บน CI เลนเดียวกันรัน **212/212** และ **185/185** โดย **skip = 0**
> (212 − 210 = 2 เคสในไฟล์ `*.int.test.ts` ที่รันได้โดยไม่ต้องมี DB — ตัวเลขสองฝั่งสอดคล้องกันพอดี)

### 2.2 E2E browser — ผมรันเองบนสแตกจริง

```
cd apps/web && CI=1 E2E_REDIS_URL=redis://localhost:56379 npx playwright test
→ 29 passed (32.5s)   exit 0
```

ครบทั้ง **E-01 · E-01b · E-02 · E-02b · E-03 · E-03b · E-04 · E-05 ×2 · E-06 ×3 · E-07 ×2 · E-08 · E-08b ·
E-09 · E-12★ · E-13 · E-13b · E-14★ ×4 · E-15 · E-15b · E-16 · S9**

⚠️ ก่อนรันต้องล้าง `throttle:*` + `orgrl:*` ใน Redis (โควตาของ F-001 หมดจาก manual pass) —
**ผมล้างแล้วรัน ไม่ได้แก้เทสต์หรือปิด throttle** · นี่คือของที่ `manual-pass-results.md` เตือนไว้ท้ายไฟล์ และมันจริง

### 2.3 CI run `33940886688` — ผมอ่านจาก log ไม่ใช่จากสถานะ

`headSha = df371049f899a2c89fc966c834a08163ca34507b` ⇒ **ตรงกับ HEAD ที่ผมตัดสิน** (ไม่ใช่ run ของคอมมิตเก่า)
9 job · conclusion = `success` ทั้งหมด: `node-ci` · `integration-api` · `db-migrate` · `e2e-web` · `mobile-e2e`
· `flutter-ci` · `contracts-drift` · `oasdiff` · `depcruise`

**จำนวนเคสที่รันจริง (ไม่ใช่แค่ ✅):**

| job | บรรทัดใน log | ตัวเลข |
|---|---|---|
| `e2e-web` | `Running 29 tests using 1 worker` → `29 passed (26.6s)` → `the browser lane ran 29 passing case(s) (floor 29).` | **29** |
| `mobile-e2e` | `00:10 +4: All tests passed!` → `mobile integration: 4 passing case(s) (floor 4)` | **4** |
| `integration-api` | `[lane-enabled guard] … total: 212 passed / 0 failed / 0 skipped (of 212)` → `OK — the required suites really ran.` | **212 · skip 0** |
| `db-migrate` | `[lane-enabled guard] … total: 185 passed / 0 failed / 0 skipped (of 185)` | **185 · skip 0** |
| `node-ci` | `Tasks: 33 successful, 33 total` · api `704 passed \| 210 skipped` · db `161 \| 24` · web `48 files` | ตรงกับเครื่องผมเป๊ะ |

**I-37 (บทเรียน F-001 "green locally ≠ tested") พิสูจน์แล้วสองชั้น** และผมตรวจถึงชั้นที่สอง:
เลน int แตกราย 13 ไฟล์ (`orgs 37 · members 34 · auth.e2e 26 · invitations 23 · redeem 16 · concurrency 15 ·
seed-kit 13 · org-leak 20 · rate-limit 6 · perf-smoke 5 · openapi-parity 5 · header-policy 4 · refresh-token 8`)
**ทุกไฟล์ `0 skipped`** ⇒ ไม่มีไฟล์ไหน `describe.skip` ทั้งก้อนแล้วเขียวลอย ๆ

---

## 3. AC ↔ เคส — **คำถามที่สำคัญที่สุดของงานนี้**

> **คำถาม:** มี AC ข้อไหนที่ไม่มีเคสไหนแตะเลยหรือเปล่า
> **คำตอบ: ไม่มี — 35/35 มีเทสต์ที่รันจริงและผ่านจริง**
>
> ผมไม่ได้ยอมรับตาราง §2 ของ test-plan ตามที่เขียนไว้ · ผมนับ AC ใหม่จากต้นทาง
> (`F-002-organization-license-membership.md` §2: US-1 4 + US-2 3 + US-3 9 + US-4 5 + US-5 7 + US-6 3 + US-7 4 = **35** ✓)
> แล้วไล่หาเทสต์**จากพฤติกรรมในโค้ด** ไม่ใช่จาก id ที่แผนอ้าง — เพราะ **id ในแผนกับ id ในโค้ดไม่ตรงกันทั้งหมด**
> (ดู §3.2 — เรื่องนี้เกือบทำให้ผมสรุปผิด และมันคือเหตุผลที่ต้องเช็คแบบนี้)

### 3.1 ตาราง AC ↔ เคสที่ผมยืนยันด้วยตาเอง

| AC | เคสที่ผมเปิดดูจริง (ไฟล์:ชื่อเทสต์) | เลนที่รัน | ผล |
|---|---|---|---|
| **AC-1.1** สร้าง org → Owner + entitlement | `orgs.e2e.int`: *★ 201 creates Organization + 3 roles + Owner membership + entitlement + warehouse* | int 212 | ✅ |
| **AC-1.2** plan มาจาก server ไม่แจก free | `orgs.e2e.int`: *★ §6.2 · 503 ORG_PROVISIONING_UNAVAILABLE — and NO shop is created* · `config/env.test` + `app.kit` pin `DEFAULT_ORG_PLAN_KEY` · `plan-provisioning.service.test` | int + unit | 🟡 ตามที่ประกาศ |
| **AC-1.3** tier ตาม plan + default warehouse 1 | เคสเดียวกับ AC-1.1 + `orgs.e2e.int`: *★ the DB-level 'one default warehouse per org' index is real* | int | ✅ |
| **AC-1.4** ทุกอย่างผูก `organizationId` | `tenancy.db.test` 19/19 (DB จริง) · `tenancy.test` 83 · `org-models.test` (อ่าน DMMF) · `user-select.test` 4 | db 185 | ✅ |
| **AC-2.1** เห็น/เลือก org (web+mobile) | `orgs.e2e.int`: *lists the caller's shops in the full shape* · web **E-02/E-02b** · mobile **E-10** *the shop list and the members list come back from the server* | int+29+4 | ✅ |
| **AC-2.2** ไม่ปนข้าม org | `org-leak.kit.int` 20/20 · `orgs.e2e.int`: *GET/PATCH leaks nothing in any direction* · **E-02** | int+29 | ✅ |
| **AC-2.3** org ที่ไม่ได้เป็นสมาชิก → 403 | `orgs.e2e.int`: *★ the Staff persona gets FORBIDDEN while the stranger gets ORG_ACCESS_DENIED (I-5)* | int | ✅ |
| **AC-3.1** เชิญ + บังคับ role → pending + วันหมดอายุ | `invitations.e2e.int`: *★ TTL comes from the ROLE, not the caller* · *422 ROLE_INVALID…* · **E-03** | int+29 | ✅ |
| **AC-3.2** (D-012) copy link เอง ไม่มี email อัตโนมัติ | `invitations.e2e.int`: *★ the raw token is returned ONCE…* · **E-03** · **ผมตรวจเองว่าไม่มี dep ส่งเมลจริง** (§6.1) | int+29+by hand | ✅ |
| **AC-3.3** แสดงลิงก์ครั้งเดียว · เก็บ hash | `invitations.e2e.int`: *★ the raw token is returned ONCE and never stored (D-018)* + *★ the list can never hand the token back* · `invitation-token.test` (packages/db) 9 | int+db | ✅ |
| **AC-3.4** ออกลิงก์ใหม่ → ลิงก์เดิมตาย · อายุนับใหม่ · บันทึกเหตุการณ์ · ห้ามคำว่า "คัดลอกลิงก์เดิม" | `invitations.e2e.int`: *★ reissue rotates the token — the OLD hash stops existing immediately* + *★ reissue restarts the clock* · **E-03b** *the old link is DEAD the moment a new one exists* · **E-11** `copy-lint.test` 6/6 | int+29+unit | ✅ |
| **AC-3.5** ยกเลิกคำเชิญ → ลิงก์ตาย | `invitations.e2e.int`: *cancel kills the link, and cancelling twice is 409 rather than a silent no-op* | int | ✅ |
| **AC-3.6** เชิญคนที่เป็นสมาชิก/มี pending | `invitations.e2e.int`: *409 INVITATION_PENDING carries the id, so the UI has a next step* + *409 ALREADY_MEMBER* · **E-03** | int+29 | ✅ |
| **AC-3.7** normalize email · token สุ่ม/hash/หมดอายุ/ผูก email | `core-domain/auth/email.test` (`normalizeEmail`) · `invitation-token.test` · `invitations.e2e.int`: *★ A-4: an expired invitation reads as `expired`* | unit+db+int | ✅ |
| **AC-3.8** (D-028) Owner-only เชิญ Owner · role สูง 24 ชม. | `invitations.e2e.int`: *★ C-1/D-028: an Admin cannot invite somebody AS an Owner* + *★ NEW-2: an Admin cannot reissue an OWNER invitation, and the DB does not move* + *★ TTL comes from the ROLE* · **M-03 ยืนยันด้วยมือ: ตรงถึงนาที 3 จอ + DB** | int+manual | ✅ |
| **AC-3.9** เห็นใครรับเมื่อไหร่ + ธงบัญชีสร้างหลังออกลิงก์ | `invitations-redeem.e2e.int`: *★ joins the shop, and records the account-age snapshot (§3.10 flag)* (assert `acceptedUserCreatedAfterInvite: true`) · `MembersScreen.invitation-status.test` (`acceptedAt` → "รับแล้วเมื่อ") · **M-04 (ครึ่ง "เมื่อไหร่" หายไปจริง → B-15 → แก้แล้ว)** | int+unit+manual | ✅ |
| **AC-4.1** มีบัญชี → กดรับ → membership ทันที | `invitations-redeem.e2e.int` (16 เคส) · **E-04** | int+29 | ✅ |
| **AC-4.2** ยังไม่มีบัญชี → สมัคร email เดิม → ผูกอัตโนมัติ | **E-05** *they sign up with the invited address and join — the link is needed twice* · `oasdiff` ยืนยัน `/auth/*` ไม่ breaking | 29+CI | ✅ |
| **AC-4.3** หมดอายุ/ยกเลิก → แจ้ง expired/invalid | `invitations-redeem.e2e.int`: *★ a token that DID resolve gets its precise state (cancelled ≠ expired ≠ accepted)* + *★ an EXPIRED link says so — expiry is derived* | int | ✅ |
| **AC-4.4** (D-027) email คนละตัว → `u***@…` ไม่เผยเต็ม | `invitations-redeem.e2e.int`: *★ the wrong account gets 403 + a MASKED address, never the address* · **E-06 ×3 รวม ★ *the address never reached this browser at all*** | int+29 | ✅ |
| **AC-4.5** (D-028) คำเชิญที่ออกก่อนถูกถอด → ปฏิเสธ · กลับเข้ามา = เหตุการณ์แยก | `invitations-redeem.e2e.int`: *★ 409 INVITATION_SUPERSEDED when the link was issued BEFORE the removal (I-1)* + *★ a link issued AFTER the removal works, and says so with its own event* | int | ✅ |
| **AC-5.1** (D-027) ถอด → request ถัดไป 403 ทันที · session ไม่ตาย | `members.e2e.int`: *★ soft-revokes AND cancels that email's pending invitation, atomically* · **E-07** *removed while inside the shop: refused at once, and sent to the picker — not to /login* · mobile **E-10** *★ removed mid-session: the shop goes, the session stays* | int+29+4 | ✅ |
| **AC-5.2** org หายจาก switcher + พากลับหน้าเลือกร้าน | `orgs.e2e.int`: *★ AC US-5: a revoked shop disappears from the DEFAULT list immediately* · **E-07** *the shop is gone from the picker, and typing its URL does not get back in* | int+29 | ✅ |
| **AC-5.3** (D-028) ถอด → ยกเลิกคำเชิญค้างของ email นั้น (tx เดียว) | `members.e2e.int`: *★ soft-revokes AND cancels that email's pending invitation, **atomically*** | int | ✅ |
| **AC-5.4** ชื่อคนถูกถอดยังอยู่ในประวัติ | `members.e2e.int`: *shows revoked rows by default and filters on request* (แถวไม่ถูกลบ) | int | 🟡 ตามที่ประกาศ |
| **AC-5.5** ดูรายชื่อ+สถานะ+role ต้องมี `manage_members` | `members.e2e.int`: *★ D-028/I-8/N-4: a Staff member gets 403 FORBIDDEN — not the directory* + *an Admin (manage_members, no full_access) may read it* · **E-08/E-08b** | int+29 | ✅ |
| **AC-5.6** ถูกถอดแล้วยังล็อกอินได้ + เห็น org อื่น | **E-07** (ยืนยันว่า "ไม่ถูกเตะไป /login") · mobile **E-10** (*the session stays*) | 29+4 | ✅ |
| **AC-5.7** (D-029) ทุก role ออกจากร้านเองได้ · Owner คนสุดท้ายไม่ได้ | `members.e2e.int`: *★ a Staff member may leave WITHOUT manage_members — and it emits member.left* · *★ 409 LAST_OWNER — the only Owner cannot leave* · *★ there is no way to point this route at somebody else* (★7) · *★ leave ‖ remove-the-other-Owner keeps the invariant* · **E-13/E-13b** · **M-06 ด้วยมือ** | int+29+manual | ✅ **เต็ม** ตามที่ §17.1 บังคับ |
| **AC-6.1** Owner ≥ 1 | `members.e2e.int`: *★ 409 LAST_OWNER when the only Owner demotes themselves* + *★ 409 LAST_OWNER — the only Owner cannot be removed* + `concurrency-matrix` *★ two parallel removals of two different Owners never leave the shop ownerless* (20 รอบ) | int 15 conc. | ✅ |
| **AC-6.2** Owner ยกคนอื่นเป็น Owner ได้ | `members.e2e.int`: *★ promoting to Owner is flagged as full access — privilege escalation is greppable* | int | ✅ |
| **AC-6.3** (D-028/D-030) Owner-only ครบ 5 call site ของ `canAssignRole` | `members.e2e.int`: *★ C-1/D-028: an Admin cannot promote anyone to Owner* · *★ C-1: an Admin cannot promote THEMSELVES, nor demote an Owner* · *★ C-1: an Admin cannot remove an Owner* · `invitations.e2e.int`: *★ C-1/D-028 invite* + *★ NEW-2 reissue* · `auth.e2e.int` + `admin-reset-authz.test` (NEW-1) | int+unit | ✅ |
| **AC-7.1** เปิด accounting → บังคับ tax profile ครบ 13 หลัก + checksum | `orgs.e2e.int`: *★ PUT stores the profile and the RESPONSE never echoes the full number* · **E-09** *a wrong checksum is corrected AT THE FIELD, and the fix goes through* | int+29 | ✅ |
| **AC-7.2** 1 org = 1 tax profile | `orgs.e2e.int` §3.16 block (`PUT` ทั้งชุด เขียนทับ ไม่มีชุดที่สอง) · `schema-f002.test` (unique) | int+db | ✅ |
| **AC-7.3** ยังไม่ประกาศ TIN → Sync tier | `taxProfileComplete` ถูก assert ที่ `orgs.e2e.int` (4 จุด: 201 = false · GET = false · หลัง PUT = true · Staff เห็น true) + `org-profile.ts` (`isTaxProfileComplete`) + `tax-card.test` (web) | int+unit | 🟡 การ gate จริง = F-007 |
| **AC-7.4** (D-028 PDPA) TIN เต็มเฉพาะ `manage_org_settings` ผ่าน `reveal` เส้นเดียว | `orgs.e2e.int`: *★ reveal returns the FULL number, with no-store + no-referrer headers* · *★ reveal is the ONLY route allowed to emit a full TIN, and the list says so* · *★ a Staff member cannot reveal — 403, and nothing is emitted* · *★ a successful reveal is ALWAYS recorded, and the record carries no digits* · *★ Staff see vatRegistered ONLY — not even the last four digits* · **E-14★ 4 เคส** · **M-07/M-07ข/M-07ค(Android) ด้วยมือบนเครื่องจริง** | int+29+manual | ✅ |

**สรุป: 35/35 — เต็ม 32 · partial 3 (AC-1.2 · AC-5.4 · AC-7.3)**
partial ทั้งสามคือ **"พื้นผิวยังไม่เกิดใน Phase 0"** ไม่ใช่ "ไม่มีคนเขียนเทสต์" — F-080/F-082 (plan จาก license),
F-005/F-011 (ledger/audit), F-007 (tier gating) · **@product รับทราบไว้แล้วใน D-029(4)** ⇒ ผมไม่นับเป็นแดง
และผมยืนยันว่า **ตัวเลขที่ถูกคือ 35/35 ไม่ใช่ 34/34** ที่ D-029(4) เขียน (D-029(2) เพิ่ม AC-5.7 เองแล้วนับก่อนหน้านั้น)

### 3.2 สิ่งที่เจอระหว่างเช็ค — id ในแผนกับ id ในโค้ดไม่ตรงกัน

ผมสแกน id ของแผนทั้ง 99 ตัวหาในโค้ด · **29 ตัวไม่ปรากฏเป็นสตริงเลย** — และ *ส่วนใหญ่ไม่ได้แปลว่าไม่มีเทสต์*
แต่แปลว่า **เทสต์มีอยู่โดยไม่ได้ติดป้าย** ⇒ ตาราง §3.1 ผมจึงยืนยันด้วย*พฤติกรรม*ไม่ใช่ด้วยป้าย

**ที่สำคัญกว่านั้น: เลข E- ในไฟล์กับเลข E- ในแผนคนละชุดกัน**

| ในแผน §12.1 | ในโค้ดจริง |
|---|---|
| E-10 = mobile Flutter | ไฟล์ `e15-root-route.spec.ts` = **E-15/E-15b** (root route, มาจาก B-14) · **E-10 ตัวจริงอยู่ที่ `apps/mobile/integration_test/org_flow_test.dart`** |
| E-11 = copy lint (static) | ไฟล์ `e16-sign-out.spec.ts` = **E-16** (sign-out, มาจาก B-16) · **E-11 ตัวจริงอยู่ที่ `apps/web/src/features/org/copy-lint.test.ts`** |
| — | **E-15 · E-15b · E-16 · S9 เป็นเคสใหม่ที่ไม่มีในแผน** (เกิดจาก B-14/B-16/B-3) |

⇒ **§12.1 วันนี้คือ 29 เคส ไม่ใช่ 14 หรือ 26** (แผนเขียน 14 flow · `tasks.md` §2358 ยังเขียน "browser 26" ซึ่งล้าสมัย)
· ผมยืนยันด้วยตัวเอง: `Running 29 tests` บน CI **และ** `29 passed` บนเครื่องผม · emulator **4** (แผนเขียน 1, tasks เขียน 3 แล้ว 4)
· **ไม่มีเคสไหนของแผนหายไป** — E-01..E-09, E-12, E-13, E-14 อยู่ครบในเบราว์เซอร์ · E-10 อยู่ที่ emulator · E-11 อยู่ที่ unit
· นี่เป็น **ปัญหาการติดป้าย ไม่ใช่ปัญหา coverage** ⇒ non-blocking (§6.4)

---

## 4. §17 ไล่ทีละข้อ (เกณฑ์ที่ผมเขียนเองตอน Gate 2)

| § | เกณฑ์ | ผล | หลักฐาน |
|---|---|---|---|
| 17.1 | AC 35 ข้อมีเทสต์ที่รันจริงและผ่าน · AC-5.7 ต้อง "เต็ม" | ✅ | §3.1 — AC-5.7 มี 4 เคส int + 2 เคส browser + M-06 |
| 17.2 | Track 1 เขียวบน **CI** · int lane พิสูจน์ว่าเปิดจริง (I-37) | ✅ | 9/9 job บน `33940886688` (headSha = HEAD) · int **212/212 skip 0** · db **185/185 skip 0** |
| 17.3 | ★1–★7 ครบ · §9 ครบ 41 finding | ✅ | `regression-pack.test.ts` เดินทะเบียนทีละแถวและ **อ่านไฟล์จริง + เช็คสตริง `must`** (ไม่ใช่ assert ลอย) · 41 distinct findings · 0 blank row |
| 17.4 | gate §10 ทั้งหมดเขียว **และ G-05 พิสูจน์ว่าแดงได้จริง** | ⚠️ **บางส่วน** | G-01/02/03/04/05/07/13/15 มีจริงและมี fixture (`route-registry.kit.test`: *RED G-13* + *RED G-13(ค)* ครบ 2 tier · *RED (the important one): an EMPTY enumeration must not be green*) · **G-06/G-09/G-11/G-12 ไม่มีตัวตน** → §6.1 |
| 17.5 | `oasdiff` ไม่ breaking · `contracts-drift` ว่าง · client 2 ฝั่ง regen+build | ✅ | job `oasdiff` + `contracts-drift` (รวม *fail on uncommitted diff incl. untracked* + *TS client typecheck-green* + Dart regen) |
| 17.6 | **manual §12.2 ทำครบและบันทึกผล** | ❌ **แดง** | M-02..M-07ข ทำครบ + บันทึกที่ `manual-pass-results.md` · **M-01 ไม่ได้ทำ** |
| 17.7 | perf smoke อยู่ใน budget | ✅ | `perf-smoke.int.test.ts` 5/5 บน `integration-api` (0 skipped) |
| 17.8 | ไม่มี test ถูก skip/ลด/ลบเพื่อให้ผ่าน · ★ มี red→green | ✅ / ⚠️ ดูหมายเหตุ | `.skip` ที่มีคือ `const d = INT_LANE_ENABLED ? describe : describe.skip` ล้วน ๆ (13 ไฟล์) ซึ่ง **I-37 บน CI บังคับให้ต้องรัน** · ไม่มี `.only` / `xit` / `xdescribe` เลย · **หมายเหตุ: red→green ผมยืนยันได้แค่ที่เป็นลายลักษณ์ใน `tasks.md`/`manual-pass-results.md` — ผมไม่ได้ย้อน revert โค้ดเพื่อดูแดงเอง (§7)** |
| 17.9 | defect ค้างถูกจัดชั้น · **blocker = 0** | ✅ | `build-defects.ts` B-1..B-18 — **18 แถว ทุกแถวมี pin, ไม่มีแถวไหนไม่มีเทสต์** · `build-defects.test.ts` บังคับเช่นเดียวกับ §9 |
| 17.10 | §11.1 ครบ 6 · R-01/R-02 ไม่ถูก skip · G-12/13/14/15 เขียว+มี fixture | ⚠️ **บางส่วน** | G-13/G-15 ครบพร้อม fixture · **G-12 ไม่มี · G-14 มีครึ่งเดียว** (`log-hygiene.test.ts` ครอบครึ่ง logger · ครึ่ง DTO ครอบด้วย `org-leak.kit` เชิงพฤติกรรมแทน) |
| 17.11 | (ก) 41 finding ไม่มีช่องว่าง (ข) NEW-1 red→green สองชั้น (ค) fail-closed บน `GET` (ง) ไม่มี 500/SQLSTATE ตอนแย่ง lock (จ) `USER_SELECT` 3 ชั้น (ฉ) G-15 มี fixture | ✅ (ยกเว้น red→green — ดู §7) | (ก) `regression-pack.test`: *★ no blank rows* · NEW-5(ข)/NEW-12 มี `noTest` ที่เขียนเหตุผลยาว >40 ตัวอักษรตามกฎของ gate เอง (ค) `capability.guard.test` + `members.routes.test` + `members.e2e.int` (ง) `org-busy.test` (U-API-21) + `concurrency-matrix` (I-C-13) + `members.e2e.int`: *★ revoke ‖ revoke … never a 500* (จ) `user-select.test` 4 + `org-models.test` + G-02 (ฉ) `role-capability-write-tripwire.test.ts` 6 เคส |

---

## 5. สองข้อที่ยังทำไม่ได้ — บล็อกหรือไม่บล็อก และทำไม

### 5.1 M-01 — ส่งลิงก์จริงทาง LINE บนมือถือจริง ⇒ **บล็อก**

**ผมตัดสินว่าบล็อก** และนี่คือเหตุผล ไม่ใช่การเล่นตามตัวอักษร:

- AC-3.2 (D-012) ไม่ใช่ AC ธรรมดา — มันคือ**กลไกเชิญคนเข้าร้านทั้งหมดของ MVP** เพราะไม่มี SMTP จนถึง F-081
  ⇒ ถ้าเส้นนี้ขาดในโลกจริง F-002 ไม่ได้ทำงานเลยแม้ทุกเลนจะเขียว
- ผมเขียนไว้เองใน §12.2 ว่า **"ห้ามพิสูจน์แค่ใน CI"** · การมาบอกตอนนี้ว่า "E-03/E-05 พิสูจน์แทนได้"
  คือการ**เปลี่ยนเกณฑ์ตอนกำลังถูกตัดสิน** ซึ่งเป็นสิ่งเดียวที่ qa ทำไม่ได้
- และมันมีเนื้อจริง: `manual-pass-results.md` เองบันทึกว่า `WEB_APP_BASE_URL` ที่ชี้ `localhost`
  จะทำให้ลิงก์เปิดไม่ได้บนโทรศัพท์ — **พลาดแบบเดียวกับ B-17 เป๊ะ** และ B-17 คือของที่ทุกเลนเขียวทับมาตลอด
  ⇒ ชั้นที่จะจับเรื่องนี้ได้มีชั้นเดียวคือคนถือเครื่อง

**สิ่งที่ตัดออกไปแล้ว (ให้เครดิตตามจริง):** รูปร่างของลิงก์ไม่ใช่ความเสี่ยงอีกต่อไป —
token เป็น base64url 43 ตัว ไม่มี space/`+`/`/`/`=`/unicode ที่ LINE จะห่อหรือตัด · 1 path 1 query
⇒ **ที่เหลือของ M-01 คือ ~20 นาทีของคน ไม่ใช่งานแก้โค้ด**

- **เจ้าของ:** คนถือเครื่อง (dispatch โดย PM) · **ไม่ใช่** @backend-api / @frontend — ไม่มีอะไรให้แก้
- **ต้องตั้งอะไรก่อน:** `WEB_APP_BASE_URL` = host ที่โทรศัพท์เปิดได้จริง (LAN IP ไม่ใช่ `localhost`)

### 5.2 M-07ค ฝั่ง iOS — **ไม่บล็อก** และผมไม่นับเป็น red

ผมตรวจ `apps/mobile/ios/Runner/AppDelegate.swift` เองแล้ว: `localOnly: true` + expiry บน `UIPasteboard`
มีอยู่จริงในโค้ด · และผม grep `.github/workflows/ci.yml` แล้ว **ไม่มี job `macos`/`xcode` เลย**
⇒ **ยืนยันว่าไฟล์นี้ไม่เคยถูก compile จริง** ตามที่ `manual-pass-results.md` เขียนไว้

**แต่มันไม่ใช่ red ของ Gate E เพราะเหตุผลทางขอบเขต ไม่ใช่เพราะผมยอม:**

1. **ไม่มี iOS release ใน Phase 0** — ไม่มี Apple cert, ไม่มี target release · ⇒ ไม่มีผู้ใช้คนไหนแตะโค้ดบรรทัดนี้ได้
   ⇒ ความเสี่ยง = 0 วันนี้ ไม่ใช่ "ความเสี่ยงที่เรารับไว้"
2. **@devops ตัดสินเป็นทางการแล้ว 2026-09-05** (ตอบ `release-gate-f §4.1`) และคำตัดสินนั้น**อยู่ใน
   `docs/features/forward-commitments.md` พร้อม trigger ที่ผูกกับเหตุการณ์**: *"ก่อน mobile feature ถัดไป
   ที่แตะ `AppDelegate.swift`/iOS-native (F-006 ใกล้สุด) หรือก่อน first iOS release"*
   ⇒ นี่คือรูปแบบที่ skill `quality-gate` อนุญาต: **ข้ามได้ แต่ต้องบอกว่าทำไม** — และมีคนชื่อจริงรับไว้พร้อมเงื่อนไขปลด
3. **Android ปิดไปแล้วจริง** — `mobile-e2e` ต้อง Gradle `assembleDebug` ก่อนรัน ⇒ `MainActivity.kt`
   (ทั้ง `FLAG_SECURE` และคลิปบอร์ดของ M-07) compile ทุกรอบ CI · **M-07ค ฝั่ง Android ผ่านด้วยมือแล้ว**
   (preview ขึ้น `••••••` ขณะที่ control ขึ้นเนื้อหาเต็ม — control คือสิ่งที่ทำให้ผลนี้มีความหมาย)

⇒ **สถานะ: `n/a — deferred, owned, triggered` ไม่ใช่ `fail`**
· **แต่ผมขอผูกเงื่อนไขไว้ตรงนี้:** ถ้า @release จะ ship iOS เมื่อไหร่ **ข้อนี้กลับมาเป็น red ทันที**
และ verdict นี้ห้ามถูกอ้างว่าครอบ iOS

---

## 6. ของที่ยังค้าง — ไม่บล็อก แต่ผมบันทึกไว้พร้อมเจ้าของ

### 6.1 static gate 4 ตัวที่ §10 ประกาศไว้ แต่ **ไม่มีตัวตนในโค้ด** — owner: **@qa (ผมเอง) + @devops**

ผม grep ทั้ง tree แล้วไม่พบ implementation ของ:

| gate | สิ่งที่แผนสั่ง | ผมตรวจด้วยมือแทนวันนี้ | สถานะ |
|---|---|---|---|
| **G-06** | ไม่มี write path ไหนเขียน `status: "invited"` | จริง — `members.service.ts:166` เขียนคอมเมนต์ว่า *"`invited` is a dead state: no production write path"* และไม่มี write จริง | คุณสมบัติ ✅ · **gate ✗** |
| **G-09** | ไม่มี dependency/โค้ดส่ง email | จริง — `grep nodemailer\|smtp\|sendgrid\|mailgun` ใน `package.json` ทุกไฟล์ = **0 hit** | คุณสมบัติ ✅ · **gate ✗** |
| **G-11** | ไม่ normalize email ด้วยมือ (ต้องเรียก `normalizeEmail`) | จริง — `.toLowerCase()` ที่เจอในเส้นทางเมลอยู่ในไฟล์เทสต์เท่านั้น · production ผ่าน `core-domain/auth/email.ts` | คุณสมบัติ ✅ · **gate ✗** |
| **G-12** | ห้าม `role.key ===` ในเส้นทางตัดสินสิทธิ์ (api+web+mobile) | จริง — ที่เจอทั้ง 4 จุดเป็น**คอมเมนต์ที่ปฏิเสธวิธีนั้น** (`roles.service.ts:36`: *"Not `key === "owner"`"*) ไม่ใช่โค้ดที่ทำ | คุณสมบัติ ✅ · **gate ✗** |

**ทำไมไม่บล็อก:** ทั้งสี่ข้อมี**ชั้นที่แรงกว่า**คุมอยู่ — G-12 มี **I-45** (`roles.service.test.ts` สลับ `Role.key`
ใน DB แล้วสิทธิ์ต้องไม่ขยับ) ซึ่งพิสูจน์เชิงพฤติกรรม แผนเองก็เขียนว่า *"gate นี้ไม่พอเดี่ยว ๆ — คู่บังคับคือ I-45"*
· G-09/G-11 คุณสมบัติวันนี้ถูกต้องและผมตรวจแล้ว · G-06 มีชั้น `members.service` + `?status=` ที่ปฏิเสธ `invited`

**ทำไมยังต้องบันทึก:** gate ที่ไม่มีตัวตน **ไม่มีวันแดง** ⇒ ไม่มีอะไรจับ regression วันหน้า —
ซึ่งเป็นรูปเดียวกับ NEW-1/B-14 เป๊ะ ("เชื่อว่าปิดเพราะเอกสารบอก") · §17.11(ก) บังคับกับ §9 ว่า
*"มีเทสต์ หรือมีเหตุผลเป็นลายลักษณ์"* — **§10 ควรอยู่ใต้กฎเดียวกันแต่วันนี้ไม่ได้อยู่**
⇒ **ผมสั่งงานตัวเอง:** เขียน G-06/G-09/G-11/G-12 เป็น source-scanning test (แบบเดียวกับ
`system-prisma-allowlist.test.ts`) **หรือ** ลดชั้นเป็นลายลักษณ์ใน §10 ว่า "ครอบด้วย I-45/…" — เลือกอย่างใดอย่างหนึ่ง
ก่อน F-003 (ตัวที่จะเปิด role CRUD ⇒ G-12 กลายเป็นของจำเป็นทันที)

### 6.2 G-08 ถูกแตะจริง — owner: **@qa (บันทึกแล้ว ไม่ต้องแก้)**

G-08 = *"diff ของ F-002 ต้องไม่แตะไฟล์ ledger/`StockMovement`/คอลัมน์เงิน"* · ผมรัน `git diff main...HEAD` แล้ว **แตะ 1 ไฟล์**:

```
packages/db/src/ledger-guard.ts | 1 +/-
-import { Prisma } from "./generated/client";
+import { Prisma } from "../generated/client";
```

**เป็นผลพวงของ B-10** (ย้าย Prisma client จาก `src/generated/` ไป `generated/` เพื่อให้ `dist/` ใช้ client ตัวเดียวกัน)
· **ไม่มีบรรทัดไหนของ semantics ของ ledger เปลี่ยน** · `ledger-guard.test.ts` 6/6 ยังเขียวบน `db-migrate`
· `db-migrate` มี step *"AC5/6/7/8 — schema shape + ledger immutability"* ผ่าน
⇒ **เจตนาของ G-08 ไม่ถูกละเมิด** แต่ **ตัวอักษรถูก** ⇒ บันทึกไว้เพื่อไม่ให้กลายเป็นบรรทัดฐานเงียบ ๆ ว่า
"แตะ ledger ได้ถ้ามีเหตุผลดี"

### 6.3 doc drift ใน `manual-pass-results.md` — owner: **@qa/@backend-api (คนที่เดิน manual pass)**

หัวไฟล์เขียนว่า B-18 **ปิดแล้ว** (user เลือกทาง (ก) 2026-09-05) และ `build-defects.ts` ก็มี pin ครบ
แต่ **§B-18 ในเนื้อไฟล์ยังขึ้นหัวว่า "🔴 เปิดอยู่"** และยังเสนอทางเลือก (ก)/(ข) เหมือนยังไม่มีคนเคาะ
⇒ คนอ่านครั้งแรกจะได้คำตอบคนละอย่างจากไฟล์เดียวกัน · **ไม่กระทบ verdict** (ผมยืนตาม `build-defects.ts` + pin ที่รันจริง)

### 6.4 ป้ายของเคสกับแผนไม่ตรงกัน — owner: **@qa (test-plan §12.1)**

ตาม §3.2 · `test-plan.md §12.1` ยังลิสต์ 14 flow และให้ E-10/E-11 คนละความหมายกับไฟล์ที่ชื่อ `e10-`/`e11-`
· `tasks.md` §2358 ยังเขียน "browser 26" ทั้งที่วันนี้ **29** · ผมเลือก**ไม่แก้ `test-plan.md` ในรอบนี้**
เพราะมันเป็นเอกสารที่ user เคาะไปแล้ว (`signoff: approved`) และการแก้ควรไปพร้อมรอบ amend ที่มีคนอ่าน
⇒ **ตัวเลขที่ถูกต้อง ณ วันนี้อยู่ในไฟล์นี้ (§2.2/§2.3) ให้ใช้ตัวนี้เป็นหลัก**

### 6.5 `--passWithNoTests` / `echo ok` นอกขอบเขต F-002 — owner: **@devops (เฝ้าไว้)**

`packages/connectors` (`test: vitest run --passWithNoTests`) · `apps/back-office` (เหมือนกัน) ·
`packages/contracts` (`build: echo ok` — แต่ `test` จริงและมี 12 เคส) · `apps/mobile` (`typecheck: echo ok`
— ตัวจริงคือ `flutter analyze` ที่รันบน `flutter-ci`)
⇒ **G-07 ไม่ถูกละเมิด** เพราะกฎเขียนว่า *"ในเวิร์กสเปซที่มีเทสต์แล้ว"* และสองตัวแรกยังไม่มีโค้ด
· บันทึกไว้เพราะวันที่ connectors/back-office มีโค้ด **สคริปต์นี้จะเขียวโดยไม่พิสูจน์อะไรเลย**

---

## 7. สิ่งที่ผมตัดสินไม่ได้ และพูดตรง ๆ ว่าทำไม

1. **red→green ของ ★-task (§17.8 · §17.11(ข))** — ผมยืนยันได้แค่ว่า *มีคนบันทึกไว้เป็นลายลักษณ์*
   (`tasks.md` · `manual-pass-results.md` เช่น B-15 *"ถอดการแก้ออก → 2 เคสแดงทันที"*)
   · ผม **ไม่ได้** revert โค้ดแต่ละจุดแล้วดูว่าแดงจริงด้วยตัวเอง — ทำไม่ได้ในรอบนี้เพราะกติกาห้ามผมแตะโค้ด
   ⇒ **ผมรับข้อนี้บนคำของ implementer ไม่ใช่บนสายตาตัวเอง** และบันทึกไว้ตรงนี้ให้ชัดว่านั่นคือคุณภาพของหลักฐาน
   · ของที่ผมยืนยันเองได้และยืนยันแล้วคือ **gate ยังแดงได้จริง** (`route-registry.kit.test.ts` มี RED case 8 ตัว
   รวม *an EMPTY enumeration must not be green*) — ซึ่งเป็นครึ่งที่สำคัญกว่า
2. **M-02 / M-05 / M-06 ครึ่งที่เป็น "อ่านแล้วเข้าใจไหม"** — ไม่ใช่ของผม เป็นของ **@ux + @product**
   · M-02 ยังไม่มีคำตอบ: *ไม่มีจอไหนเรียก `adminResetPassword` เลย* ⇒ คำถามที่ runbook ถามตอบไม่ได้เชิงโครงสร้าง
   · **ผมไม่ถือว่าบล็อก Gate E** (พฤติกรรม API ถูกต้องและมีเทสต์: Staff→200 · Owner→404 · รหัสเดิมยังใช้ได้)
   แต่ **@product ต้องรู้ก่อน release** ว่าราคาที่เคาะไว้ใน D-030 ยังไม่เคยมีใครเห็นบนจอ
3. **จะ ship ทั้งที่ M-01 ยังไม่ได้ทำหรือไม่** — **ไม่ใช่การตัดสินของ qa** เป็นของ **@release**
   · ผมให้ได้แค่ข้อเท็จจริง: *ไม่มี defect ค้างในโค้ด · เลนอัตโนมัติเขียวครบ · ช่องที่ขาดคือหลักฐานจากคน 1 ข้อ*
   ⇒ ถ้า @release เลือกเดินต่อ **ห้ามเขียนว่า "qa-green"** — เขียนว่า *"qa NOT DONE, release ยอมรับความเสี่ยง M-01"*

---

## 8. ทางกลับมาเป็นเขียว (สั้นและชัด)

```
เหลืองานเดียว: M-01
  1. ตั้ง WEB_APP_BASE_URL = LAN IP ของเครื่องที่รัน web (ไม่ใช่ localhost) แล้ว restart api
  2. เชิญสมาชิก → คัดลอกลิงก์ → ส่งทาง LINE → เปิดบนโทรศัพท์จริง → กดรับจนเข้าร้าน
  3. บันทึกผลลง manual-pass-results.md
  4. กลับมาที่ไฟล์นี้ แก้ §1 verdict block เป็น ✅ DONE + §4 แถว 17.6 เป็น ✅
เมื่อ 4 ข้อนี้จบ Gate E = DONE โดยไม่ต้องรันเลนไหนใหม่ (ยกเว้นมีคอมมิตใหม่เข้ามาระหว่างนั้น)
```

> **ห้ามทำ:** แก้ §12.2 ให้ M-01 หายไป หรือแทน M-01 ด้วย E-03/E-05 · ถ้าจะเปลี่ยนเกณฑ์นี้จริง ๆ ต้องมี **D-XXX**
> และเป็นการตัดสินของ user ไม่ใช่ของ qa ไม่ใช่ของคนที่อยากให้ feature ผ่าน
