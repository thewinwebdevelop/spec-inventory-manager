# F-002 — runbook ของ manual pass (§12.2 M-01..M-07)

> **ทำไมมีไฟล์นี้:** quality gate เหลือช่องแดงช่องเดียวคือ manual pass และ §17.6 บังคับให้มีก่อนตัดสิน verdict
> · มันเป็นงานคน ผมทำแทนไม่ได้ — แต่สิ่งที่ทำให้ได้คือ **ทำให้มันถูกและเร็ว**: คำสั่งที่รู้ว่าใช้ได้จริง
> (เป็นชุดเดียวกับที่ CI ใช้แล้วเขียว ไม่ใช่ชุดที่ควรจะใช้ได้) + ช่องให้ติ๊กทีละข้อ
>
> เจ้าของผลลัพธ์: **qa** (verdict) · ข้อ M-02/M-05 ส่งต่อ **ux/product** ตามที่ §12.2 เขียนไว้

---

## 0. ยกสแตกขึ้นมาในเครื่อง

✅ **อัปเดต 2026-08-19 (B-10 ปิดครบ):** `pnpm --filter api start` = `node dist/main.js` **ใช้ได้ตรง ๆ แล้ว**
ไม่ต้องมี TypeScript loader ในเส้นทางบูตอีก · `@omnistock/config` และ `@omnistock/db` ship `dist/` เหมือน `core-domain`
· เทสต์ `run-scripts.test.ts` เฝ้าไว้ทั้งสองชั้น (ห้าม loader ใน boot path · ทุก package ที่โหลด runtime ต้องมี `main` ลงท้าย `.js`)
· **CI ก็รันคำสั่งเดียวกันนี้แล้ว** ไม่ใช่คำสั่งที่มีอยู่แค่ในไฟล์ workflow

ชุดที่ **ใช้ได้จริง** (คือชุดที่ job `e2e-web` รันแล้วเขียวทุกวัน — คำสั่งเดียวกันเป๊ะ):

```bash
# 1. Postgres + Redis
pnpm db:up

# 2. env — คัดลอกจากตัวอย่างแล้วใส่ค่าของคุณเอง
#    (ห้ามใช้ค่าใน CI ซ้ำ · ดูว่าตัวไหนบังคับที่ infra/env/README.md)
cp .env.example .env    # แล้วเปิดแก้: JWT_ACCESS_SECRET / JWT_REFRESH_SECRET /
                        # INVITATION_TOKEN_SECRET ต้องยาว ≥ 32 และต่างกันทั้งสามตัว
                        # WEB_APP_BASE_URL=http://localhost:3001
                        # DEFAULT_ORG_PLAN_KEY=comp_full   ← ไม่มีอันนี้ = สร้างร้านได้ 503 ทุกครั้ง

# 3. build + เตรียม DB
pnpm turbo build --filter=api --filter=web
pnpm --filter @omnistock/db exec prisma generate
pnpm --filter @omnistock/db exec prisma migrate deploy
pnpm --filter @omnistock/db exec prisma db seed

# 4. API (คอนโซลที่ 1)
pnpm --filter api start

# 5. web (คอนโซลที่ 2)
pnpm --filter web exec next start -p 3001
```

เช็คว่าขึ้นครบ: `curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/health` → `200`/`503`
และเปิด http://localhost:3001 เจอหน้า login

### มือถือ (ใช้กับ M-01 · M-05 ฝั่ง mobile)

```bash
cd apps/mobile
fvm flutter run --dart-define=API_BASE_URL=http://<LAN-IP-ของเครื่องคุณ>:3000
```

`10.0.2.2` ใช้ได้เฉพาะ Android emulator · **โทรศัพท์จริงต้องใช้ IP ในวง LAN** (`ipconfig getifaddr en0` บน mac)
และเครื่องต้องอยู่ Wi-Fi เดียวกัน

---

## 1. รายการที่ต้องติ๊ก

| # | ทำอะไร | ดูอะไร | ผล | ใครใช้ผลนี้ |
|---|---|---|---|---|
| **M-01** | คัดลอกลิงก์คำเชิญจริง → ส่งทาง **LINE** → เปิดบนมือถือจริง | เปิดแล้วไปต่อได้จนเข้าร้าน · ลิงก์ไม่โดน LINE ตัด/ห่อจนพัง | ☐ | qa |
| **M-02** | ลอง **admin-reset รหัสให้เจ้าของร้าน** แล้วอ่าน 404 ที่ได้ | Owner ที่เจอข้อความนี้ รู้ไหมว่าต้องทำอะไรต่อ | ☐ | **ux + product** (ตัดสิน copy ก่อน release) |
| **M-03** | เชิญด้วย role **เจ้าของร้าน/ผู้ดูแล** | หน้าจอบอก 24 ชม. และตรงกับ `expiresAt` จริง (ไม่ใช่ค่าคงที่ที่พิมพ์ไว้) | ☐ | qa |
| **M-04** | หลังมีคนกดรับ → เปิดจอสมาชิก | เห็นว่าใครรับเมื่อไหร่ · ธง "บัญชีถูกสร้างหลังออกลิงก์" **อ่านแล้วไม่เป็นการกล่าวหา** | ☐ | ux |
| **M-05** | ไล่ error ไทย 18 ตัวบนจอจริง **ทั้ง web และ mobile** | อ่านรู้เรื่องระดับ SME · **ใช้คำว่า "ร้าน" ไม่ใช่ "องค์กร" ทุกที่** — ไล่ที่จอ ไม่ใช่ที่ไฟล์ i18n เพราะข้อความที่ hardcode ในคอมโพเนนต์คือที่ที่มันหลุด | ☐ | ux |
| **M-06** | Owner คนสุดท้ายกด **"ออกจากร้านนี้"** | อ่าน `409 LAST_OWNER` แล้วรู้ว่าต้องตั้งคนอื่นเป็นเจ้าของก่อน · **ประเมินว่าตันไหม เพราะ F-002 ไม่มี "ลบร้าน"** | ☐ | ux + product |
| **M-07** | ~~กด "แสดงเลขเต็ม" บนมือถือจริง~~ | **ทำไม่ได้ — ดูด้านล่าง** | ⛔ | qa (ต้องแก้ §12.2) |

### ⛔ M-07 ทำไม่ได้ และไม่ใช่เพราะยังไม่ได้ทำ

`apps/mobile/lib/features/org/presentation/screens/` มี 5 จอ: สร้างร้าน · เลือกร้าน · สมาชิก · เชิญ · ลิงก์คำเชิญ
· `grep` หา `taxProfile` / `แสดงเลขเต็ม` ทั้ง `apps/mobile/lib` → **ไม่เจอเลย**

**จอภาษีไม่มีอยู่บนมือถือใน F-002** และ test-plan รู้เรื่องนี้อยู่แล้วที่ §13 ข้อ 3
("เพิ่ม lane ของ mobile storage assertion **ตอนที่จอ tax profile ลง mobile จริง (ยังไม่มีใน F-002)**")
⇒ **§12.2 M-07 ขัดกับ §13 ข้อ 3 ของเอกสารตัวเอง** · ข้อเสนอ: ทำ M-07 เป็น N/A ของ F-002 แล้วผูกไว้กับ feature ที่เอาจอภาษีขึ้น mobile
— **แต่เอกสารเป็นของ qa ผมจึงไม่แก้ให้** เขียนไว้ตรงนี้เพื่อให้ตัดสินได้ในนาทีเดียว

---

## 2. สิ่งที่เลนอัตโนมัติพิสูจน์ไปแล้ว (ไม่ต้องทำซ้ำด้วยมือ)

| ทำไปแล้ว | ที่ไหน |
|---|---|
| เชิญ → รับ → เข้าร้าน (มีบัญชีอยู่แล้ว) | E-04 · browser |
| ยังไม่มีบัญชี → สมัคร → เข้าร้าน | E-05 · browser |
| ล็อกอินคนละบัญชีแล้วกดรับ + **ไม่มีอีเมลเต็มหลุดใน response ก้อนไหนเลย** | E-06 · browser |
| ออกลิงก์ใหม่ → ลิงก์เดิมตายทันที | E-03/E-03b · browser |
| ถูกถอดกลางคัน → กลับหน้าเลือกร้าน · **ไม่หลุดล็อกอิน** | E-07 · browser · E-10 · emulator |
| Owner คนสุดท้ายออกจากร้านไม่ได้ (ตัวข้อความ) | E-13 · browser |
| token หายจาก URL/storage/history | E-12 ★ · browser |
| แสดง/ซ่อนเลขภาษี + Staff ยิงตรงได้ 403 | E-14 ★ · browser |

**M-01..M-06 คือส่วนที่เครื่องตัดสินแทนไม่ได้จริง ๆ**: LINE ของจริง · โทรศัพท์ของจริง · และ "อ่านแล้วเข้าใจไหม"
ซึ่งเป็นคำถามที่มีแต่คนตอบได้
