# F-002 — ผลของ manual pass §12.2 (M-01..M-07ค)

> **รันเมื่อ 2026-09-04** บนสแตกจริงในเครื่อง: Postgres+Redis (docker) · API `node dist/main.js` :3000 ·
> web `next start` :3001 · **Android 13 emulator (Pixel_5_API_33)** สำหรับฝั่งมือถือ
> · เดินด้วยเบราว์เซอร์จริงทีละจอ ไม่ใช่การอ่านโค้ด — ข้อไหนอ่านโค้ดประกอบจะเขียนไว้ว่าอ่านอะไร
>
> **เจ้าของ verdict คือ qa** · ไฟล์นี้คือ*หลักฐาน*ให้ qa/ux/product ตัดสิน ไม่ใช่คำตัดสิน
> · runbook (วิธีทำ) อยู่ที่ [manual-pass-runbook.md](manual-pass-runbook.md)

---

## สรุปหัวตาราง

| # | ผล | ใครต้องอ่านต่อ |
|---|---|---|
| **M-01** ลิงก์จริงผ่าน LINE บนมือถือจริง | 🟡 **แคบลงแล้ว** — เปิด `/invite` บน Chrome ของ Android จริงได้ ✅ · เหลือครึ่งที่ยังต้องใช้คน (ดู §M-01) | qa (คน) |
| **M-02** admin-reset ให้เจ้าของร้าน → 404 | ⚠️ **ตอบคำถามที่ถามไม่ได้ เพราะไม่มีจอ** (ดู §M-02) | **ux + product** |
| **M-03** คำเชิญ Owner/Admin = 24 ชม. ตรงกับ `expiresAt` | ✅ **ผ่าน** — ตรงถึงนาที บน 3 จอ + DB | qa |
| **M-04** ใครรับเมื่อไหร่ + ธง "บัญชีสร้างหลังออกลิงก์" | ⚠️ **ครึ่งเดียว** → ธงถ้อยคำผ่าน · "เมื่อไหร่" ไม่มี (**B-15**, แก้แล้ว) | ux |
| **M-05** error ไทย + คำว่า "ร้าน" | ✅ **ผ่านครึ่งที่เครื่องตรวจได้** · ครึ่ง "อ่านรู้เรื่องไหม" เป็นของ ux | ux |
| **M-06** Owner คนสุดท้ายกดออก | ✅ **ผ่าน** — ข้อความบอกทางออกชัด · มี nit 1 ข้อ | ux + product |
| **M-07 / M-07ข / M-07ค** มือถือ: แสดงเลขเต็ม | ✅ **ผ่านครบ** — หลัง product เคาะทาง (ก) แล้วต่อ nav ให้ (**B-18** ปิด) · ดู §M-07 | qa + ux |

**ของใหม่ที่เจอ 4 ข้อ: B-15 · B-16 · B-17 · B-18 — ปิดครบทั้งสี่แล้ว**
(B-18 เปิดค้างไว้ให้ product เคาะ · user เลือกทาง (ก) 2026-09-05 ⇒ ต่อ nav ชั่วคราวแล้วเดิน M-07 จนจบ)

---

## M-01 — ส่งลิงก์ทาง LINE บนมือถือจริง 🟡 **แคบลงแล้ว แต่ยังไม่จบ**

**ทำไม่ได้ทั้งข้อ** — ต้องมีคนถือโทรศัพท์ที่มี LINE เครื่องจริง · แต่รอบนี้**ตัดของที่เครื่องตัดได้ออกไปแล้ว**
เพื่อให้คนที่ทำ M-01 เหลือเดินสั้นที่สุด

### 1 · รูปร่างของลิงก์ — ตัดข้อสงสัยออกแล้ว

```
http://localhost:3001/invite?token=uH6-XRKB-8TplM1CXaYZjjBglTvS3DQeWd0_GLU9FOA
```

token = **base64url 43 ตัว** (`A–Z a–z 0–9 - _`) ไม่มีอักขระที่ LINE ห่อ/ตัด (ไม่มี space, `+`, `/`, `=`,
ไม่มี unicode) · path เดียว query เดียว

### 2 · ⛔ คำแนะนำเดิมของ runbook **ทำตามไม่ได้** — และผมเป็นคนเขียนเอง

รอบแรกผมเขียนว่า "ตั้ง `WEB_APP_BASE_URL` เป็น host ที่โทรศัพท์เปิดได้จริง" · ลองแล้ว **API ไม่ยอมบูต**:

```
$ WEB_APP_BASE_URL=http://10.0.2.2:3001 node dist/main.js
Invalid environment variables:
  - WEB_APP_BASE_URL: WEB_APP_BASE_URL must use https:// (F-002 §6.4) — plain http is
    accepted only on localhost/127.0.0.1 outside production, because this origin carries
    the invitation token
```

`packages/config/src/env.ts:103` ยอมรับแค่ `https:` หรือ `http:` บน `localhost`/`127.0.0.1`/`[::1]`
· **กฎนี้ถูก** — origin นี้ถือ token ของคำเชิญ ⇒ เส้นทาง "ตั้งเป็น LAN IP" ตายตั้งแต่ต้น
**นี่คือ B-17 รอบที่สาม: คำสั่งใน runbook ที่ทำตามแล้วไม่ทำงาน**

**วิธีที่ใช้ได้จริง (ยืนยันแล้ว):** `adb reverse tcp:3001 tcp:3001` — ให้ `localhost` **ของตัวเครื่อง**
ชี้กลับมาที่โฮสต์ ⇒ `WEB_APP_BASE_URL` คงเป็น `http://localhost:3001` (ผ่าน guard) และลิงก์เปิดได้บนเครื่องนั้นจริง
· ใช้ได้ทั้ง emulator และ **โทรศัพท์จริงที่เสียบ USB** ⇒ ขั้นตอนอยู่ใน [runbook](manual-pass-runbook.md)

### 3 · ✅ `/invite` เปิดได้จริงบน Chrome ของ Android 13 — ครั้งแรกที่มีใครเปิดมันบนเบราว์เซอร์มือถือ

design-system §8.3 บอกว่าเบราว์เซอร์มือถืออยู่นอกขอบเขต **ยกเว้น route ที่มาจากลิงก์แชท** (`/invite`)
— ซึ่งไม่เคยมีใครเปิดจริงมาก่อน · รอบนี้เปิดแล้ว:

- การ์ดคำเชิญแสดงครบ: ชื่อร้าน · สิทธิ์ที่ถูกเชิญ (`"พนักงาน"`) · อีเมล**ที่ mask แล้ว** · บรรทัดหมดอายุ
- ปุ่มเต็มความกว้าง แตะสบาย ตัวอักษรไทยอ่านออกสบายที่ขนาดจอโทรศัพท์
- **URL bar อ่านว่า `localhost:3001/invite` — token ถูกถอดออกจาก URL แล้ว** ⇒ **E-12 พิสูจน์บนเบราว์เซอร์มือถือจริงเป็นครั้งแรก** (ก่อนหน้านี้พิสูจน์แค่บน Chromium ขนาดเดสก์ท็อป)

### 4 · 🟠 เจอของใหม่ระหว่างทาง — "ไม่มี token" กับ "token ใช้ไม่ได้" ใช้ข้อความเดียวกัน

หลังกด "สมัครบัญชีใหม่" จากการ์ดคำเชิญ แล้วถูกพากลับมาที่ `/invite` **โดยไม่มี token** (เพราะหน้านั้น
ถอด token ทิ้งตั้งแต่โหลดครั้งแรก — E-12 ซึ่งเป็นพฤติกรรมที่ถูกต้อง) จอขึ้นว่า:

> **ลิงก์คำเชิญนี้ใช้ไม่ได้**
> ลิงก์อาจถูกคัดลอกมาไม่ครบ หรือถูกยกเลิกไปแล้ว — ขอลิงก์ใหม่จากเจ้าของร้าน

**ทั้งสองข้อไม่จริงสักข้อ** — ลิงก์ครบและยังไม่ถูกยกเลิก · คนที่ทำตามที่จอบอก (สมัครก่อน แล้วค่อยกลับมา)
จะถูกบอกว่าลิงก์ของตัวเองพัง

`InviteScreen.tsx:66` เขียนเหตุผลไว้ตรง ๆ ว่า *"from the reader's side it is the same event: the link
they were sent does not work"* — **ซึ่งถูกสำหรับคนที่ copy ลิงก์มาไม่ครบ แต่ผิดสำหรับเคสนี้ เพราะแอปเป็นคนถอด
token ออกเอง** · E-05 รู้จักเคสนี้ในชื่อ *"the link is needed twice"* แต่ไม่เคยมีใครประเมิน**ถ้อยคำ**ตอนนั้น
· บนมือถือมันเจ็บกว่า เพราะเส้นทางธรรมชาติคือ แตะลิงก์ใน LINE → สมัคร → เด้งกลับ → เจอ "ลิงก์ใช้ไม่ได้"

⇒ **@ux + @product:** จะแก้ด้วย copy (แยกเคส "ไม่มี token") หรือด้วยการอุ้ม token ข้าม auth round trip
(ซึ่งเป็นการตัดสินใจเชิงความปลอดภัย ไม่ใช่แค่ถ้อยคำ)

### 5 · ที่ยังเหลือให้คนทำ

| ยังไม่ได้ทำ | ทำไมเครื่องทำแทนไม่ได้ |
|---|---|
| ส่งลิงก์ผ่าน **LINE จริง** แล้วดูว่า LINE ห่อ/ตัด/แปลงลิงก์ไหม | ไม่มี LINE บน emulator และเป็นพฤติกรรมของแอปคนอื่น |
| เดินต่อจากการ์ดคำเชิญ → สมัคร/ล็อกอิน → กดรับ → เข้าร้าน **บนเบราว์เซอร์มือถือ** | ผมขับฟอร์มด้วย `adb input` ไม่สำเร็จ (คีย์บอร์ด/พิกัดคลาดกัน) — **ไม่ใช่หลักฐานว่าฟีเจอร์พัง** · เส้นทางเดียวกันนี้ E-05 เดินผ่านทุกรอบ CI บนเบราว์เซอร์เดสก์ท็อป |

## M-02 — admin-reset ให้เจ้าของร้าน ⚠️ **คำถามที่ runbook ถาม ตอบไม่ได้ และเหตุผลหนักกว่าที่คิด**

runbook ถามว่า *"Owner ที่เจอข้อความ 404 นี้ รู้ไหมว่าต้องทำอะไรต่อ"*
**Owner ไม่มีวันเจอข้อความนี้ เพราะไม่มีจอไหนเรียก endpoint นี้เลย**

```
$ grep -rn "reset-password|resetPassword" apps/web/src apps/mobile/lib   → 0 hits
$ grep -n  "reset-password" packages/contracts/openapi/root.yaml         → มี endpoint
```

**พฤติกรรมของ API เองถูกต้อง** (ยิงตรงด้วย bearer ของ Admin จริงในร้านจริง):

| ทำอะไร | ผล |
|---|---|
| Admin รีเซ็ตรหัสให้ **พนักงาน** (control) | `200 {"ok":true}` |
| Admin รีเซ็ตรหัสให้ **เจ้าของร้าน** | `404 {"error":{"code":"NOT_FOUND","message":"ไม่พบข้อมูล","traceId":"…"}}` |
| ตามด้วย: เจ้าของร้านล็อกอินด้วยรหัส**เดิม** | `200` — รหัสไม่ถูกแตะ ⇒ fail-closed จริง ไม่ใช่ write ที่พัง |

⇒ D-030/NEW-1 ทำงานตามที่เคาะไว้ · control ที่ได้ `200` คือสิ่งที่ทำให้ `404` แปลว่าอะไรได้จริง

**เรื่องที่ต้องให้ ux + product อ่าน — ทางกู้บัญชีมีอยู่แค่ใน API:**
จอเดียวที่พูดถึงเรื่องนี้คือ `/login/help`:

> "ตอนนี้ระบบยังไม่รองรับการขอรีเซ็ตรหัสผ่านด้วยตัวเอง **กรุณาติดต่อเจ้าของร้าน/ผู้ดูแลระบบ (Owner/Admin) ในทีมของคุณ เพื่อให้ตั้งรหัสผ่านใหม่ให้**"

…แล้วเจ้าของร้าน/ผู้ดูแลที่ถูกติดต่อ **ไม่มีปุ่มให้กด** ⇒ คำแนะนำบนจอชี้ไปที่ทางที่ยังไม่มีอยู่
· ราคาที่ D-030 เคาะรับไว้คือ "ร้านที่มี Owner คนเดียวกู้เองไม่ได้" · **ของจริงตอนนี้กว้างกว่านั้น:
ร้านที่มี Owner หลายคนก็กู้ไม่ได้ เพราะไม่มีจอ**

---

## M-03 — คำเชิญ Owner/Admin แสดง 24 ชม. ตรงกับ `expiresAt` จริง ✅

เชิญจริง 3 ใบด้วย 3 สิทธิ์ แล้วเทียบกับแถวใน Postgres:

| สิทธิ์ | จอบอก | `expiresAt` ใน DB (UTC → ICT) |
|---|---|---|
| เจ้าของร้าน | `ลิงก์ใช้ได้ถึง 5 ก.ย. 2569 20:30 (อีกประมาณ 24 ชั่วโมง)` | `2026-09-05 13:30:51` → **20:30:51** ✅ |
| ผู้ดูแล | `ลิงก์ใช้ได้ถึง 5 ก.ย. 2569 20:31 (อีกประมาณ 24 ชั่วโมง)` | `2026-09-05 13:31:32` → **20:31:32** ✅ |
| พนักงาน | `ลิงก์ใช้ได้ถึง 11 ก.ย. 2569 20:36 (อีกประมาณ 7 วัน)` | +7 วัน ✅ |

**หลักฐานว่าไม่ใช่ค่าคงที่ที่พิมพ์ไว้ (ซึ่งเป็นสิ่งที่ M-03 ถามจริง ๆ):**
คำเชิญสองใบที่ออกห่างกัน **41 วินาที** แสดงเวลา **20:30 กับ 20:31** ⇒ ตัวเลขเดินตามเวลาที่ออกลิงก์
· และสิทธิ์ที่ต่างกันให้ TTL ต่างกัน (24 ชม. vs 7 วัน) ⇒ อ่านจาก `expiresAt` ไม่ใช่จากค่าคงที่

ตรงกันทั้ง **3 จอ**: dialog ตอนสร้างลิงก์ · แถวในรายการคำเชิญ · จอรับคำเชิญ `/invite` (ที่คนถูกเชิญเห็น)

---

## M-04 — "ใครรับไปแล้วเมื่อไหร่" + ธง `acceptedUserCreatedAfterInvite` ⚠️

**ครึ่งที่ผ่าน — ถ้อยคำของธง ✅**
สมัครบัญชีใหม่ด้วยอีเมลที่ถูกเชิญ (บัญชีจึงเกิด*หลัง*ลิงก์) แล้วกดรับ · แถวคำเชิญขึ้นว่า:

> **บัญชีที่กดรับถูกสร้างขึ้นหลังจากออกลิงก์ ตรวจสอบว่าเป็นคนที่คุณตั้งใจเชิญ**

อ่านแล้วเป็น "ช่วยตรวจหน่อย" ไม่ใช่ "คนนี้น่าสงสัย" ⇒ **AC-3.9 ผ่าน**

**ครึ่งที่ไม่ผ่าน — "เมื่อไหร่" ไม่มีอยู่บนจอเลย → B-15**
แถวคำเชิญที่ถูกรับไปแล้วยังขึ้นว่า `ลิงก์ใช้ได้ถึง 5 ก.ย. 2569 20:30 (อีกประมาณ 24 ชั่วโมง)`
— ลิงก์ที่ถูกใช้ไปแล้ว · `acceptedAt` อยู่ใน contract ตั้งแต่ล็อกสเปก และ**ไม่มีโค้ด production ฝั่งเว็บบรรทัดไหนเคยอ่านมันเลย**
(ดู §B-15 — แก้แล้ว)

**เรื่องที่ต้องให้ ux ตัดสิน — ธงนี้ซ่อนอยู่หลังปุ่มที่ชื่อไม่ตรงกับของข้างใน:**
คำเชิญที่ถูก**รับแล้ว**จะเห็นได้ต่อเมื่อกด **"ดูคำเชิญที่หมดอายุ/ยกเลิกแล้ว"** เท่านั้น
· คนที่เพิ่งเชิญคนเข้าร้านไม่มีเหตุผลจะไปกดปุ่มที่เขียนว่า "หมดอายุ/ยกเลิก" ⇒ **คำเตือนความปลอดภัยอยู่ในที่ที่ไม่มีใครเปิด**
· หัวข้อในกล่องเปลี่ยนเป็น "คำเชิญทั้งหมด" ถูกแล้ว — ที่ไม่ตรงคือ*ป้ายบนปุ่ม* ซึ่งเป็น copy ของ ux (ux-wireframe §7 บรรทัด 486/548)

**อีกข้อที่สเปกสั่งไว้แต่ยังไม่มี:** ux-wireframe §7 บรรทัด 525 บอกว่าแถวที่ไม่ pending ควรมีปุ่ม
**"เชิญใหม่อีกครั้ง"** ที่เปิด S7 พร้อมเติมอีเมล/สิทธิ์เดิม — ยังไม่มีในของจริง (ไม่ได้แก้ในรอบนี้ · ของ ux/product จัดลำดับ)

---

## M-05 — error ไทย 18 ตัว + คำว่า "ร้าน" ไม่ใช่ "องค์กร" ✅ (ครึ่งที่เครื่องตรวจได้)

| ตรวจอะไร | ผล |
|---|---|
| `"องค์กร"` ใน source ที่ขึ้นจอ (web + mobile, ไม่นับ test) | **0 จุด** |
| `"องค์กร"` ใน message ฝั่ง server | **0 จุด** (เจอ 1 ที่เป็นคอมเมนต์อธิบายกฎใน generated types) |
| **client เอา `message` ดิบจาก server มาขึ้นจอไหม** | **ไม่เลยทั้งสองฝั่ง** — web แปลง `code`→copy ของตัวเองทุก call site · mobile ไม่มี `failure.message`/`e.message` สักจุด |

ข้อสุดท้ายคือข้อที่ทำให้ M-05 ตอบได้จริง: runbook เตือนว่า "ข้อความที่ hardcode ในคอมโพเนนต์คือที่ที่มันหลุด"
— ของจริงคือ **ไม่มีเส้นทางให้สตริงฝั่ง server ไปโผล่บนจอเลย** ⇒ สิ่งที่ ux ต้องอ่านคือ copy ของ client ล้วน ๆ

**ที่ยังต้องให้ ux อ่านเอง (เครื่องตอบแทนไม่ได้):**
`apps/web/src/i18n/errors.ts` มาร์กเอง 5 ตัวว่า `‡new` = ไม่มี wireframe รองรับ (`server` · `validation` ·
`notFound` · `conflict` · และ `retry`) ⇒ ยังรอ ux ยืนยัน

---

## M-06 — Owner คนสุดท้ายกดออกจากร้าน ✅

กดจริงในร้านที่มี Owner คนเดียว · dialog ยืนยัน → กดยืนยัน → ได้ `409 LAST_OWNER` แล้ว **dialog ค้างไว้พร้อมข้อความ**:

> **คุณเป็นเจ้าของร้านคนเดียวของร้านนี้ — ตั้งคนอื่นเป็นเจ้าของร้านก่อน แล้วจึงออกจากร้านนี้ได้**
> [ ไปหน้าสมาชิก ]

⇒ บอก**ทางออก**ไม่ใช่แค่บอกว่าห้าม · ไม่มี error ดิบ/ไม่มีคำว่า `409`/`LAST_OWNER` โผล่ ⇒ **ผ่าน**

**"ตันไหม" (ครึ่งที่ product ต้องตอบ):** ไม่ตันสำหรับการ*ออก* (ตั้งคนอื่นก่อนได้)
แต่ตันสำหรับการ*เลิกใช้ร้าน* — F-002 ไม่มี "ลบร้าน" ⇒ คนที่อยู่คนเดียวและไม่อยากได้ร้านนั้นแล้ว
เป็นเจ้าของมันตลอดไป · ตัวกันไว้ก่อนมีอยู่จริงและเห็นตั้งแต่สร้างร้าน: แบนเนอร์ "เริ่มต้นใช้งาน" มีปุ่ม
**"เชิญเจ้าของร้าน"** พร้อมประโยค *"ตั้งเจ้าของร้านสำรองไว้อีก 1 คน เผื่อวันหนึ่งคุณเข้าระบบไม่ได้"*

**nit (ของ ux):** CTA "ไปหน้าสมาชิก" ชี้ไป `/o/{id}/settings/members`
— ซึ่งเป็น**หน้าที่ยืนอยู่**ตอนกดออกจากหน้าสมาชิก ⇒ กดแล้วไม่เกิดอะไร (ถูกต้องเฉพาะตอนเปิด dialog จากจอ "ข้อมูลร้าน")

---

## M-07 / M-07ข / M-07ค — มือถือ ✅ **เดินจนจบแล้ว**

รอบแรกทำไม่ได้เลยเพราะ **B-18** (จอ F-002 ทั้งชุดไม่มีทางเข้าถึงในแอป) · **user เคาะทาง (ก) เมื่อ 2026-09-05**
⇒ ต่อ navigation ชั่วคราวให้ (`apps/mobile/lib/app/shop_shell.dart`) แล้วเดินทั้งสามข้อบน **Android 13 จริง (Pixel_5_API_33)**
· ระหว่างทางเจอและแก้ **B-17** ไปก่อน (แอปต่อ API ไม่ได้เลย)

### M-07 — จอ "ข้อมูลร้าน" บนมือถือ

| ข้อ | ผล | หลักฐาน |
|---|---|---|
| (ก) เลข**อ่านออก**บนจอเล็ก | ✅ | เลข 13 หลักขึ้นเต็มบรรทัดเดียว ไม่ตัดคำ ที่ `type.body.md` — ภาพจอจริงเก็บไว้แล้ว · **ยังควรให้คนดูบนเครื่องจริงอีกครั้ง** (emulator ไม่ตอบเรื่องความสบายตา) |
| (ข) **คัดลอกได้** (กดค้าง → Copy) | ✅ | กดค้างบนเลข → เมนู **ปุ่มเดียว "คัดลอก"** (ไม่มี Select all/Share — `contextMenuBuilder` ตัดทิ้งโดยตั้งใจ) |
| (ค) กด "ซ่อนเลข" แล้วเลขหายจริง | ✅ | กลับเป็น `•••••••••3454` ทันที |
| (ง) **สลับไปแอปอื่น → app switcher ต้องไม่เห็นเลข** | ✅✅ | ดูด้านล่าง — การ์ดในหน้า recents **ว่างเปล่าทั้งใบ** |
| (จ) กลับเข้าแอป เลขไม่ค้าง | ✅ | กด HOME แล้วกลับเข้ามา → `•••••••••3454` |

**(ง) คือข้อที่ runbook บอกว่าเครื่องตรวจแทนไม่ได้ — ตรวจได้แล้ว และผ่านสองชั้น:**

| จังหวะ | `adb shell screencap` |
|---|---|
| ก่อนกดแสดงเลข (guard ปิด) | **exit 0 · 165 KB** |
| หลังกดแสดงเลข (guard เปิด) | **exit 1 · 0 ไบต์** — ระบบปฏิเสธการจับภาพ |
| หน้า Home ของ Android | **exit 0 · 1.36 MB** |

⇒ ไม่ใช่แค่ "เราสั่ง FLAG_SECURE" แต่ **OS บังคับใช้จริง** และเปิด/ปิดตามสถานะเลขจริง ๆ
· และเมื่อกด HOME ตอนเลขโชว์อยู่ **การ์ดของ OmniStock ในหน้า recents เป็นสี่เหลี่ยมขาวเปล่า** — ไม่มีชื่อร้าน ไม่มีเลข ไม่มีอะไรเลย

### M-07ข — 3 ทางที่ probe ของ security reviewer เคยเจอว่าพัง ✅ ทั้งสาม

| | ทำอะไร | ผล |
|---|---|---|
| (1) | เปิดเลข → ไปแท็บ "สมาชิก" → กลับมา | `•••••••••3454` ✅ |
| (2) | เปิดเลข → **สลับไปร้านที่สอง** (สร้างจากในแอป) | การ์ดร้านใหม่ไม่มีตัวเลขของร้านเดิมเลย — ขึ้น "ร้านนี้ยังไม่ได้ประกาศข้อมูลผู้เสียภาษี" ✅ |
| (3) | **ออกจากระบบ → ให้อีกคนล็อกอิน** (Owner คนที่สองของร้านเดียวกัน) | ไล่ดู 6 เฟรมแรกติดกัน — `•••••••••3454` ทุกเฟรม ไม่มีเลขของคนก่อน ✅ |

(1) เป็นผลของการออกแบบเปลือกโดยตั้งใจ: **ไม่ใช้ `IndexedStack`** ⇒ ออกจากแท็บ = `_OrgProfileScreenState` ถูก dispose
· มีเทสต์ยึดไว้แล้ว และเทสต์นั้น**แดงจริง**ถ้าเปลี่ยนไปใช้ `IndexedStack`

### M-07ค — clipboard preview ของ Android 13 ✅ พร้อม control สองทาง

| คัดลอกอะไร | ผ่านทางไหน | preview ที่ระบบเด้งขึ้น |
|---|---|---|
| เลขผู้เสียภาษี | `SensitiveClipboard` (`EXTRA_IS_SENSITIVE`) | **`••••••`** — เนื้อหาถูกปิดบัง |
| ข้อความธรรมดาในช่อง "ชื่อร้าน" ของ**แอปเดียวกัน** | clipboard ปกติของ Flutter | **`CONTROL-TEXT-1234`** เต็ม ๆ (แถบ clipboard ของคีย์บอร์ดก็ขึ้นด้วย) |

⇒ control ฝั่งซ้ายเป็นสิ่งที่ทำให้ฝั่งขวามีความหมาย: ถ้าไม่มีมัน "ไม่เห็นตัวเลข" อาจแปลว่า Android รุ่นนี้ไม่โชว์ preview เลยก็ได้

**ยังทำไม่ได้ — ฝั่ง iOS ของ M-07ค** (`localOnly` / Universal Clipboard): ไม่มี mac runner ไม่มีเครื่อง Apple
และ **`AppDelegate.swift` ยังไม่เคยถูก compile เลยสักครั้ง** ⇒ อยู่ในความรับผิดชอบของ forward-commitment ฝั่ง iOS ของ devops

# ของใหม่ที่เจอในรอบนี้

## B-15 — แถวคำเชิญที่ตายแล้ว ยังโฆษณาว่าลิงก์ยังใช้ได้ ✅ แก้แล้ว

กด "ยกเลิกคำเชิญ" · dialog ยืนยันบอกว่า *"ลิงก์ที่ส่งไปแล้วจะใช้ไม่ได้ทันที"* · กดยืนยัน · แถวที่เหลือไว้เขียนว่า:

> **ลิงก์ใช้ได้ถึง 11 ก.ย. 2569 20:36 (อีกประมาณ 7 วัน)**

คำเชิญที่ถูก**รับไปแล้ว**ก็พูดแบบเดียวกัน · สิ่งเดียวที่ต่างระหว่างแถวเป็น ๆ กับแถวตายคือ **การไม่มีปุ่มสองปุ่ม**

**รากของมันคือรูปที่เจอซ้ำทั้งฟีเจอร์นี้:** `expiry.ts` เตือนเรื่องนี้ไว้ใน doc comment ของตัวเองว่า
*"telling somebody a dead link is live"* แล้ว `MembersScreen` ก็เรียก `formatExpiry` กับทุกแถวโดยไม่ดู `status`
— **โมดูลรู้กฎ ไม่เท่ากับจอทำตามกฎ** · `expiresAt` เป็น NON-NULL ทุกแถวตามสัญญา (ux Q14) มันจึงอ่านได้เสมอและผิดเสมอ

**แก้:** ป้ายสถานะตามที่ **ux-wireframe §7 บรรทัด 549 สั่งไว้อยู่แล้ว** (ไม่ใช่ copy ใหม่):
`รอตอบรับ · หมดอายุแล้ว · ยกเลิกแล้ว · รับแล้วเมื่อ {วันเวลา}`
· บรรทัดหมดอายุขึ้น**เฉพาะแถวที่ลิงก์ยังใช้ได้จริง** · แถวที่เลย `expiresAt` ขึ้น "หมดอายุแล้ว" ทันทีโดยไม่รอ server (§14 บรรทัด 1091)
· `acceptedAt` ถูกอ่านเป็นครั้งแรก ⇒ **ปิดครึ่งที่ขาดของ M-04 ไปด้วย**

เทสต์: `expiry.test.ts` (12) + `MembersScreen.invitation-status.test.tsx` (3 — มีเคส non-vacuity ว่าแถวเป็น ๆ ต้องยังมีบรรทัดหมดอายุ)
· red→green ยืนยันแล้ว: ถอดการแก้ออก → 2 เคสแดงทันที

## B-16 — ทั้งแอปเว็บไม่มีปุ่ม "ออกจากระบบ" ✅ แก้แล้ว

เจอตอนจะสลับบัญชีเพื่อทำ M-04 · ทางเดียวที่ออกจากระบบได้คือ **"ออกจากระบบทุกอุปกรณ์"** ที่หน้าความปลอดภัย
— คนละคำถามกันคนละเรื่อง (ฆ่าทุก session ทุกเครื่องของคนนั้น)

- **ux-wireframe §S2 วาด "ออกจากระบบ" ไว้เป็นแถวสุดท้ายของ sidebar** ⇒ สเปกมี ของไม่มี
- `orgTh.shell.nav.logout = "ออกจากระบบ"` มีอยู่ในดิกชันนารี **และไม่มีใครเรียกเลยทั้ง tree**

**แก้:** `NavAction` (แถว sidebar ที่ทำ action แทนที่จะ navigate) + เรียก `logoutDevice()` = จบเฉพาะ session นี้
· **ล้มแล้วอยู่กับที่และบอก** ไม่เด้งไป `/login` แบบหลอก ๆ (เพราะ `logoutDevice` เคลียร์ token หลังได้ `204` เท่านั้น
⇒ ถ้าล้ม session ยังอยู่ครบ การพาไปหน้า login จะเป็นการโชว์การออกจากระบบที่ไม่ได้เกิดขึ้น)

เทสต์: `AppShell.logout.test.tsx` (4) + **E-16 บนเบราว์เซอร์จริง** — กดออก แล้ว**โหลด `/` ใหม่**ต้องยังอยู่ที่ login
(ครึ่งนี้ component test พิสูจน์ไม่ได้ เพราะ refresh cookie เป็น httpOnly)

⚠️ copy ตอนล้ม (`ออกจากระบบไม่สำเร็จ ลองใหม่อีกครั้ง`) ผมเขียนจาก `logoutAllFailed` ของ ux ย่อลงมาเหลือเครื่องเดียว
— มาร์ก COPY GAP ไว้ในไฟล์ i18n แล้ว **รอ ux ยืนยัน**

## B-17 — แอปมือถือชี้ไป API ไหนไม่ได้เลย ✅ แก้แล้ว

`--dart-define=API_BASE_URL=…` ที่ทั้ง runbook และเลน CI ส่งเข้าไป **ถูกอ่านโดย
`integration_test/org_flow_test.dart` ไฟล์เดียว** · `lib/main.dart` ฮาร์ดโค้ด `http://localhost:3000` ไว้
— ซึ่งใน emulator (และบนมือถือจริง) `localhost` คือ**ตัวเครื่องเอง** ⇒ ทุก request ตาย

จอ login รายงานว่า **"เกิดข้อผิดพลาด กรุณาลองใหม่อีกครั้ง"** เพราะ `login_controller.dart` มี `catch (_)`
ที่กลืน exception ทั้งหมด (มีสตริง `errorNetwork` = "เชื่อมต่อไม่ได้ กรุณาลองใหม่" อยู่ใน ARB แต่เส้นนี้ไม่เคยใช้)
⇒ ผมเสียเวลาไล่ผิดทางไปหลายรอบ (สงสัย cleartext HTTP ก่อน — ลองแล้วไม่ใช่) เพราะข้อความมันไม่บอกอะไรเลย
· **ฝากเป็นคำถามของ ux/mobile: connection failure ควรพูดว่า "เชื่อมต่อไม่ได้" ไหม** — สตริงมีอยู่แล้ว

**ทำไม CI ถึงเขียวมาตลอด:** E-10 สร้าง provider graph เองจาก define แล้วเรียก `buildAppOverrides` ตรง ๆ
ไม่เคยผ่าน `main.dart` ⇒ **harness กับแอปต่างคนต่างสอดคล้องกับตัวเอง แล้วไม่ตรงกัน** — รูปเดียวกับที่ audit ทั้งชุดเจอ

**แก้:** `main.dart` อ่าน `String.fromEnvironment('API_BASE_URL', defaultValue: 'http://localhost:3000')`
· tripwire `test/app/base_url_define_test.dart` บังคับว่า **แอป · integration test · runbook ต้องพูดถึงคีย์เดียวกัน**
(เทสต์พฤติกรรมจับไม่ได้: `String.fromEnvironment` เป็น compile-time const) · red→green ยืนยันแล้ว
**หลังแก้: ล็อกอินบน emulator สำเร็จจริง**

## B-18 — ✅ **ปิดแล้ว** (เปิดไว้ให้ product เคาะ · user เลือกทาง (ก) 2026-09-05) จอ F-002 ทั้งชุดบนมือถือไม่มีทางเข้าถึงในแอปจริง

หลังแก้ B-17 แล้วล็อกอินได้ · แอปพาไปที่ **`SecurityScreen` ของ F-001** แล้วจบ — ไม่มีทางไปไหนต่อ

```
CreateOrgScreen  : 0 อ้างอิงจากที่อื่นใน lib/
MembersScreen    : 0
OrgPickerScreen  : 0
OrgProfileScreen : 0        ← จอที่ M-07 ต้องใช้
```

`app/app.dart` มี 3 ปลายทาง: `bootstrap` · `authFlow` · `authenticated → SecurityScreen`
· ไฟล์นั้นเขียนไว้เองว่า *"F-006 owns real navigation/IA (`app/router.dart`)"*

**นี่คือ B-4 อีกชั้นหนึ่ง** — B-4 คือ "provider ไม่ถูก wire เข้าแอป" (ปิดไปแล้ว) · ชั้นนี้คือ
**provider ถูก wire แล้ว แต่ไม่มี*จอ*ไหนถูก wire** · และเป็นรูปเดียวกับ B-14 บนเว็บ (`/` เป็น placeholder
เพราะไม่มีเทสต์ไหนเปิดมัน) — **E-10** (`apps/mobile/integration_test/org_flow_test.dart`) สร้าง
`MembersScreen` ขึ้นมาเองใน `MaterialApp` ของตัวเอง
⇒ พิสูจน์ได้ว่า**จอทำงานเมื่อมีคนพามันขึ้นมา** · พิสูจน์ไม่ได้ว่า**มีใครไปถึงมันได้**

**ผลที่ต้องพูดตรง ๆ:** `CHANGELOG.md` เขียนว่า F-002 ship *"mobile: จอเลือกร้าน/สร้างร้าน/สลับร้าน/สมาชิก/เชิญ/โปรไฟล์ร้าน + การ์ดภาษี"*
— จอมีจริงและทำงานจริง แต่**เปิดไม่ได้ในแอป**

**ตอนแรกผมไม่แก้เอง** เพราะการสร้าง navigation หลัง login เป็นขอบเขตที่ `app.dart` ประกาศว่าเป็นของ **F-006**
และเป็นคำถามเรื่อง scope แบบเดียวกับ B-14 ⇒ ยกให้ **@product** เคาะระหว่าง (ก) ต่อ nav ชั่วคราวใน F-002 เลย
กับ (ข) ยอมรับว่ามือถือยังใช้ไม่ได้จนถึง F-006 แล้วแก้ CHANGELOG/AC ให้ตรง

### ✅ **user เคาะทาง (ก) เมื่อ 2026-09-05** — ต่อ nav ชั่วคราวแล้ว

`apps/mobile/lib/app/shop_shell.dart` (ใหม่) — เปลือก 3 แท็บที่ **F-006 ควรลบทิ้ง ไม่ใช่ต่อยอด** ·
`app.dart` เอาปลายทางมาจาก `SessionState` แทน enum ส่วนตัว · `OrgProfileScreen` ใช้ `OrgSwitcherTitle`
บน AppBar ตาม §13 · `SecurityScreen` รับ `appBarActions` เพื่อให้เปลือกใส่ปุ่ม "ออกจากระบบ" ได้

**ครึ่งที่อันตรายกว่า:** ไม่ใช่แค่จอไม่มีคนเรียก แต่ **`signedIn()` ไม่เคยถูกเรียกจากที่ไหนเลยใน `lib/`**
⇒ session ค้างที่ `SessionUnknown` และ `switchOrg` มี early-return ⇒ **ต่อแค่จอโดยไม่ต่อ session
= แตะร้านแล้วเงียบสนิท** (`learnCapabilities` กลืน error ตามดีไซน์)

⇒ **M-07 · M-07ข · M-07ค เดินได้จนจบทั้งหมด** — ผลอยู่ที่ §M-07 ด้านบน

---

## หมายเหตุเรื่องเลน browser ระหว่างทำ manual pass

หลัง manual pass เลน Playwright แดง 6 เคส · **ไม่ใช่ regression** — ผมใช้ throttle ของ F-001 หมดไปกับการเดินจอด้วยมือ
(`throttle:ip:*` + `orgrl:*` ใน Redis) · เคลียร์แล้วเขียว **29/29 ใน 27.9 วิ**
· บันทึกไว้เพราะคนต่อไปที่ทำ §12.2 จะเจอเหมือนกัน: **ทำ manual pass แล้วเลนจะแดงจนกว่าจะเคลียร์ตัวนับ**
