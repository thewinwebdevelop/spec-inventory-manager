---
doc: ux-wireframe
owner: "@ux"
signoff: approved   # user 2026-07-29 (หลัง mockup sign-off D-026/D-031)
---
# [F-002] UX wireframe & flow

> เจ้าภาพ: `ux` · ออกแบบบน constraint ของ [api-spec.md](api-spec.md) (DRAFT — รอคำตอบชุดนี้เพื่อ lock)
> + [architecture.md](architecture.md) Contract summary · Gate-1 AC:
> [F-002-organization-license-membership.md](../F-002-organization-license-membership.md)
> · decision ที่ผูกตรง: **D-012** (ไม่มี email — copy link) · **D-027** (ลิงก์ครั้งเดียว + ออกลิงก์ใหม่)
> · **D-028** (Owner-only, PDPA, TTL 24 ชม.) · **D-025** (ApiFailure/error code) · **D-026** (token Calm Teal)
> · **D-029** (คำว่า "ร้าน" + ออกจากร้านเอง) · **D-030** (ห้ามผู้ดูแลรีเซ็ตรหัสเจ้าของร้าน → 404 · reveal TIN เปิดถึงผู้ดูแล)
> · ตรงกับ contract **amend #4** ของ [api-spec.md](api-spec.md) (2026-07-28)
> เกณฑ์ตรวจงาน: "แม่ค้าที่ไม่เคยใช้ ERP เปิดมาใช้เป็นโดยไม่ต้องอ่านคู่มือ" (skill `thai-ux`)

## Contract summary (≤20 บรรทัด — ทีม consumer อ่านแค่ส่วนนี้)

1. **13 จอ/แผ่น** (S1–S13) ครอบ US-1…US-7 + เคส 403 สองแบบ — ตาราง §1.2
2. **คำเดียวบนจอ = "ร้าน"** (ไม่ใช้คำว่า "องค์กร"/"tenant"/"license" บน UI) · glossary §1.3
3. **จอสมาชิก = จอเดียว 2 ส่วนบนหน้าเดียว** (คำเชิญที่รอตอบรับ ↑ / รายชื่อสมาชิก ↓) ใช้ 2 endpoint เดิม — **ไม่ต้องมี endpoint รวม** (ตอบ Q1)
4. **ลิงก์คำเชิญแสดงครั้งเดียว** — แผ่น `CopyLinkPanel` + **แถบเตือน "แสดงครั้งเดียว" เป็นด่านเดียว** (ไม่มี dialog กันปิด — user เคาะ 2026-07-28) · ปุ่มบนคำเชิญค้างชื่อ **"ออกลิงก์ใหม่"** + ยืนยันก่อนเสมอ (D-027) · **ห้ามมีคำว่า "คัดลอกลิงก์เดิม" ที่ไหนเลย**
5. **อายุลิงก์อ่านจาก `expiresAt` เท่านั้น** — UI แสดงเป็นวัน-เวลาไทย + "(อีกประมาณ …)" · **ไม่มีตัวเลข 7 วัน/24 ชม. hardcode** ในทุก copy (ตอบ Q14)
6. **`403 ORG_ACCESS_DENIED` → พากลับหน้าเลือกร้าน + refetch `/me/organizations`** · **`403 FORBIDDEN` → อยู่หน้าเดิม + toast** (route-level = panel) — คนละ handler เด็ดขาด (ตอบ Q6, §11)
7. **Staff:** ซ่อนเมนู "สมาชิก" ตาม `myMembership.capabilities` + มี `ForbiddenPanel` ถ้าเข้าตรงผ่าน URL · บล็อกภาษีเห็นแค่ **สถานะ + จด VAT ไหม ไม่มีตัวเลขใด ๆ แม้แต่ 4 ตัวท้าย** (ตอบ Q13)
8. **สร้างร้านเสร็จ → เข้าร้านใหม่ทันที** จาก response 201 (ไม่ยิง `/me/organizations` ซ้ำ, แค่ invalidate cache) — ตอบ Q5
9. **จอ tax profile = โหมดดู + ปุ่ม "แก้ไข"** (PUT ทั้งชุด) · เลขเต็มถูก mask ไว้ก่อนเสมอ มีปุ่ม "แสดงเลขเต็ม" (ตอบ Q7)
10. ปุ่มที่สร้างของ (สร้างร้าน / สร้างลิงก์คำเชิญ / ออกลิงก์ใหม่) **disabled ระหว่างรอ** — ยังไม่มี `Idempotency-Key`
11. **`/invite` เป็นข้อยกเว้นเดียวของ design-system §8** — ต้องใช้งานได้ดีบน **เบราว์เซอร์มือถือ** (ลิงก์ถูกส่งทางแชต ผู้รับเปิดบนมือถือเป็นหลัก)
12. reuse component เดิมของ F-001 เป็นหลัก · ของใหม่ 6 ตัวเข้า design system กลาง — รายละเอียดใน [ui.md](ui.md)
13. ไม่มี field ใหม่ที่บังคับจาก backend · **ขอ 1 field แบบ "มีก็ดี"**: `key` (slug) ของ role — มี fallback ถ้าไม่ให้ (ตอบ Q4)
14. **"แสดงเลขเต็ม" = การกระทำที่ตั้งใจ** → เรียก `POST …/tax-profile/reveal` ทุกครั้ง (เลขเต็มไม่ติดมากับ `GET /orgs/{orgId}` อีกแล้ว) · ค่าอยู่ใน memory เท่านั้น · "ซ่อนเลข" = ทิ้งค่า · **จำกัด 20 ครั้ง/ชม.** → มี copy ของ 429 + ทางไปต่อที่ไม่ตัน (§5/§6)
15. **`409 CONFLICT` + `details.reason="busy"` มี copy กลาง** ("ระบบกำลังทำรายการอื่นของร้านนี้อยู่…" + ลองใหม่) — ผู้ใช้ต้องไม่เห็นข้อความดิบ/ไม่คิดว่าระบบพัง (§1.4)
16. **"ออกจากร้านนี้" = `DELETE /orgs/{orgId}/membership`** (D-029) — สมาชิก active ทุกคนทำได้ ⇒ **ทางเข้าอยู่ที่ S4 ซึ่งทุกคนเห็น** ไม่ใช่แค่จอสมาชิก · Owner คนสุดท้ายออกไม่ได้ (`409 LAST_OWNER`) มี copy เฉพาะ
17. **S13 (รีเซ็ตรหัสผ่าน) ครอบ 2 เคสด้วยข้อความเดียว** (D-028/C-2 = สังกัดหลายร้าน · **D-030/NEW-1 = target เป็นเจ้าของร้าน**) — ตั้งความคาดหวังก่อนกดแบบไม่ระบุเงื่อนไข · ล้มเหลวแล้วได้ข้อความเดียวกันเสมอ ไม่บอกสาเหตุ (§12.3)
18. **ร้านที่มีเจ้าของร้านคนเดียว = กู้รหัสเองไม่ได้** (ราคาของ D-030) ⇒ ชวน "ตั้งเจ้าของร้านสำรอง" **ล่วงหน้า 2 จุด**: การ์ดเริ่มต้นใช้งานของ S4 (ร้านเพิ่งสร้าง — §5) + แถบ info บนจอสมาชิก (§7 ท้ายหัวข้อ) · **ไม่เตือนในฟอร์มสร้างร้าน** (ตอนนั้นทำอะไรไม่ได้)

---

## 1. ภาพรวม

### 1.1 Flow map

```
[F-001 login สำเร็จ]
        │
        ▼
  ┌──────────────────────┐   ยังไม่มีร้านเลย    ┌─────────────────────┐
  │ S1 เลือกร้าน          │ ─────────────────▶ │ S2 สร้างร้านใหม่      │
  │  /select-org         │ ◀───────────────── │  /orgs/new          │
  └──────────┬───────────┘   (ยกเลิก)          └──────────┬──────────┘
             │ เลือกร้าน                                   │ สร้างสำเร็จ (201)
             ▼                                            ▼
  ┌───────────────────────────────────────────────────────────────────┐
  │ S3 โครงหน้าจอของร้าน  /o/{orgId}/**   (มีตัวสลับร้านอยู่เสมอ)          │
  │   ├─ S4 ข้อมูลร้าน (หน้าแรกของร้านใน Phase 0) ──▶ S5 ข้อมูลผู้เสียภาษี   │
  │   └─ S6 สมาชิก  (เฉพาะผู้มีสิทธิ์จัดการทีมงาน)                          │
  │        ├─▶ S7 เชิญสมาชิก ──▶ S8 ลิงก์คำเชิญ (แสดงครั้งเดียว)            │
  │        │        └─(อีเมลนี้มีคำเชิญค้าง)─▶ ออกลิงก์ใหม่ / ยกเลิกคำเชิญ    │
  │        ├─▶ S9 เปลี่ยนสิทธิ์                                          │
  │        └─▶ S10 ถอดออกจากร้าน / ออกจากร้านนี้                          │
  └───────────────────────────────────────────────────────────────────┘
             │ ระหว่างใช้งานแล้วถูกถอด (403 ORG_ACCESS_DENIED)
             ▼
       S12ก พากลับ S1 พร้อมแถบแจ้ง          S12ข ขาดสิทธิ์ (403 FORBIDDEN) = อยู่หน้าเดิม + toast

[ผู้ถูกเชิญได้ลิงก์ทางแชต]
        ▼
  ┌────────────────────────┐  ยังไม่ล็อกอิน ─▶ [F-001 login/signup] ─┐
  │ S11 รับคำเชิญ  /invite  │  ล็อกอินผิดบัญชี ─▶ S11ค แจ้ง + สลับบัญชี │
  │  (public, มือถือเป็นหลัก) │  ล็อกอินถูกบัญชี ─▶ [เข้าร่วมร้าน] ───────┴─▶ S3
  └────────────────────────┘
```

### 1.2 ตารางจอ ↔ AC ↔ endpoint

| # | จอ | ตอบ AC | endpoint หลัก |
|---|---|---|---|
| S1 | เลือกร้าน | US-2 | `GET /me/organizations` |
| S2 | สร้างร้านใหม่ | US-1 | `POST /organizations` |
| S3 | โครงหน้าจอ + ตัวสลับร้าน | US-2 | `GET /me/organizations`, `GET /orgs/{orgId}` |
| S4 | ข้อมูลร้าน (หน้าแรกของร้าน) | US-1/US-7 | `GET /orgs/{orgId}`, `PATCH /orgs/{orgId}` |
| S5 | ข้อมูลผู้เสียภาษี | US-7 | `PUT /orgs/{orgId}/tax-profile` |
| S6 | สมาชิก (คำเชิญค้าง + รายชื่อ) | US-3/US-5 | `GET …/members`, `GET …/invitations` |
| S7 | เชิญสมาชิก | US-3 | `POST …/invitations`, `GET …/roles` |
| S8 | ลิงก์คำเชิญ / ออกลิงก์ใหม่ | US-3 (D-027) | `POST …/invitations/{id}/link` |
| S9 | เปลี่ยนสิทธิ์ | US-5/US-6 | `PATCH …/members/{userId}` |
| S10 | ถอดออกจากร้าน / ออกจากร้านนี้ | US-5/US-6 | `DELETE …/members/{userId}` · **`DELETE /orgs/{orgId}/membership`** (D-029) |
| S11 | รับคำเชิญ `/invite` | US-4 | `POST /invitations/preview`, `POST /invitations/accept` |
| S12 | 403 สองแบบ | US-2/US-5 | (ทุกเส้น org-scoped) |
| S13 | copy ฝากไว้: บางกรณีตั้งรหัสผ่านใหม่ให้ไม่ได้ | D-028/C-2 · **D-030/NEW-1** | `POST …/reset-password` (F-001, จอของ F-004) |

### 1.3 Glossary ของ F-002 (คำเดียวต่อ 1 ความหมาย — ห้ามใช้คำอื่นแทน)

| ความหมายในโมเดล | คำบนจอ | ห้ามใช้ |
|---|---|---|
| Organization | **ร้าน** | องค์กร, tenant, workspace, license, บริษัท |
| Membership / member | **สมาชิก** (รวม ๆ เรียก "ทีมงาน") | ผู้ใช้, user, account |
| Role | **สิทธิ์** ("ให้สิทธิ์เป็น…") | role, บทบาท, ตำแหน่ง |
| Owner / Admin / Staff | **เจ้าของร้าน / ผู้ดูแล / พนักงาน** | Owner, Admin, Staff (ภาษาอังกฤษบนจอ) |
| Invitation | **คำเชิญ** · ลิงก์ = **ลิงก์คำเชิญ** | invite, invitation |
| revoke membership | **ถอดออกจากร้าน** (ตัวเอง = **ออกจากร้านนี้**) | ลบ, remove, ระงับ |
| rotate token | **ออกลิงก์ใหม่** | คัดลอกลิงก์เดิม, ส่งใหม่, resend |
| tax profile / TIN | **ข้อมูลผู้เสียภาษี** / **เลขผู้เสียภาษี** | TIN, tax id, นิติฐานะ (คำนี้ใช้ในสเปคได้ ไม่ขึ้นจอ) |
| entityType | **บุคคลธรรมดา / นิติบุคคล** | personal, company |
| vatRegistered | **จดทะเบียน VAT** | VAT registered |

> **ทำไมเลือก "ร้าน":** ผู้ใช้เป้าหมาย (dogfood + SME) เรียกหน่วยธุรกิจของตัวเองว่า "ร้าน" ไม่ใช่ "องค์กร" ·
> เคสนิติบุคคล/สำนักงานบัญชียังอ่านรู้เรื่อง เพราะทุกจอที่พูดถึงนิติบุคคลจะพูดผ่านคำว่า "ข้อมูลผู้เสียภาษีของร้าน" ·
> **ย้อนกลับได้ถูก** — เป็นค่าใน i18n key ล้วน ถ้า user อยากใช้ "องค์กร" แก้ที่ `messages.th` จุดเดียว

### 1.4 กติกาที่ใช้ร่วมทุกจอ

- **4 states ทุกจอ** ตาม design-system §2: loading = **skeleton shimmer ตาม layout จริง** (ไม่ใช่ spinner) · empty = ข้อความ + CTA · error = สาเหตุ + ปุ่ม "ลองใหม่" · data
- **วันที่/เวลา** = "3 ส.ค. 2026, 14:30" (Asia/Bangkok, แปลงจาก ISO ที่ server ส่ง) · เวลาที่เหลือแสดงเป็น "(อีกประมาณ 6 ชั่วโมง)" — **คำนวณจาก `expiresAt` เท่านั้น**
- **ปุ่มที่สร้างของ disabled + เปลี่ยนเป็น loading label ระหว่างรอ** (ยังไม่มี `Idempotency-Key`)
- **ซ่อน/แสดงปุ่มตาม `myMembership.capabilities`** (§3.3 ของ api-spec) — เป็น UX เท่านั้น, server enforce เสมอ; ทุกปุ่มที่ซ่อนต้องมี error path รองรับกรณี server ปฏิเสธ
- **`429 RATE_LIMITED`** ทุกจุด reuse `ThrottleBanner` ของ F-001 (นับถอยหลังจาก `Retry-After`) — ห้ามใช้คำว่า "ถูกล็อก/ถูกระงับ" (D-005)
- **`422 ORG_MISMATCH` / `ORG_CONTEXT_REQUIRED`** = บั๊กฝั่ง client → toast กลาง "เกิดข้อผิดพลาด กรุณาลองใหม่อีกครั้ง" · **ห้ามเตะผู้ใช้ออกจากร้าน** (ต่างจาก `ORG_ACCESS_DENIED`)
- **`409 CONFLICT` + `details.reason === "busy"`** (ระบบชนกันชั่วคราว — api-spec §1 "Lock contention" / architecture §5.2)
  → **ข้อความกลางชุดเดียวทุกจอ** ในที่เดียวกับ error ของจอนั้น (toast สำหรับปุ่ม · `ErrorBanner` ในกล่องสำหรับฟอร์ม):
  > "ระบบกำลังทำรายการอื่นของร้านนี้อยู่ กรุณารอสักครู่แล้วลองใหม่อีกครั้ง" + ปุ่ม/ลิงก์ "ลองใหม่"
  · **ห้ามแสดงคำว่า `busy`/`CONFLICT`/`reason` หรือ `traceId` ให้ผู้ใช้เห็น** · ห้ามใช้ถ้อยคำว่า "ระบบล่ม/ผิดพลาดร้ายแรง"
  (นี่คือสถานการณ์ชั่วคราว ไม่ใช่ของพัง) · ฟอร์ม **ไม่ล้างค่าที่กรอกไว้** ผู้ใช้กดซ้ำได้ทันที
  · **client ที่ยังไม่รู้จัก `reason` ต้องทำงานถูก** → ถ้าไม่มี `details.reason` ให้ใช้ข้อความ `409` เฉพาะจอนั้นตามเดิม
  (เช่น "คำเชิญนี้ไม่ได้รออยู่แล้ว") — เช็ค `reason === "busy"` ก่อนเสมอ แล้วค่อย fallback

### 1.5 ข้อความสำรอง (fallback) — เมื่อจอไม่ได้ระบุคำของตัวเอง

> เพิ่ม 2026-09-05 (manual pass §12.2 / M-05) · ของจริงอยู่ที่ `apps/web/src/i18n/errors.ts`
> · **นี่คือด่านสุดท้าย ไม่ใช่ทางลัด** — จอที่รู้ว่าอะไรพังต้องเขียนคำของตัวเองเสมอ (ตาราง error ของแต่ละจอ)
> ด่านนี้มีไว้ไม่ให้ผู้ใช้เห็น error code ดิบเมื่อมีเคสที่จอลืมนึกถึง

**กติกาของข้อความสำรอง:** ต้องบอกให้ได้ 2 อย่าง คือ **นี่เป็นปัญหาของใคร** และ **ทำอะไรต่อ** ·
"เกิดข้อผิดพลาด" เฉย ๆ ไม่บอกทั้งสองอย่าง (B-17 บนมือถือเสียเวลาไล่บั๊กไปหลายรอบ เพราะแอปพูดว่า
"เกิดข้อผิดพลาด" ทั้งที่ความจริงคือ "ต่อเซิร์ฟเวอร์ไม่ติด")

| กรณี | ข้อความ | ทำไมคำนี้ |
|---|---|---|
| ต่อไม่ติด (network / timeout / DNS / TLS) | เชื่อมต่อไม่สำเร็จ ตรวจสอบอินเทอร์เน็ตแล้วลองใหม่อีกครั้ง | เป็นกรณีเดียวที่ผู้ใช้ **แก้เองได้** ⇒ บอกสิ่งที่ให้ไปตรวจ |
| 5xx (คำขอถึงเราแล้ว แต่ฝั่งเราพัง) | ระบบขัดข้องชั่วคราว กรุณาลองใหม่อีกครั้ง | บอกว่าเป็นของ**เรา** ไม่ใช่ของเขา + "ชั่วคราว" กันความรู้สึกว่าข้อมูลหาย · ⛔ ห้ามใช้คำเดียวกับบั๊ก client (ด้านล่าง) — ใช้คำเดียวกันแล้วแยกไม่ออกว่าใครพัง |
| `422 ORG_MISMATCH` / `ORG_CONTEXT_REQUIRED` (บั๊ก client) | เกิดข้อผิดพลาด กรุณาลองใหม่อีกครั้ง | ตาม §1.4 · เป็นความผิดของโค้ดเรา ผู้ใช้ทำอะไรไม่ได้ ⇒ ไม่ชี้ให้ไปตรวจอะไรทั้งนั้น |
| 422 ที่ไม่มี `fieldErrors` ให้ชี้ | ข้อมูลที่กรอกไม่ถูกต้อง กรุณาตรวจสอบแล้วลองใหม่อีกครั้ง | "ที่กรอก" ชี้ไปที่ฟอร์ม ไม่ใช่ข้อมูลของร้าน |
| 404 | ไม่พบข้อมูลนี้แล้ว — อาจถูกลบหรือเปลี่ยนไป กรุณาโหลดหน้านี้ใหม่ | สาเหตุที่พบบ่อยสุดคือ**คนอื่นในร้านเปลี่ยนไปแล้ว** ⇒ บอกเหตุ + ทางที่ได้ผลจริง (โหลดใหม่) |
| 409 ที่จอไม่ได้จัดการ code นั้น | ทำรายการไม่สำเร็จ เพราะข้อมูลเพิ่งเปลี่ยนไป กรุณาโหลดหน้านี้ใหม่แล้วลองอีกครั้ง | เดียวกัน — "ลองใหม่" เฉย ๆ จะวนซ้ำที่เดิมจนกว่าจะโหลดใหม่ |
| ปุ่มลองใหม่ (ทุกกล่อง error) | ลองใหม่ | คำมาตรฐานตาม §1.4 |

- **ห้ามแสดง `code` / `traceId` / คำอังกฤษของระบบ** ในข้อความสำรองทุกตัว (ตาม §1.4)
- ทุกตัวข้างบนใช้ร่วม **web + mobile** — mobile map เข้า `failureMessage(t, failure)` (`core/error/api_failure.dart`)

---

## 2. S1 — เลือกร้าน (`/select-org` · mobile: route `/select-org`)

**Wireframe (web ≥lg — mobile = โครงเดียวกัน เต็มจอ ไม่มี card)**

```
┌────────────────────────────────────────────────────────────┐
│  OmniStock                                somchai@shop.com │
├────────────────────────────────────────────────────────────┤
│                                                            │
│              เลือกร้านที่จะเข้าใช้งาน                          │
│              คุณเป็นสมาชิกอยู่ 2 ร้าน                          │
│   ┌──────────────────────────────────────────────────────┐ │
│   │ [ห] ร้านหอมกรุ่นเบเกอรี่                  เจ้าของร้าน  › │ │
│   ├──────────────────────────────────────────────────────┤ │
│   │ [ค] คลังของฝากเชียงใหม่                     พนักงาน  › │ │
│   └──────────────────────────────────────────────────────┘ │
│                    [ + สร้างร้านใหม่ ]                        │
└────────────────────────────────────────────────────────────┘
```

**องค์ประกอบ**
- หัวข้อ + จำนวนร้าน · แถวร้าน = ตัวย่อชื่อร้าน (วงกลม) + ชื่อร้าน + **สิทธิ์ของฉันในร้านนั้น** (`membership.roleName` → คำไทย)
- ปุ่มรอง "สร้างร้านใหม่" (ล่างสุด — ไม่ใช่ปุ่มหลัก เพราะเคสหลักคือเลือกร้านที่มีอยู่)
- ถ้ามี `nextCursor` → ปุ่ม "โหลดเพิ่ม" ท้ายรายการ (cap 50 ร้าน/คน จึงเกิดยาก)
- **ไม่แสดงแพ็กเกจ/tier** ในเวอร์ชันนี้ (`tierLabel` เป็นคำอังกฤษของระบบ — การแสดงผล tier เป็นของ F-007)
- **ไม่ทำรายการ "ร้านที่เคยสังกัด"** (`?status=all`) ใน F-002 — ไม่มี AC รองรับ และเพิ่มความสับสนโดยไม่มีประโยชน์

**Copy (ไทย)**

| องค์ประกอบ | ข้อความ |
|---|---|
| หัวข้อ | เลือกร้านที่จะเข้าใช้งาน |
| คำอธิบายรอง | คุณเป็นสมาชิกอยู่ {n} ร้าน |
| ป้ายสิทธิ์ | เจ้าของร้าน / ผู้ดูแล / พนักงาน |
| ปุ่มรอง | สร้างร้านใหม่ *(ไอคอน plus มาจาก component — ห้ามฝัง "+" ไว้ใน i18n string, [ui.md §2.7](ui.md))* |
| ปุ่มโหลดเพิ่ม | โหลดเพิ่ม |

**States**

| state | หน้าตา | Copy |
|---|---|---|
| loading | skeleton 3 แถวตามความสูงแถวจริง | — |
| empty (ไม่ได้อยู่ร้านไหนเลย) | `EmptyState` + ปุ่มหลัก | หัวข้อ "คุณยังไม่ได้อยู่ในร้านไหน" · เนื้อ "สร้างร้านของคุณเอง หรือถ้ามีคนชวนคุณเข้าร้าน ให้ขอลิงก์คำเชิญจากเจ้าของร้าน" · ปุ่มหลัก "สร้างร้านใหม่" |
| error (เครือข่าย/5xx) | `ErrorBanner` + ปุ่ม | "โหลดรายการร้านไม่สำเร็จ" + ปุ่ม "ลองใหม่" |
| data | รายการร้าน | — |
| มาจากการถูกถอด (S12ก) | แถบเตือนสีเหลือง เหนือรายการ | "คุณไม่ได้เป็นสมาชิกของร้าน "{ชื่อร้าน}" แล้ว — เลือกร้านอื่นด้านล่าง หรือติดต่อเจ้าของร้านถ้าคิดว่าไม่ถูกต้อง" |

**Platform note** — web: การ์ดกึ่งกลาง กว้างสุด 480px · mobile: เต็มจอ, แถวสูงอย่างน้อย 56px (แตะง่าย), ไม่มีปุ่ม back (เป็น root ของ flow นี้)

---

## 3. S2 — สร้างร้านใหม่ (`/orgs/new` · mobile: full-screen)

**Wireframe**

```
┌──────────────────────────────────────────────┐
│ ‹ ย้อนกลับ            สร้างร้านใหม่            │
├──────────────────────────────────────────────┤
│  ชื่อร้าน                                     │
│  ┌────────────────────────────────────────┐  │
│  │ ร้านหอมกรุ่นเบเกอรี่                     │  │
│  └────────────────────────────────────────┘  │
│  ตั้งชื่อที่คุณเรียกร้านตัวเอง เปลี่ยนทีหลังได้      │
│                                              │
│  ℹ เมื่อสร้างเสร็จ คุณจะเป็นเจ้าของร้านนี้         │
│    และระบบจะเตรียม "คลังหลัก" ให้ 1 คลัง        │
│    ใช้สกุลเงินบาท (฿) เวลาไทย                  │
│                                              │
│              [      สร้างร้าน      ]          │
└──────────────────────────────────────────────┘
```

**องค์ประกอบ** — ฟิลด์เดียว (`name`, 1–120 ตัวอักษร) · ข้อความอธิบายสิ่งที่จะเกิด · ปุ่มหลัก
**เจตนา:** ไม่ถามเขตเวลา/สกุลเงิน/แพ็กเกจ (server เป็นคนผูก plan — ไม่มี field ให้เลือกอยู่แล้ว) = default ฉลาด ลดการตัดสินใจ

**Copy**

| องค์ประกอบ | ข้อความ |
|---|---|
| หัวข้อ | สร้างร้านใหม่ |
| label | ชื่อร้าน |
| placeholder | เช่น ร้านหอมกรุ่นเบเกอรี่ |
| helper | ตั้งชื่อที่คุณเรียกร้านตัวเอง เปลี่ยนทีหลังได้ |
| กล่องอธิบาย | เมื่อสร้างเสร็จ คุณจะเป็นเจ้าของร้านนี้ และระบบจะเตรียม "คลังหลัก" ให้ 1 คลัง · ใช้สกุลเงินบาท (฿) และเวลาประเทศไทย |
| ปุ่มหลัก | สร้างร้าน |
| ปุ่มหลัก (loading) | กำลังสร้างร้าน... |

**States / error mapping**

| สถานการณ์ | UI | Copy |
|---|---|---|
| loading (ยังไม่มีอะไรโหลด) | — (ฟอร์มล้วน ไม่มี fetch ตอนเข้า) | — |
| กำลังส่ง | ปุ่ม loading + **disabled** (กันสร้างซ้ำ — ไม่มี Idempotency-Key) | "กำลังสร้างร้าน..." |
| ชื่อว่าง/ยาวเกิน (`422 VALIDATION_FAILED` + `fieldErrors.name`) | inline ใต้ฟิลด์ + focus กลับ | "กรอกชื่อร้าน (ไม่เกิน 120 ตัวอักษร)" |
| `409 ORG_LIMIT_REACHED` (`details.limit`) | `ErrorBanner` เหนือฟอร์ม | "คุณสร้างร้านครบ {limit} ร้านแล้ว หากต้องการเพิ่ม กรุณาติดต่อทีมงาน OmniStock" |
| `503 ORG_PROVISIONING_UNAVAILABLE` | `ErrorBanner` + ปุ่ม "ลองใหม่" | "ตอนนี้ระบบยังเปิดร้านใหม่ให้ไม่ได้ (ไม่ใช่ความผิดของคุณ) ลองใหม่อีกครั้ง หรือติดต่อทีมงาน OmniStock" |
| `429 RATE_LIMITED` | `ThrottleBanner` + ฟอร์ม disabled จนนับครบ | "สร้างร้านถี่เกินไป กรุณารอสักครู่แล้วลองใหม่ · เหลือ {mm}:{ss}" |
| network/5xx | `ErrorBanner` + "ลองใหม่" | "สร้างร้านไม่สำเร็จ กรุณาลองใหม่อีกครั้ง" |
| สำเร็จ (201) | **พาเข้าร้านใหม่ทันที** → `/o/{orgId}` (S4) + toast | "สร้างร้าน "{ชื่อร้าน}" เรียบร้อย" |

> **ตอบ Q5:** response 201 มี `organization` + `membership` + `entitlement` + `defaultWarehouse` ครบพอสำหรับตั้ง active org
> โดยไม่ยิง `GET /me/organizations` ซ้ำ — client เพียง seed cache ของร้านนี้ + **invalidate** รายการร้าน (ให้ตัวสลับร้านถูกต้องรอบหน้า)

**Platform note** — web: การ์ดกึ่งกลาง 480px, ปุ่ม "ย้อนกลับ" กลับไป S1 · mobile: full-screen + AppBar มีปุ่ม back (design-system §7), ปุ่มหลักเต็มความกว้าง

---

## 4. S3 — โครงหน้าจอของร้าน + ตัวสลับร้าน (`/o/{orgId}/**`)

**Wireframe — web ≥lg (sidebar ถาวร)**

```
┌────────────┬───────────────────────────────────────────────┐
│ OmniStock  │                                               │
│ ┌────────┐ │   (เนื้อหาของหน้านั้น)                          │
│ │[ห] ร้าน│ │                                               │
│ │หอมกรุ่น▾│ │                                               │
│ └────────┘ │                                               │
│  ข้อมูลร้าน  │                                               │
│  สมาชิก ●   │  ● = แสดงเฉพาะผู้มีสิทธิ์จัดการทีมงาน            │
│  ความปลอดภัย │                                               │
│            │                                               │
│  ออกจากระบบ │                                               │
└────────────┴───────────────────────────────────────────────┘

กดที่กล่องร้าน → ▾
        ┌────────────────────────────────┐
        │ ✓ ร้านหอมกรุ่นเบเกอรี่  เจ้าของร้าน │
        │   คลังของฝากเชียงใหม่     พนักงาน │
        ├────────────────────────────────┤
        │ + สร้างร้านใหม่                  │
        └────────────────────────────────┘
```

**Wireframe — mobile (ไม่มี sidebar: ชื่อร้านอยู่บน AppBar, แตะแล้วเปิด bottom sheet)**

```
┌──────────────────────────────┐      แตะชื่อร้าน ▾
│ ร้านหอมกรุ่นเบเกอรี่ ▾     ☰ │  →  ┌────────────────────────────┐
├──────────────────────────────┤     │ สลับร้าน                    │
│ (เนื้อหาของหน้านั้น)            │     │ ✓ ร้านหอมกรุ่น    เจ้าของร้าน │
│                              │     │   คลังของฝาก…      พนักงาน │
│                              │     │ ────────────────────────── │
│                              │     │ + สร้างร้านใหม่              │
└──────────────────────────────┘     └────────────────────────────┘
```

**พฤติกรรม**
- **web: สลับร้าน = เปลี่ยน URL** (`/o/org_1/...` → `/o/org_2/...`) ตาม web.md §3.2 — เปิด 2 แท็บคนละร้านได้ · **mobile: สลับร้าน = เขียน `activeOrgId` ตัวเดียว** (mobile.md §3.2) แล้วกลับไปหน้าแรกของร้านใหม่
- ตัวสลับร้านใช้ข้อมูลชุดเดียวกับ S1 (`GET /me/organizations`) · แถวปัจจุบันมีเครื่องหมาย ✓ + อ่านออกด้วยข้อความ ไม่ใช่สีอย่างเดียว
- **org context ต้องเห็นตลอดเวลา** — ชื่อร้านปรากฏบนทุกจอ (กฎ "org context ชัดเสมอ" ของ ux-heuristic)
- เมนู "สมาชิก" **ซ่อน** เมื่อ `capabilities` ไม่มี `manage_members` (ตอบ Q13) — ไม่ disable ทิ้งไว้ให้เกิดคำถาม
- tablet (md): sidebar ยุบเป็น top bar + drawer (design-system §8.2) — ตัวสลับร้านอยู่บนสุดของ drawer

**Wireframe — web <lg (top bar + drawer ที่ "ลอยทับ" หน้า ไม่ใช่ดันหน้าลง)**

```
ปิดอยู่ (ค่าตั้งต้น)                    กด ☰ → drawer เลื่อนออกมาทับ
┌──────────────────────────────┐      ┌───────────────┬──────────────┐
│ ☰  OmniStock                 │      │ OmniStock   ✕ │▓▓▓▓▓▓▓▓▓▓▓▓▓▓│
├──────────────────────────────┤      │ ┌───────────┐ │▓▓ scrim ▓▓▓▓▓│
│ (เนื้อหาของหน้านั้น)            │  →   │ │[ห] ร้าน ▾ │ │▓▓ (กดเพื่อ ▓│
│                              │      │ └───────────┘ │▓▓  ปิด)   ▓▓│
│                              │      │  ข้อมูลร้าน     │▓▓▓▓▓▓▓▓▓▓▓▓▓▓│
│                              │      │  สมาชิก ●      │▓▓▓▓▓▓▓▓▓▓▓▓▓▓│
│                              │      │  ความปลอดภัย   │▓▓▓▓▓▓▓▓▓▓▓▓▓▓│
│                              │      │  ─────────    │▓▓▓▓▓▓▓▓▓▓▓▓▓▓│
│                              │      │  ออกจากระบบ    │▓▓▓▓▓▓▓▓▓▓▓▓▓▓│
└──────────────────────────────┘      └───────────────┴──────────────┘
    ↑ top bar สูง 56px                  ↑ `size.drawer.w` (280px)
      ปุ่ม ☰ = `icon.menu` tap 44px       สูงสุด `size.drawer.max-w` (85%)
```

- **drawer ต้องลอยทับ (overlay) ไม่ใช่แทรกในสายเนื้อหา** — ถ้าดันหน้าลง มันคือ "ส่วนหนึ่งของหน้า"
  ไม่ใช่ "ชั้นที่ปิดได้" ผู้ใช้จะไม่รู้ว่ากดตรงไหนให้หายไป · ต้องเหลือ scrim ให้เห็นหน้าเดิมข้างหลัง
  (จึงมีเพดาน 85% — drawer ที่เต็มจอพอดีจะดูเหมือน "เปลี่ยนหน้า")
- ปิดได้ 3 ทาง: ปุ่ม ✕ · กด scrim · `Escape` · เมื่อเลือกเมนูแล้วต้องปิดเอง (ไม่งั้นมันบังหน้าที่เพิ่งขอ)

**Copy**

| องค์ประกอบ | ข้อความ | หมายเหตุ |
|---|---|---|
| ป้ายเมนู (landmark) | เมนูของร้าน | `aria-label` ของ `<nav>` ทั้ง drawer และ sidebar |
| ปุ่ม ☰ (icon-button) | เปิดเมนู | **`aria-label`** — screen reader อ่านชื่อนี้เดี่ยว ๆ จึงใช้กริยานำ ("เปิดเมนู") ไม่ใช่คำนาม ("เมนู") |
| ปุ่ม ✕ ใน drawer | ปิดเมนู | `aria-label` เช่นกัน |
| แถวเมนู | ข้อมูลร้าน · สมาชิก · ความปลอดภัย | "สมาชิก" ซ่อนเมื่อไม่มี `manage_members` |
| แถวสุดท้าย | ออกจากระบบ | จบ **เฉพาะ session นี้** — คนละอย่างกับ "ออกจากระบบทุกอุปกรณ์" ที่จอความปลอดภัย |
| ออกจากระบบล้มเหลว | ออกจากระบบไม่สำเร็จ ลองใหม่อีกครั้ง | toast · **อยู่ที่เดิม ห้ามพาไป `/login`** — session ยังอยู่จริง การพาไปหน้า login คือการโชว์การออกจากระบบที่ไม่ได้เกิดขึ้น |

> **ที่มา (2026-09-05):** ทั้งสี่บรรทัดล่างเป็นของที่ **ไม่เคยมีใน wireframe** เพราะ drawer และแถว
> "ออกจากระบบ" ไม่เคยถูกสร้าง (B-16) · frontend เขียนไว้ก่อนแล้วมาร์ก COPY GAP — ux ทวนแล้ว **ยืนยันทุกคำ**
> และย้ายมาบันทึกที่นี่ เพื่อไม่ให้ drawer ตัวถัดไปต้องเดาใหม่

**States**

| state | Copy / หน้าตา |
|---|---|
| loading (bootstrap org context) | โครง shell + skeleton ชื่อร้าน + skeleton เนื้อหา |
| error (โหลด `GET /orgs/{orgId}` ไม่ได้ — network) | เต็มพื้นที่เนื้อหา: "เปิดข้อมูลร้านไม่สำเร็จ" + ปุ่ม "ลองใหม่" (ยังอยู่ในร้านเดิม ไม่เตะออก) |
| `403 ORG_ACCESS_DENIED` | → S12ก (พากลับ S1 + แถบแจ้ง) |
| รายการร้านในตัวสลับโหลดไม่ได้ | ใน dropdown/sheet: "โหลดรายการร้านไม่สำเร็จ" + "ลองใหม่" (ไม่ทำให้ทั้งจอพัง) |

---

## 5. S4 — ข้อมูลร้าน / หน้าแรกของร้าน (`/o/{orgId}` → `settings/org`)

> Phase 0 ยังไม่มี dashboard (F-030) — จอนี้ทำหน้าที่เป็นหน้าแรกของร้านไปก่อน

**Wireframe — ผู้มีสิทธิ์จัดการข้อมูลร้าน (Owner/ผู้ดูแล)**

```
┌──────────────────────────────────────────────────────────┐
│ ข้อมูลร้าน                                                │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ เริ่มต้นใช้งาน                                          │ │
│ │  ○ ชวนทีมงานเข้าร้าน            [ เชิญสมาชิก ]          │ │
│ │  ○ ประกาศข้อมูลผู้เสียภาษี        [ กรอกข้อมูล ]         │ │
│ │  ○ ตั้งเจ้าของร้านสำรองอีก 1 คน   [ เชิญเจ้าของร้าน ]     │ │
│ └──────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ ชื่อร้าน            ร้านหอมกรุ่นเบเกอรี่        [ แก้ไข ] │ │
│ │ เวลา/สกุลเงิน       เวลาไทย · บาท (฿)                  │ │
│ │ ทีมงาน             สมาชิก 4 คน · คำเชิญค้าง 1 ใบ  ›     │ │
│ └──────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ ข้อมูลผู้เสียภาษี                            [ แก้ไข ] │ │
│ │ ประเภท        นิติบุคคล                                │ │
│ │ เลขผู้เสียภาษี  •••••••••4567      [ แสดงเลขเต็ม ]      │ │
│ │ VAT           จดทะเบียน VAT                           │ │
│ │ รหัสสาขา       00000 (สำนักงานใหญ่)                     │ │
│ └──────────────────────────────────────────────────────┘ │
│                                          ออกจากร้านนี้     │
└──────────────────────────────────────────────────────────┘
```

**Wireframe — พนักงาน (ไม่มี `manage_org_settings`, ไม่มี `manage_members`)**

```
┌──────────────────────────────────────────────────────────┐
│ ข้อมูลร้าน                                                │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ ชื่อร้าน            ร้านหอมกรุ่นเบเกอรี่                  │ │
│ │ เวลา/สกุลเงิน       เวลาไทย · บาท (฿)                  │ │
│ │ ทีมงาน             สมาชิก 4 คน                        │ │
│ └──────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ ข้อมูลผู้เสียภาษี                                      │ │
│ │ ✓ ร้านนี้ประกาศข้อมูลผู้เสียภาษีแล้ว · จดทะเบียน VAT      │ │
│ │ (รายละเอียดเปิดให้เฉพาะผู้ที่ดูแลข้อมูลร้าน)               │ │
│ └──────────────────────────────────────────────────────┘ │
│                                          ออกจากร้านนี้     │
└──────────────────────────────────────────────────────────┘
```

> **"ออกจากร้านนี้" อยู่ท้ายจอนี้ ไม่ใช่จอสมาชิก** (D-029 · `DELETE /orgs/{orgId}/membership`) —
> เพราะ **สมาชิก active ทุกคนทำได้** แต่จอสมาชิกเปิดให้เฉพาะผู้มี `manage_members` ⇒ พนักงานต้องหาเจอด้วย
> เป็นลิงก์ข้อความ (ไม่ใช่ปุ่มเด่น) มุมล่าง ห่างจากปุ่มอื่น · กดแล้วเปิด confirm §10.3

**กติกาการแสดงผล (ตอบ Q13 + PDPA/D-028 + `reveal` ตาม api-spec §3.16)**
- ผู้มี `manage_org_settings`: เห็น `taxIdMasked` **เป็นค่าตั้งต้น** + ปุ่ม **"แสดงเลขเต็ม"**
  - **การกดปุ่มนี้ = การกระทำที่ตั้งใจ** → ยิง `POST /orgs/{orgId}/tax-profile/reveal` **ทุกครั้ง**
    (เลขเต็มไม่ติดมากับ `GET /orgs/{orgId}` อีกแล้ว) · ระหว่างรอ ปุ่มเป็น loading "กำลังขอเลข..." + disabled
  - **กดซ้ำ = "ซ่อนเลข" → ทิ้งค่าทิ้งจริง** (memory เท่านั้น ห้าม persist/log) · จะดูอีกต้องขอใหม่ (นับ rate limit ใหม่)
  - ใต้ปุ่มมีบรรทัดเล็กความจริง: **"การกดดูเลขเต็มถูกบันทึกไว้เพื่อความปลอดภัยของร้าน"** (ระบบ emit event จริง — บอกก่อนกด ไม่ใช่แอบเก็บ)
  - **จำกัด 20 ครั้ง/ชม.** → `429` มี copy + ทางไปต่อที่ไม่ตัน (ตาราง States ด้านล่าง)
  - **เลขที่เปิดอยู่ต้องซ่อนตัวเองเมื่อ "จอไม่ได้อยู่กับเราแล้ว"** (ตัดสิน 2026-09-05 — รายละเอียด/เหตุผลที่ **§15**)
- **ไม่มีสิทธิ์: ไม่แสดงตัวเลขใด ๆ แม้แต่ 4 ตัวท้าย** — แสดงแค่ "ประกาศแล้ว/ยังไม่ประกาศ" + จดทะเบียน VAT ไหม (ข้อมูลนี้จำเป็นกับงานหน้าร้านจริง) · client **ต้องไม่พึ่ง** การมี/ไม่มี field `taxId` เพื่อตัดสินว่าประกาศแล้วหรือยัง → ใช้ `taxProfileComplete`
- การ์ด "เริ่มต้นใช้งาน" แสดงเฉพาะผู้มีสิทธิ์ และแสดงเฉพาะข้อที่ยังไม่ทำ (`counts.activeMembers === 1` / `taxProfileComplete === false`) — ทำครบแล้วการ์ดหายไปเอง
- **ข้อ "ตั้งเจ้าของร้านสำรองอีก 1 คน" (D-030)** แสดงเมื่อ **`myMembership.capabilities` มี `full_access` **และ** `counts.activeMembers === 1`**
  (= ฉันเป็นเจ้าของร้าน และในร้านมีฉันคนเดียว ⇒ อนุมานได้แน่นอนว่าเจ้าของร้านมีคนเดียว **โดยไม่ต้องขอ field ใหม่**)
  · กดปุ่ม → เปิด S7 พร้อม**เลือกสิทธิ์ "เจ้าของร้าน" ไว้ให้ล่วงหน้า** · ข้อนี้ **ปิดเองไม่ได้** แต่หายทันทีที่มีสมาชิกคนที่สอง
  · เหตุผลที่ไม่ประดิษฐ์การนับเจ้าของร้านที่จอนี้: `GET /orgs/{orgId}` ไม่มีจำนวน Owner — เคสที่มีสมาชิกหลายคนไปเตือนที่ S6 แทน (§7 ท้ายหัวข้อ)
- แถว "ทีมงาน" กดเข้าจอสมาชิกได้เฉพาะผู้มี `manage_members` (คนอื่นเห็นเป็นข้อความเฉย ๆ — `counts` เป็นตัวเลขรวม ไม่ใช่รายชื่อ จึงไม่ขัด PDPA)

**Copy**

| องค์ประกอบ | ข้อความ |
|---|---|
| หัวข้อหน้า | ข้อมูลร้าน |
| การ์ดเริ่มต้น (หัวข้อ) | เริ่มต้นใช้งาน |
| ข้อ 1 | ชวนทีมงานเข้าร้าน — ปุ่ม "เชิญสมาชิก" |
| ข้อ 2 | ประกาศข้อมูลผู้เสียภาษี เพื่อออกใบกำกับภาษีได้ — ปุ่ม "กรอกข้อมูล" |
| ข้อ 3 (D-030) | ตั้งเจ้าของร้านสำรองไว้อีก 1 คน เผื่อวันหนึ่งคุณเข้าระบบไม่ได้ — ปุ่ม "เชิญเจ้าของร้าน" |
| label ชื่อร้าน / เวลา-สกุลเงิน / ทีมงาน | ชื่อร้าน · เวลา/สกุลเงิน · ทีมงาน |
| ค่า ทีมงาน | สมาชิก {n} คน · คำเชิญค้าง {m} ใบ |
| หัวข้อบล็อกภาษี | ข้อมูลผู้เสียภาษี |
| ยังไม่ประกาศ (มีสิทธิ์) | "ยังไม่ได้ประกาศข้อมูลผู้เสียภาษี — ตอนนี้ร้านนี้ยังออกใบกำกับภาษีไม่ได้" + ปุ่ม "กรอกข้อมูลผู้เสียภาษี" |
| ยังไม่ประกาศ (ไม่มีสิทธิ์) | "ร้านนี้ยังไม่ได้ประกาศข้อมูลผู้เสียภาษี" |
| ประกาศแล้ว (ไม่มีสิทธิ์) | "ร้านนี้ประกาศข้อมูลผู้เสียภาษีแล้ว · จดทะเบียน VAT" / "…แล้ว · ไม่ได้จดทะเบียน VAT" + บรรทัดรอง "รายละเอียดเปิดให้เฉพาะผู้ที่ดูแลข้อมูลร้าน" |
| ปุ่มเผยเลข | แสดงเลขเต็ม / ซ่อนเลข · (loading) กำลังขอเลข... |
| บรรทัดใต้ปุ่มเผยเลข | การกดดูเลขเต็มถูกบันทึกไว้เพื่อความปลอดภัยของร้าน |
| ลิงก์ท้ายจอ | ออกจากร้านนี้ |

**แก้ไขชื่อร้าน (`PATCH /orgs/{orgId}`)** — inline dialog ฟิลด์เดียว: หัวข้อ "แก้ไขชื่อร้าน" · ปุ่ม "บันทึก"/"ยกเลิก" · สำเร็จ → toast "บันทึกชื่อร้านแล้ว" · ผิดพลาด 422 → inline "กรอกชื่อร้าน (ไม่เกิน 120 ตัวอักษร)"
> **โลโก้ร้าน: ไม่มีในเวอร์ชันนี้** — contract รับได้เฉพาะ `null` จนกว่าจะมี F-040 ⇒ **ไม่ออกแบบปุ่มอัปโหลดโลโก้** (แสดงตัวย่อชื่อร้านแทน avatar ทุกที่)

**States**

| state | Copy |
|---|---|
| loading | skeleton การ์ด 2–3 ก้อนตาม layout |
| error | "เปิดข้อมูลร้านไม่สำเร็จ" + "ลองใหม่" |
| `403 FORBIDDEN` ตอนกดบันทึก | toast "คุณไม่มีสิทธิ์แก้ไขข้อมูลร้าน ติดต่อเจ้าของร้านให้เปิดสิทธิ์ให้" (อยู่หน้าเดิม) |
| empty | ไม่มี (จอนี้มีข้อมูลเสมอ) |
| **เผยเลข — `429 RATE_LIMITED`** | `ThrottleBanner` ในการ์ดภาษี (ไม่ล็อกทั้งจอ): "ขอดูเลขเต็มบ่อยเกินไป กรุณารอสักครู่แล้วลองใหม่ · เหลือ {mm}:{ss}" + บรรทัดรอง **ทางไปต่อ**: "ระหว่างนี้ยังแก้ไขข้อมูลอื่นของร้านได้ตามปกติ" (ปุ่ม "แก้ไข" ยังกดได้ — ดู §6 กรณีฟอร์มขอเลขเดิมไม่ได้) |
| **เผยเลข — `404 NOT_FOUND`** (ถูกลบ/ยังไม่ประกาศ) | ปิดค่าที่แสดงอยู่ + refetch การ์ด + toast "ร้านนี้ยังไม่ได้ประกาศข้อมูลผู้เสียภาษี" |
| **เผยเลข — `403 FORBIDDEN`** | toast มาตรฐาน §12.2 + ซ่อนปุ่ม (refetch สิทธิ์) |
| **เผยเลข — network/5xx** | helper สีแดงใต้ปุ่ม: "ขอดูเลขเต็มไม่สำเร็จ กรุณาลองใหม่อีกครั้ง" (เลขที่แสดงอยู่เดิมไม่หาย ถ้ายังไม่ได้กดซ่อน) |

---

## 6. S5 — ข้อมูลผู้เสียภาษี (dialog บน web · full-screen บน mobile)

**Wireframe**

```
┌ ข้อมูลผู้เสียภาษี ────────────────────────────────────┐
│ ประเภทผู้เสียภาษี                                     │
│   ( ) บุคคลธรรมดา        (•) นิติบุคคล                 │
│                                                     │
│ เลขประจำตัวผู้เสียภาษี (13 หลัก)                        │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 0105551234567                                   │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ จดทะเบียน VAT                                        │
│   (•) จดทะเบียน VAT     ( ) ไม่ได้จดทะเบียน           │
│                                                     │
│ รหัสสาขา (ถ้ามี)                                      │
│ ┌───────────┐  เว้นว่าง = สำนักงานใหญ่ (00000)         │
│ │ 00000     │                                       │
│ └───────────┘                                       │
│                                                     │
│ 🔒 ข้อมูลนี้เปิดให้เห็นเฉพาะผู้ที่ดูแลข้อมูลร้าน           │
│                          [ ยกเลิก ]  [ บันทึก ]      │
└─────────────────────────────────────────────────────┘
```

**กติกา (ตอบ Q7)**
- **มีโหมดแก้ไข** — จอ S4 = โหมดดู, จอนี้ = โหมดกรอก/แก้ (API เป็น `PUT` ทั้งชุด แก้ซ้ำได้)
- กรอกครบทั้งชุดเสมอ (ไม่มีการกรอกครึ่ง ๆ) — ฟอร์มโหลดค่าเดิมมาให้ครบเมื่อเคยประกาศแล้ว
- **เลขเดิมในฟอร์มมาจาก `POST …/tax-profile/reveal` เท่านั้น** (api-spec §3.16) → เปิดฟอร์มแบบ "แก้ไข" = ขอ 1 ครั้ง
  (นับใน 20 ครั้ง/ชม. ร่วมกับปุ่ม "แสดงเลขเต็ม" ของ S4) · ค่าอยู่ใน memory ของฟอร์ม ปิดฟอร์ม = ทิ้งค่า
- **ถ้าขอเลขเดิมไม่ได้ (429/เครือข่าย) — ต้องไม่ตัน:** เปิดฟอร์มต่อได้ โดยช่องเลขว่างไว้ + helper
  "ตอนนี้ระบบยังไม่ให้ดูเลขเดิม — ถ้าต้องการบันทึกข้อมูลชุดนี้ ให้พิมพ์เลขผู้เสียภาษี 13 หลักใหม่อีกครั้ง"
  (`PUT` เป็นทั้งชุด ⇒ ต้องมีเลขเสมอ — พูดตรง ๆ ดีกว่าปล่อยให้กดบันทึกแล้ว error)
- เลือก **บุคคลธรรมดา** → **ซ่อนช่อง "รหัสสาขา"** (ไม่มีความหมาย) + แสดงหมายเหตุความเป็นส่วนตัว
- **แก้ทับของเดิม** → confirm ก่อนบันทึก: "เปลี่ยนข้อมูลผู้เสียภาษีของร้านนี้? ร้าน 1 ร้านมีข้อมูลผู้เสียภาษีได้ชุดเดียว ข้อมูลใหม่จะแทนที่ของเดิม" [ยกเลิก] [บันทึกทับ]

**Copy**

| องค์ประกอบ | ข้อความ |
|---|---|
| หัวข้อ | ข้อมูลผู้เสียภาษี |
| label ประเภท | ประเภทผู้เสียภาษี |
| ตัวเลือก | บุคคลธรรมดา · นิติบุคคล |
| helper (บุคคลธรรมดา) | สำหรับบุคคลธรรมดา เลข 13 หลักนี้คือเลขบัตรประชาชนของเจ้าของกิจการ — ระบบเปิดให้เห็นเฉพาะผู้ที่ดูแลข้อมูลร้าน |
| label เลข | เลขประจำตัวผู้เสียภาษี (13 หลัก) |
| label VAT | จดทะเบียน VAT |
| ตัวเลือก VAT | จดทะเบียน VAT · ไม่ได้จดทะเบียน |
| label สาขา | รหัสสาขา (ถ้ามี) |
| helper สาขา | เว้นว่าง = สำนักงานใหญ่ (00000) |
| ปุ่ม | บันทึก / ยกเลิก |
| ปุ่ม (loading) | กำลังบันทึก... |
| success toast | บันทึกข้อมูลผู้เสียภาษีแล้ว — ร้านนี้ออกใบกำกับภาษีได้แล้ว |

**Error mapping**

| error | UI + Copy |
|---|---|
| `422 TAX_ID_INVALID` (+`fieldErrors.taxId`) | inline ใต้ฟิลด์ + focus | "เลขผู้เสียภาษีไม่ถูกต้อง — ต้องเป็นตัวเลข 13 หลัก และตรวจสอบเลขหลักสุดท้ายอีกครั้ง" |
| `422 VALIDATION_FAILED` (branchCode) | inline | "รหัสสาขาต้องเป็นตัวเลข 5 หลัก" |
| `403 FORBIDDEN` | toast + ปิดฟอร์ม | "คุณไม่มีสิทธิ์แก้ไขข้อมูลร้าน ติดต่อเจ้าของร้านให้เปิดสิทธิ์ให้" |
| `429` ตอนขอเลขเดิม (reveal) | ช่องเลขว่าง + helper (ฟอร์มยังใช้ได้) | "ตอนนี้ระบบยังไม่ให้ดูเลขเดิม — ถ้าต้องการบันทึกข้อมูลชุดนี้ ให้พิมพ์เลขผู้เสียภาษี 13 หลักใหม่อีกครั้ง" |
| network/5xx | `ErrorBanner` ในฟอร์ม + "ลองใหม่" | "บันทึกไม่สำเร็จ กรุณาลองใหม่อีกครั้ง" |

**a11y / ความเป็นส่วนตัว** — ฟิลด์เลขใช้แป้นตัวเลข (mobile `keyboardType: number`) · ไม่ autofill · หน้าจอนี้บนมือถือควรกัน screenshot ตาม pattern FLAG_SECURE ที่ F-001 วางไว้ (ข้อเสนอ ไม่ใช่ AC — ผูกกับ `frontend` ตัดสินความเป็นไปได้)

---

## 7. S6 — สมาชิก (`/o/{orgId}/settings/members`)

> **ตอบ Q1:** **จอเดียว 2 ส่วนบนหน้าเดียวกัน** (ไม่ใช่ 2 แท็บ, ไม่ต้องมี endpoint รวม) — เหตุผลใน §12

**Wireframe — web**

```
┌──────────────────────────────────────────────────────────────────┐
│ สมาชิก                                        [ + เชิญสมาชิก ]     │
│                                                                  │
│ คำเชิญที่รอตอบรับ (1)                                              │
│ ┌──────────────────────────────────────────────────────────────┐ │
│ │ ✉ new@example.com   ผู้ดูแล   ลิงก์ใช้ได้ถึง 29 ก.ค. 2026, 14:30 │ │
│ │                              [ ออกลิงก์ใหม่ ]   [ ⋯ ]         │ │
│ └──────────────────────────────────────────────────────────────┘ │
│ ดูคำเชิญทั้งหมด รวมที่รับแล้ว                                        │
│                                                                  │
│ สมาชิกในร้าน (3)                                                  │
│ ┌──────────────────────────────────────────────────────────────┐ │
│ │ [ส] somchai@shop.com  (คุณ)   เจ้าของร้าน  ใช้งานอยู่    [ ⋯ ] │ │
│ ├──────────────────────────────────────────────────────────────┤ │
│ │ [ม] malee@shop.com            ผู้ดูแล      ใช้งานอยู่    [ ⋯ ] │ │
│ ├──────────────────────────────────────────────────────────────┤ │
│ │ [น] noi@shop.com              พนักงาน     ใช้งานอยู่    [ ⋯ ] │ │
│ └──────────────────────────────────────────────────────────────┘ │
│ แสดงสมาชิกที่ถูกถอดออกแล้ว                                          │
└──────────────────────────────────────────────────────────────────┘
```

**Wireframe — mobile (จอเดียวกัน เลื่อนลง, ปุ่มการกระทำอยู่ใน bottom sheet)**

```
┌────────────────────────────────┐        แตะแถว →
│ ‹     สมาชิก              [+]  │   ┌──────────────────────────┐
├────────────────────────────────┤   │ malee@shop.com           │
│ คำเชิญที่รอตอบรับ (1)            │   │ ผู้ดูแล · ใช้งานอยู่        │
│ ┌────────────────────────────┐ │   │ ────────────────────────  │
│ │ ✉ new@example.com          │ │   │ เปลี่ยนสิทธิ์               │
│ │ ผู้ดูแล · ใช้ได้ถึง 29 ก.ค.  │ │   │ ถอดออกจากร้าน  (สีแดง)     │
│ │ [ ออกลิงก์ใหม่ ]  [ ⋯ ]     │ │   └──────────────────────────┘
│ └────────────────────────────┘ │
│ สมาชิกในร้าน (3)                │
│ ┌────────────────────────────┐ │
│ │ [ส] somchai@shop.com (คุณ) │ │
│ │     เจ้าของร้าน · ใช้งานอยู่  │ │
│ ├────────────────────────────┤ │
│ │ [ม] malee@shop.com         │ │
│ │     ผู้ดูแล · ใช้งานอยู่      │ │
│ └────────────────────────────┘ │
└────────────────────────────────┘
```

**องค์ประกอบ + กติกา**
- **ส่วนคำเชิญอยู่บน** เพราะเป็น "งานค้างที่ต้องจัดการ" · โหลดจาก `GET …/invitations?status=pending` · **ถ้าไม่มีคำเชิญค้าง = ซ่อนทั้งส่วน** (ไม่รก)
- ปุ่มข้อความ **"ดูคำเชิญทั้งหมด รวมที่รับแล้ว"** → refetch `?status=all` แสดงในส่วนเดียวกัน + **หัวข้อส่วนเปลี่ยนตาม filter**
  เป็น "คำเชิญทั้งหมด ({n})" (หัวข้อ "รอตอบรับ" ค้างอยู่บนรายการที่มีของหมดอายุปนอยู่ = โกหกเงียบ ๆ)
  · **ปุ่มเป็น toggle 2 ทางเสมอ** — ขากลับ "ดูเฉพาะคำเชิญที่รอตอบรับ" · ปุ่มห้ามหายไปหลังกด (กดแล้วกลับไม่ได้ = ติดกับ)
- **ส่วนสมาชิก** โหลด `GET …/members?status=active` เป็นค่าตั้งต้น · ปุ่มข้อความ "แสดงสมาชิกที่ถูกถอดออกแล้ว" → refetch `status=all`
  (แถวที่ถูกถอด = สีจาง `opacity.dim` + ป้าย "ถูกถอดแล้ว" + ไม่มีปุ่มการกระทำ) — ตอบ AC "ดูรายชื่อ + สถานะ"
  · ขากลับ = **"ซ่อนสมาชิกที่ถูกถอดออกแล้ว"** (คู่ตรงข้ามแบบ แสดง ⇄ ซ่อน บอกชัดว่าอะไรกำลังจะหายไป)
- **filter ที่กว้างขึ้นแล้วไม่มีอะไรโผล่ ต้องพูดว่าไม่มี** — ห้ามให้จอเงียบหลังการกดที่ตั้งใจ (ดูตาราง States)
- แถวของฉันมีป้าย **"(คุณ)"** · เมนู `⋯` ต่อแถวมีเฉพาะรายการที่ทำได้จริง:

| target | ฉัน = เจ้าของร้าน | ฉัน = ผู้ดูแล |
|---|---|---|
| สมาชิกทั่วไป (ไม่ใช่เจ้าของ) | เปลี่ยนสิทธิ์ · ถอดออกจากร้าน | เปลี่ยนสิทธิ์ (ยกเป็นเจ้าของไม่ได้) · ถอดออกจากร้าน |
| สมาชิกที่เป็นเจ้าของร้าน | เปลี่ยนสิทธิ์ · ถอดออกจากร้าน | **ไม่มีเมนู** + ข้อความจาง "เฉพาะเจ้าของร้านเท่านั้นที่แก้สิทธิ์ของเจ้าของร้านคนอื่นได้" |
| แถวของฉันเอง | เปลี่ยนสิทธิ์ของฉัน · **ออกจากร้านนี้** | เปลี่ยนสิทธิ์ของฉัน · **ออกจากร้านนี้** |

- **ธงเตือนเบา ๆ บนคำเชิญที่รับไปแล้ว** (`acceptedUserCreatedAfterInvite === true`, เห็นได้จาก `?status=all`):
  ไอคอน ⓘ + ข้อความ "บัญชีที่กดรับถูกสร้างขึ้นหลังจากออกลิงก์ ตรวจสอบว่าเป็นคนที่คุณตั้งใจเชิญ" — **ไม่ใช้ถ้อยคำกล่าวหา** (D-028/I-7)
- **ไม่แสดง "เชิญโดยใคร"** ใน F-002 (`invitedByUserId` เป็นรหัสดิบ แปลงเป็นชื่อคนไม่ได้จาก contract ปัจจุบัน — ดู Q4)

**ปุ่ม "เชิญใหม่อีกครั้ง" บนแถวคำเชิญที่ไม่ pending** — สเปกเต็ม (ยังไม่มีของจริง ณ 2026-09-05)

| เรื่อง | กติกา |
|---|---|
| แสดงเมื่อไหร่ | เฉพาะแถวที่ `status` = **`expired` หรือ `cancelled`** เท่านั้น |
| ⛔ ห้ามแสดงเมื่อ | `status === "accepted"` — คนนั้นอยู่ในร้านแล้ว "เชิญใหม่" จึงเป็นคำที่ไม่จริง · ถ้าเขาถูกถอดออกทีหลัง ทางที่ถูกคือปุ่ม "เชิญสมาชิก" ตามปกติ |
| กดแล้วเกิดอะไร | เปิด **S7** โดยเติม `email` + สิทธิ์เดิมของแถวนั้นไว้ให้ (ยังแก้ได้ทั้งคู่ — เป็นค่าตั้งต้น ไม่ใช่ค่าตายตัว) |
| สิทธิ์เดิมที่ตั้งให้ไม่ได้แล้ว | ถ้าแถวเดิมเป็น "เจ้าของร้าน" แต่คนกดไม่ใช่เจ้าของร้าน → เปิด S7 โดย**ไม่เลือกสิทธิ์ไว้** + helper เดิม "เฉพาะเจ้าของร้านเท่านั้นที่ตั้งเจ้าของร้านคนใหม่ได้" (⛔ ห้าม pre-fill ตัวเลือกที่กดส่งไม่ผ่าน) |
| ข้อมูลอาจเก่า | ไม่ต้องเช็คล่วงหน้าและ **ไม่ขอ field ใหม่** — ถ้าเขาเข้าร้านไปแล้ว/มีคำเชิญค้างอยู่แล้ว server จะปฏิเสธ และ S7 มีคำรองรับทั้งสองเคสอยู่แล้ว ("คนนี้เป็นสมาชิกของร้านอยู่แล้ว" / "มีคำเชิญของอีเมลนี้ค้างอยู่แล้ว — ออกลิงก์ใหม่หรือยกเลิกคำเชิญเดิมก่อน") |
| หน้าตา | `Button variant="secondary" size="sm"` ใน slot ขวาของแถว (ที่เดียวกับ "ออกลิงก์ใหม่" ของแถว pending) — สูง ≥44px |

> ### ⚠️ ค้างอยู่ — ธงความปลอดภัยอยู่ในที่ที่คนเชิญไม่ได้มอง (manual pass M-04, 2026-09-05)
>
> `acceptedUserCreatedAfterInvite` ขึ้นได้เฉพาะบนแถวคำเชิญที่ **ถูกรับแล้ว** ⇒ เห็นได้ต่อเมื่อกดขยาย filter
> · เดิมปุ่มนั้นเขียนว่า "ดูคำเชิญที่หมดอายุ/ยกเลิกแล้ว" ซึ่งคนที่เพิ่งชวนเพื่อนเข้าร้าน **ไม่มีเหตุผลจะกด**
> ⇒ คำเตือนความปลอดภัยข้อเดียวของ F-002 ซ่อนอยู่หลังประตูที่ไม่มีใครเปิด
>
> - **แก้ไปแล้วเท่าที่ทำได้วันนี้ (copy ล้วน):** ป้ายปุ่มเปลี่ยนเป็น "ดูคำเชิญทั้งหมด รวมที่รับแล้ว" — บอกว่ามีอะไรอยู่ข้างใน
> - **ที่ยังแก้ไม่ได้:** ให้ธงไปโผล่ตรงที่คนเชิญมองอยู่แล้ว = **แถวของคนคนนั้นในรายชื่อสมาชิก** ·
>   contract ปัจจุบันไม่มี field นี้บน `MemberRow` และ **ux ไม่ออกแบบจอบนข้อมูลที่ยังไม่มี** ⇒ ต้องให้
>   **@backend-api** ตอบว่าเติมได้ไหม และ **@product** ตัดสินว่าอยู่ใน F-002 หรือเลื่อน
> - ⛔ ทางที่ **ไม่เอา**: เปลี่ยน default ของจอเป็น `?status=all` — ขัด "ส่วนคำเชิญ = งานค้างที่ต้องจัดการ"
>   (§7 + คำตอบ Q1) และทำให้รายการที่ต้องตามงานเจือจางด้วยของที่จบไปแล้ว

**Copy**

| องค์ประกอบ | ข้อความ |
|---|---|
| หัวข้อหน้า | สมาชิก |
| ปุ่มหลัก | เชิญสมาชิก *(ไอคอน plus มาจาก component — ห้ามฝัง "+" ไว้ใน i18n string, [ui.md §2.7](ui.md))* |
| หัวข้อส่วน 1 (filter = pending) | คำเชิญที่รอตอบรับ ({n}) |
| หัวข้อส่วน 1 (filter = all) | คำเชิญทั้งหมด ({n}) |
| แถวคำเชิญ | {email} · {สิทธิ์} · ลิงก์ใช้ได้ถึง {วันเวลา} *(บรรทัดอายุลิงก์ขึ้น **เฉพาะแถวที่ลิงก์ยังใช้ได้จริง** — B-15)* |
| ปุ่มบนแถวคำเชิญ (pending) | ออกลิงก์ใหม่ · (เมนู) ยกเลิกคำเชิญ |
| ปุ่มบนแถวคำเชิญ (หมดอายุ/ยกเลิก) | เชิญใหม่อีกครั้ง |
| ปุ่มสลับการแสดง (คำเชิญ) | **ดูคำเชิญทั้งหมด รวมที่รับแล้ว** ⇄ ดูเฉพาะคำเชิญที่รอตอบรับ |
| ป้ายสถานะคำเชิญ | รอตอบรับ · หมดอายุแล้ว · ยกเลิกแล้ว · รับแล้วเมื่อ {วันเวลา} |
| หัวข้อส่วน 2 (filter = active) | สมาชิกในร้าน ({n}) |
| หัวข้อส่วน 2 (filter = all) | สมาชิกทั้งหมด ({n}) |
| ป้ายสถานะสมาชิก | ใช้งานอยู่ · ถูกถอดแล้ว |
| ปุ่มสลับการแสดง (สมาชิก) | แสดงสมาชิกที่ถูกถอดออกแล้ว ⇄ ซ่อนสมาชิกที่ถูกถอดออกแล้ว |
| เมนูแถว | เปลี่ยนสิทธิ์ · ถอดออกจากร้าน · ออกจากร้านนี้ |

> **ป้ายปุ่มสลับ = สัญญาว่าอะไรจะโผล่ ไม่ใช่ชื่อของ query** — ปุ่มที่บอกไม่ครบว่าเปิดอะไรออกมา
> ทำให้คนไม่กด และของที่อยู่ข้างในก็เท่ากับไม่มี (M-04) · ⛔ ห้ามใช้คำว่า "filter/all/status" บนจอ

**States**

| state | UI + Copy |
|---|---|
| loading | skeleton: 1 แถวคำเชิญ + 3 แถวสมาชิก (ตาม layout จริง) |
| empty — ยังไม่มีคำเชิญค้าง | ซ่อนส่วนคำเชิญทั้งส่วน |
| empty — กด "ดูคำเชิญทั้งหมด" แล้วไม่มีอะไรเลย | ข้อความ `body.sm` สี `text.muted` ข้างปุ่ม: **"ร้านนี้ยังไม่เคยมีคำเชิญ"** *(filter นี้รวมของที่ pending ด้วย ⇒ ว่าง = ไม่เคยมีเลยจริง ๆ)* |
| empty — กด "แสดงสมาชิกที่ถูกถอดออกแล้ว" แล้วไม่มี | ข้อความข้างปุ่ม: **"ไม่มีสมาชิกที่ถูกถอดออก"** |
| empty — มีแค่ตัวเอง | ใต้รายชื่อ: `EmptyState` เล็ก "ตอนนี้มีคุณอยู่คนเดียวในร้านนี้ — ชวนทีมงานเข้ามาช่วยกันได้เลย" + ปุ่ม "เชิญสมาชิก" |
| error (ส่วนใดส่วนหนึ่งโหลดไม่ได้) | แสดง error เฉพาะส่วนนั้น + "ลองใหม่" (อีกส่วนยังใช้งานได้) — "โหลดรายชื่อสมาชิกไม่สำเร็จ" / "โหลดคำเชิญไม่สำเร็จ" |
| ไม่มีสิทธิ์ (เข้าตรงผ่าน URL) | `ForbiddenPanel` เต็มพื้นที่ (§11) |
| มีข้อมูลเยอะ | ปุ่ม "โหลดเพิ่ม" ท้ายแต่ละส่วน (cursor) |

### แถบชวนตั้งเจ้าของร้านสำรอง — เมื่อร้านมีเจ้าของร้านคนเดียว (D-030)

**Wireframe (อยู่บนสุดของจอสมาชิก เหนือส่วน "คำเชิญที่รอตอบรับ")**

```
┌──────────────────────────────────────────────────────────────────┐
│ สมาชิก                                        [ + เชิญสมาชิก ]     │
│ ┌──────────────────────────────────────────────────────────────┐ │
│ │ ℹ ตอนนี้ร้านนี้มีเจ้าของร้านคนเดียวคือคุณ                        │ │
│ │   ถ้าวันหนึ่งคุณเข้าระบบไม่ได้ จะไม่มีใครในร้านช่วยตั้งรหัสผ่าน     │ │
│ │   ใหม่ให้ได้ แนะนำให้ตั้งคนที่คุณไว้ใจอีก 1 คนเป็นเจ้าของร้านไว้    │ │
│ │              [ เชิญเจ้าของร้านอีกคน ]   ไว้ทีหลัง                │ │
│ └──────────────────────────────────────────────────────────────┘ │
│ คำเชิญที่รอตอบรับ (1)   …                                          │
```

**เงื่อนไขการแสดง (อ่านจากข้อมูลที่มีจริง — ไม่ขอ field ใหม่)**
- ฉันมี `full_access` **และ** ในรายชื่อที่โหลดมามีสมาชิก `status=active` ที่ `roleKey === "owner"` **เพียงคนเดียว = ฉันเอง**
- **และ `nextCursor === null`** (โหลดรายชื่อครบแล้ว) — ถ้ายังโหลดไม่ครบ **ไม่แสดงแถบนี้** เพราะนับไม่ครบ = พูดในสิ่งที่ยังไม่รู้
- role ที่ `roleKey === null` (custom role ของ F-003) ⇒ **นับไม่ได้ → ไม่แสดงแถบ** (fail-quiet ดีกว่าเตือนผิด)
- `Banner` tone **info (ไม่ใช่ warning/danger)** — ผู้ใช้ไม่ได้ทำอะไรผิด และร้านไม่ได้มีปัญหาตอนนี้
- "ไว้ทีหลัง" = ซ่อนแถบต่อร้านนี้ (เก็บ local ฝั่ง client) · กลับมาแสดงอีกครั้งเมื่อ**เปิดแอปรอบใหม่**
  — ไม่ตามรบทุกจอ แต่ไม่ปล่อยให้ลืมถาวร · ปุ่มหลักเปิด S7 พร้อมเลือกสิทธิ์ "เจ้าของร้าน" ไว้ให้แล้ว

**Copy**

| องค์ประกอบ | ข้อความ |
|---|---|
| แถบ (ℹ) | ตอนนี้ร้านนี้มีเจ้าของร้านคนเดียวคือคุณ — ถ้าวันหนึ่งคุณเข้าระบบไม่ได้ จะไม่มีใครในร้านช่วยตั้งรหัสผ่านใหม่ให้ได้ แนะนำให้ตั้งคนที่คุณไว้ใจอีก 1 คนเป็นเจ้าของร้านไว้ |
| ปุ่มหลัก | เชิญเจ้าของร้านอีกคน |
| ปุ่มรอง | ไว้ทีหลัง |

> **ทำไมต้องมีแถบนี้ (D-030):** ตั้งแต่ D-030 **ไม่มีใครในร้านรีเซ็ตรหัสผ่านให้เจ้าของร้านได้อีก** (แม้แต่ผู้ดูแล)
> และเวอร์ชันนี้ยังไม่มีการตั้งรหัสใหม่เองทางอีเมล ⇒ ร้านที่มีเจ้าของคนเดียวจะกู้เองไม่ได้เลย ·
> **น้ำเสียง:** บอกวิธีป้องกันล่วงหน้า ไม่ใช่ขู่ — ห้ามใช้คำว่า "อันตราย/เสี่ยง/ข้อมูลจะหาย" และห้ามใช้ไอคอนเตือนสีแดง
> **จุดที่สอง** ของข้อความชุดนี้อยู่ที่การ์ด "เริ่มต้นใช้งาน" ของ S4 (เคสร้านเพิ่งสร้าง ยังมีคนเดียว) — §5

---

## 8. S7 — เชิญสมาชิก (dialog บน web · full-screen บน mobile)

**Wireframe**

```
┌ เชิญสมาชิก ─────────────────────────────────────────┐
│ อีเมลของคนที่จะเชิญ                                   │
│ ┌─────────────────────────────────────────────────┐ │
│ │ malee@shop.com                                  │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ ให้สิทธิ์เป็น                                          │
│ ┌─────────────────────────────────────────────────┐ │
│ │ ( ) เจ้าของร้าน                                   │ │
│ │     ทำได้ทุกอย่าง รวมถึงตั้ง/ถอดเจ้าของร้านคนอื่น      │ │
│ ├─────────────────────────────────────────────────┤ │
│ │ (•) ผู้ดูแล                                       │ │
│ │     จัดการทีมงานและงานประจำวันได้ แต่ตั้งเจ้าของไม่ได้ │ │
│ ├─────────────────────────────────────────────────┤ │
│ │ ( ) พนักงาน                                      │ │
│ │     ทำงานประจำวัน ไม่เห็นรายชื่อทีมและข้อมูลผู้เสียภาษี│ │
│ └─────────────────────────────────────────────────┘ │
│ ⏳ สิทธิ์ระดับนี้ ลิงก์คำเชิญจะมีอายุสั้นกว่าปกติเพื่อความปลอดภัย│
│ ℹ ระบบยังไม่ส่งอีเมลให้อัตโนมัติ — คุณจะได้ลิงก์ไว้ส่งเอง  │
│   ทางแชต (เช่น LINE)                                 │
│                        [ ยกเลิก ]  [ สร้างลิงก์คำเชิญ ] │
└─────────────────────────────────────────────────────┘
```

**กติกา**
- `roleId` **บังคับเลือก** (AC US-3) — ค่าตั้งต้น = "พนักงาน" (สิทธิ์ต่ำสุด, ปลอดภัยที่สุด); รายการมาจาก `GET /orgs/{orgId}/roles`
- ตัวเลือก **"เจ้าของร้าน" disabled เมื่อฉันไม่ใช่เจ้าของ** + helper "เฉพาะเจ้าของร้านเท่านั้นที่ตั้งเจ้าของร้านคนใหม่ได้" (บอกเหตุผล ดีกว่าซ่อนเงียบ ๆ)
- แถบ ⏳ แสดง **เฉพาะตอนเลือกสิทธิ์ระดับสูง** (เจ้าของร้าน/ผู้ดูแล) และ **ไม่ระบุตัวเลขชั่วโมง** (ตัวเลขจริงมาจาก `expiresAt` หลังสร้างเสร็จเท่านั้น — ตอบ Q14)
- แถบ ℹ (D-012) อยู่เหนือปุ่มเสมอ — ตั้งความคาดหวังก่อนกด ว่าต้องส่งลิงก์เอง
- ปุ่มหลัก **disabled ระหว่างรอ** · สำเร็จ (201) → ไปต่อที่ S8 ทันทีในกล่องเดิม (ไม่ปิดแล้วเปิดใหม่)

**Copy**

| องค์ประกอบ | ข้อความ |
|---|---|
| หัวข้อ | เชิญสมาชิก |
| label อีเมล | อีเมลของคนที่จะเชิญ |
| placeholder | เช่น malee@shop.com |
| label สิทธิ์ | ให้สิทธิ์เป็น |
| คำอธิบาย เจ้าของร้าน | ทำได้ทุกอย่าง รวมถึงตั้ง/ถอดเจ้าของร้านคนอื่น |
| คำอธิบาย ผู้ดูแล | จัดการทีมงานและงานประจำวันได้ แต่ตั้งเจ้าของร้านไม่ได้ |
| คำอธิบาย พนักงาน | ทำงานประจำวัน ไม่เห็นรายชื่อทีมงานและข้อมูลผู้เสียภาษี |
| helper (เจ้าของ disabled) | เฉพาะเจ้าของร้านเท่านั้นที่ตั้งเจ้าของร้านคนใหม่ได้ |
| แถบอายุลิงก์ | สิทธิ์ระดับนี้ ลิงก์คำเชิญจะมีอายุสั้นกว่าปกติเพื่อความปลอดภัย |
| แถบวิธีส่ง | ระบบยังไม่ส่งอีเมลให้อัตโนมัติ — คุณจะได้ลิงก์ไว้ส่งเองทางแชต (เช่น LINE) |
| ปุ่มหลัก | สร้างลิงก์คำเชิญ |
| ปุ่มหลัก (loading) | กำลังสร้างลิงก์... |

**Error mapping**

| error | UI | Copy |
|---|---|---|
| `422 VALIDATION_FAILED` (`fieldErrors.email`) | inline ใต้ฟิลด์ | "รูปแบบอีเมลไม่ถูกต้อง" |
| `409 ALREADY_MEMBER` | inline ใต้ฟิลด์ + ลิงก์ | "อีเมลนี้เป็นสมาชิกของร้านอยู่แล้ว" + ลิงก์ "ดูในรายชื่อสมาชิก" |
| **`409 INVITATION_PENDING`** (+`details`) | เปลี่ยนกล่องเป็น **แผงทางเลือก** (ไม่ปล่อยให้ตัน — D-027) | หัวข้อ "อีเมลนี้มีคำเชิญค้างอยู่แล้ว" · เนื้อ "คำเชิญเดิม: สิทธิ์ {roleName} · ลิงก์ใช้ได้ถึง {expiresAt}" · ปุ่ม **[ ออกลิงก์ใหม่ ]** (→ S8 ผ่าน `invitationId`) · ปุ่มรอง **[ ยกเลิกคำเชิญเดิม ]** · ลิงก์ "กลับไปแก้อีเมล" |
| `409 INVITATION_LIMIT_REACHED` | `ErrorBanner` ในกล่อง | "มีคำเชิญที่รอตอบรับค้างอยู่มากเกินไป — ยกเลิกคำเชิญที่ไม่ใช้แล้วก่อน จึงจะเชิญคนใหม่ได้" |
| `422 ROLE_INVALID` | inline ที่รายการสิทธิ์ + refetch roles | "สิทธิ์ที่เลือกใช้ไม่ได้แล้ว กรุณาเลือกใหม่อีกครั้ง" |
| `403 FORBIDDEN` | `ErrorBanner` ในกล่อง | "คุณไม่มีสิทธิ์เชิญสมาชิกด้วยสิทธิ์ระดับนี้ — เฉพาะเจ้าของร้านเท่านั้นที่ตั้งเจ้าของร้านคนใหม่ได้" |
| `429` | `ThrottleBanner` + ปุ่ม disabled | "เชิญสมาชิกถี่เกินไป กรุณารอสักครู่แล้วลองใหม่ · เหลือ {mm}:{ss}" |
| network/5xx | `ErrorBanner` + "ลองใหม่" | "สร้างคำเชิญไม่สำเร็จ กรุณาลองใหม่อีกครั้ง" |

---

## 9. S8 — ลิงก์คำเชิญ (แสดงครั้งเดียว) + ออกลิงก์ใหม่

### 9.1 แผ่นลิงก์ (`CopyLinkPanel`) — หลังสร้างคำเชิญ / หลังออกลิงก์ใหม่

**Wireframe**

```
┌ ลิงก์คำเชิญพร้อมแล้ว ─────────────────────────────────┐
│ ส่งลิงก์นี้ให้ malee@shop.com ทางแชต                    │
│ ┌─────────────────────────────────────────────────┐ │
│ │ https://app.omnistock.co/invite?token=9f2b…      │ │
│ └─────────────────────────────────────────────────┘ │
│ [        คัดลอกลิงก์        ]                        │
│                                                     │
│ ⏳ ลิงก์ใช้ได้ถึง 29 ก.ค. 2026, 14:30 (อีกประมาณ 24 ชั่วโมง)│
│ ⚠ ลิงก์นี้แสดงครั้งเดียว — ปิดหน้านี้แล้วเปิดดูซ้ำไม่ได้       │
│   ถ้าทำลิงก์หาย กด "ออกลิงก์ใหม่" ที่รายการคำเชิญ          │
│                                        [ เสร็จแล้ว ] │   ← ปุ่มรอง (ยังไม่ได้คัดลอก)
└─────────────────────────────────────────────────────┘
      ↓ กด "คัดลอกลิงก์"
┌ ลิงก์คำเชิญพร้อมแล้ว ─────────────────────────────────┐
│ …                                                   │
│ [       ✓ คัดลอกแล้ว       ]  ← 2 วินาที + toast      │
│ ⏳ / ⚠ แถบเดิมยังอยู่ครบ (ไม่หายไปหลังคัดลอก)            │
│                                        [ เสร็จแล้ว ] │   ← กลายเป็นปุ่มหลัก
└─────────────────────────────────────────────────────┘
```

**กติกา (D-027 — ห้ามแหก)**
- กล่องลิงก์เป็น **read-only** (เลือกคัดลอกด้วยมือได้) + ปุ่ม "คัดลอกลิงก์" · กดแล้ว → ปุ่มเปลี่ยนเป็น "คัดลอกแล้ว ✓" 2 วินาที + toast
- **วันหมดอายุอ่านจาก `expiresAt` เสมอ** แสดง 2 ชั้น: วัน-เวลาเต็ม + "(อีกประมาณ …)" — **ห้ามเขียน "7 วัน" หรือ "24 ชั่วโมง" เป็นข้อความตายตัว**
- **แถบเตือน "ลิงก์นี้แสดงครั้งเดียว" = ด่านเดียว** (user เคาะ 2026-07-28) — **ไม่มี dialog กันปิดก่อนคัดลอก**
  - เหตุผล: 2 ชั้น (แถบเตือน + dialog ตอนจะปิด) ถือว่ามากเกินไป · แถบเตือนอยู่ **เหนือปุ่มปิดเสมอ** ⇒ อยู่ในสายตาตอนกำลังจะกดปิดอยู่แล้ว
  - สิ่งที่ยังต้องทำแทน (ห้ามตัด): แถบเตือนใช้ tone **warning** ไม่ใช่ helper text จาง ๆ · และ **ปุ่ม "เสร็จแล้ว" เป็นปุ่มรอง (`secondary`) จนกว่าจะกดคัดลอก แล้วจึงกลายเป็นปุ่มหลัก (`primary`)** — ทางออกจะ "เด่น" ก็ต่อเมื่อปลอดภัยแล้ว
- ปิดแล้ว → รีเฟรชรายการคำเชิญใน S6
- **ห้ามมีคำว่า "คัดลอกลิงก์เดิม" ที่ใดในระบบ** (ปุ่มบนคำเชิญค้างชื่อ "ออกลิงก์ใหม่" เท่านั้น)

**Copy**

| องค์ประกอบ | ข้อความ |
|---|---|
| หัวข้อ | ลิงก์คำเชิญพร้อมแล้ว |
| คำอธิบาย | ส่งลิงก์นี้ให้ {email} ทางแชต (เช่น LINE) — ระบบไม่ได้ส่งอีเมลให้อัตโนมัติ |
| ปุ่มคัดลอก | คัดลอกลิงก์ → (หลังกด) คัดลอกแล้ว |
| toast | คัดลอกลิงก์แล้ว |
| แถบอายุ | ลิงก์ใช้ได้ถึง {วัน เดือน ปี, เวลา} (อีกประมาณ {ระยะเวลา}) |
| แถบเตือน | ลิงก์นี้แสดงครั้งเดียว — ปิดหน้านี้แล้วเปิดดูซ้ำไม่ได้ ถ้าทำลิงก์หาย กด "ออกลิงก์ใหม่" ที่รายการคำเชิญ |
| ปุ่มปิด | เสร็จแล้ว *(ปุ่มรองก่อนคัดลอก → ปุ่มหลักหลังคัดลอก)* |

**States** — loading: ปุ่มที่พามาถึงแผ่นนี้เป็นตัว loading อยู่แล้ว (แผ่นนี้เปิดเมื่อมีข้อมูลจริงเท่านั้น) · error: ไม่มี (ถ้าสร้างไม่สำเร็จจะไม่มาถึงจอนี้) · copy ล้มเหลว (clipboard API ถูกปฏิเสธ) → helper ใต้ปุ่ม "คัดลอกอัตโนมัติไม่สำเร็จ — กดค้างที่ลิงก์เพื่อคัดลอกเอง"

### 9.2 ยืนยันก่อนออกลิงก์ใหม่ (`POST …/invitations/{id}/link`)

**Wireframe**

```
┌ ออกลิงก์ใหม่? ───────────────────────────────┐
│ ลิงก์เดิมที่ส่งไปแล้วจะใช้ไม่ได้ทันที               │
│ ถ้าคุณส่งลิงก์เดิมให้ใครไว้ ต้องส่งลิงก์ใหม่ให้เขาแทน  │
│                                             │
│ อีเมลและสิทธิ์ของคำเชิญไม่เปลี่ยน                 │
│ (malee@shop.com · ผู้ดูแล)                    │
│                                             │
│                 [ ยกเลิก ]  [ ออกลิงก์ใหม่ ]  │
└─────────────────────────────────────────────┘
```

- default focus = **"ยกเลิก"** · ปุ่มยืนยันชื่อ **"ออกลิงก์ใหม่"** (ตรงตาม D-027)
- สำเร็จ → เปิด `CopyLinkPanel` (§9.1) ด้วยลิงก์ใหม่ + `expiresAt` ใหม่ + toast "ออกลิงก์ใหม่แล้ว ลิงก์เดิมใช้ไม่ได้แล้ว"
- error: `409 CONFLICT` → toast "คำเชิญนี้ไม่ได้รออยู่แล้ว (อาจถูกรับหรือยกเลิกไปแล้ว)" + refetch รายการ · `404` → toast "ไม่พบคำเชิญนี้แล้ว" + refetch · `429` → `ThrottleBanner` · `403` → toast มาตรฐาน (§11ข)

### 9.3 ยกเลิกคำเชิญ (`DELETE …/invitations/{id}`)

```
┌ ยกเลิกคำเชิญนี้? ─────────────────────────────┐
│ ลิงก์ที่ส่งไปแล้วจะใช้ไม่ได้ทันที                   │
│ malee@shop.com · ผู้ดูแล                       │
│               [ ไม่ยกเลิก ]  [ ยกเลิกคำเชิญ ]  │
└──────────────────────────────────────────────┘
```
- ปุ่มยืนยัน = variant destructive · default focus = "ไม่ยกเลิก"
- สำเร็จ → toast "ยกเลิกคำเชิญแล้ว" + แถวหายจากส่วน "รอตอบรับ"
- error `409 CONFLICT`/`404` → toast "คำเชิญนี้ไม่ได้รออยู่แล้ว" + refetch

---

## 10. S9/S10 — เปลี่ยนสิทธิ์ · ถอดออกจากร้าน · ออกจากร้านนี้

### 10.1 S9 เปลี่ยนสิทธิ์ (`PATCH …/members/{userId}`)

**Wireframe**

```
┌ เปลี่ยนสิทธิ์ของ malee@shop.com ─────────────────────┐
│ ┌─────────────────────────────────────────────────┐ │
│ │ ( ) เจ้าของร้าน   ทำได้ทุกอย่าง …                   │ │
│ │ (•) ผู้ดูแล        จัดการทีมงาน …   (สิทธิ์ปัจจุบัน)  │ │
│ │ ( ) พนักงาน       ทำงานประจำวัน …                 │ │
│ └─────────────────────────────────────────────────┘ │
│ ⚠ ร้านนี้ต้องมีเจ้าของอย่างน้อย 1 คนเสมอ                │
│                        [ ยกเลิก ]  [ บันทึกสิทธิ์ ]   │
└─────────────────────────────────────────────────────┘
```

- รายการสิทธิ์เดียวกับ S7 · ป้าย "(สิทธิ์ปัจจุบัน)" ที่ค่าเดิม · ปุ่มบันทึก disabled ถ้าไม่เปลี่ยนค่า
- ตัวเลือก "เจ้าของร้าน" **disabled เมื่อฉันไม่ใช่เจ้าของ** + helper เดิม (Owner-only, D-028)
- แถบ ⚠ แสดงเมื่อกำลังลดสิทธิ์ของเจ้าของร้าน (รวมถึงตัวเอง) — เตือนล่วงหน้า ไม่ต้องรอ error
- สำเร็จ → toast "เปลี่ยนสิทธิ์ของ {email} เป็น {สิทธิ์} แล้ว" + refetch รายชื่อ

**Error mapping**

| error | UI + Copy |
|---|---|
| `409 LAST_OWNER` | `ErrorBanner` ในกล่อง: "ร้านนี้ต้องมีเจ้าของอย่างน้อย 1 คน — ตั้งคนอื่นเป็นเจ้าของร้านก่อน แล้วค่อยเปลี่ยนสิทธิ์นี้" |
| `403 FORBIDDEN` | `ErrorBanner` ในกล่อง: "คุณไม่มีสิทธิ์เปลี่ยนสิทธิ์นี้ — เฉพาะเจ้าของร้านเท่านั้นที่ตั้งหรือแก้สิทธิ์ของเจ้าของร้าน" |
| `422 ROLE_INVALID` | "สิทธิ์ที่เลือกใช้ไม่ได้แล้ว กรุณาเลือกใหม่อีกครั้ง" + refetch roles |
| `404 NOT_FOUND` | toast "คนนี้ไม่ได้เป็นสมาชิกของร้านนี้แล้ว" + ปิดกล่อง + refetch |
| network/5xx | `ErrorBanner` + "ลองใหม่" |

### 10.2 S10 ถอดออกจากร้าน (`DELETE …/members/{userId}`)

**Wireframe**

```
┌ ถอด malee@shop.com ออกจากร้าน? ─────────────────────┐
│ • เขาจะเข้าถึงข้อมูลของร้านนี้ไม่ได้ทันที                  │
│ • ประวัติการทำรายการที่เขาเคยทำไว้ยังอยู่ครบ               │
│ • ถ้ามีคำเชิญของอีเมลนี้ค้างอยู่ ระบบจะยกเลิกให้ด้วย         │
│ • ให้กลับเข้ามาใหม่ได้ด้วยการเชิญใหม่เท่านั้น               │
│                    [ ยกเลิก ]  [ ถอดออกจากร้าน ]     │
└─────────────────────────────────────────────────────┘
```

- ปุ่มยืนยัน variant destructive · default focus = "ยกเลิก"
- สำเร็จ → toast "ถอด {email} ออกจากร้านแล้ว" · ถ้า `cancelledInvitations > 0` ต่อท้าย "· ยกเลิกคำเชิญที่ค้างอยู่ {n} ใบด้วย"
- error: `409 LAST_OWNER` → "ร้านนี้ต้องมีเจ้าของอย่างน้อย 1 คน — ตั้งคนอื่นเป็นเจ้าของร้านก่อน" · `403 FORBIDDEN` → "เฉพาะเจ้าของร้านเท่านั้นที่ถอดเจ้าของร้านคนอื่นได้" · `404` → toast "คนนี้ไม่ได้เป็นสมาชิกของร้านนี้แล้ว" + refetch

### 10.3 ออกจากร้านนี้ (ถอดตัวเอง — `DELETE /orgs/{orgId}/membership` · D-029)

```
┌ ออกจากร้านหอมกรุ่นเบเกอรี่? ──────────────────────────┐
│ คุณจะเข้าถึงข้อมูลของร้านนี้ไม่ได้อีก จนกว่าจะมีคนเชิญคุณ   │
│ กลับเข้ามาใหม่ · ร้านอื่นของคุณไม่ได้รับผลกระทบ           │
│                    [ ยกเลิก ]  [ ออกจากร้านนี้ ]      │
└─────────────────────────────────────────────────────┘
```
- **ทางเข้า 2 ที่:** (ก) **ลิงก์ท้ายจอ S4 "ออกจากร้านนี้"** — ทางเข้าหลัก เพราะ **สมาชิก active ทุกคนทำได้**
  (endpoint แยก ไม่ต้องมี `manage_members` — api-spec §3.17) และพนักงานเข้าจอสมาชิกไม่ได้
  (ข) เมนู `⋯` บนแถวของฉันเองในจอสมาชิก (สำหรับผู้ที่เห็นจออยู่แล้ว)
- สำเร็จ (200) → พากลับ **S1 เลือกร้าน** + toast "คุณออกจากร้าน "{ชื่อร้าน}" แล้ว" (+ refetch `/me/organizations`)
  · ถ้า `cancelledInvitations > 0` ไม่ต้องพูดถึง (เป็นคำเชิญของตัวเอง — ไม่ใช่สิ่งที่ผู้ใช้กำลังสนใจ)
- **error mapping**
  | error | UI + Copy |
  |---|---|
  | `409 LAST_OWNER` | `ErrorBanner` ในกล่อง confirm (ไม่ปิดกล่อง): "คุณเป็นเจ้าของร้านคนเดียวของร้านนี้ — ตั้งคนอื่นเป็นเจ้าของร้านก่อน แล้วจึงออกจากร้านนี้ได้" · **+ ทางไปต่อ ตามตารางใต้หัวข้อนี้** |
  | `409` + `details.reason="busy"` | copy กลาง §1.4 (ระบบกำลังทำรายการอื่น… + ลองใหม่) — กล่องไม่ปิด |
  | `403 ORG_ACCESS_DENIED` (กดซ้ำหลังออกไปแล้ว) | ปิดกล่อง → S12ก ตามปกติ (ไม่ใช่ error ให้ผู้ใช้แก้) |
  | network/5xx | `ErrorBanner` + "ลองใหม่" |
- **ไม่มี `403 FORBIDDEN` บนเส้นนี้** (ไม่มี capability ให้ขาด) ⇒ ถ้า client เจอ 403 ให้ถือเป็น `ORG_ACCESS_DENIED` เท่านั้น

#### ทางไปต่อของ `409 LAST_OWNER` — ขึ้นกับว่า dialog ถูกเปิดจากจอไหน (แก้ 2026-09-05 · M-06 nit)

```
┌ ออกจากร้านหอมกรุ่นเบเกอรี่? ──────────────────────────┐
│ คุณจะเข้าถึงข้อมูลของร้านนี้ไม่ได้อีก จนกว่าจะมีคนเชิญคุณ   │
│ กลับเข้ามาใหม่ · ร้านอื่นของคุณไม่ได้รับผลกระทบ           │
│ ┌─────────────────────────────────────────────────┐ │
│ │ ⓧ คุณเป็นเจ้าของร้านคนเดียวของร้านนี้ — ตั้งคนอื่น    │ │
│ │   เป็นเจ้าของร้านก่อน แล้วจึงออกจากร้านนี้ได้         │ │
│ └─────────────────────────────────────────────────┘ │
│ (ก) เปิดจากจอ "ข้อมูลร้าน" → ไปตั้งเจ้าของร้านคนใหม่      │ ← ลิงก์ ไปจอสมาชิก
│ (ข) เปิดจากจอ "สมาชิก"    → เลือกคนที่คุณไว้ใจในรายชื่อ   │ ← ประโยค ไม่มีลิงก์
│                            ด้านล่าง แล้วกด "เปลี่ยนสิทธิ์" │
│                            เป็นเจ้าของร้าน                │
│                    [ ยกเลิก ]  [ ออกจากร้านนี้ ]         │
└──────────────────────────────────────────────────────┘
```

| เปิดจากไหน | ทางไปต่อ | ข้อความ |
|---|---|---|
| จอ **"ข้อมูลร้าน" (S4)** — ทางเข้าหลัก | `Link` ไป `/o/{orgId}/settings/members` | **"ไปตั้งเจ้าของร้านคนใหม่"** |
| จอ **"สมาชิก" (S6)** — เปิดจากเมนูแถวของตัวเอง | **ไม่มีลิงก์** — ประโยคบอกขั้นตอนถัดไปแทน | **"เลือกคนที่คุณไว้ใจในรายชื่อด้านล่าง แล้วกด "เปลี่ยนสิทธิ์" เป็นเจ้าของร้าน"** |
| ไม่มี `manage_members` | ไม่มีทั้งลิงก์และประโยค (เหลือแค่ `ErrorBanner`) | — *(คนที่ชน `LAST_OWNER` เป็นเจ้าของร้านเสมอ จึงแทบไม่เกิด — แต่ยังต้องไม่ชวนไปจอที่ตอบ 403)* |

- **ที่มา:** ปุ่มเดิมเขียนว่า "ไปหน้าสมาชิก" และชี้ไป `/o/{id}/settings/members` เสมอ ⇒ เมื่อ dialog ถูกเปิด
  **จากจอสมาชิกเอง** ปุ่มนั้นชี้มาที่หน้าที่ยืนอยู่ **กดแล้วไม่เกิดอะไรเลย** · CTA ที่กดแล้วเงียบ แย่กว่าไม่มี CTA
  เพราะมันมาถึงในวินาทีที่ผู้ใช้เพิ่งถูกปฏิเสธ ⇒ อ่านว่า "แอปพัง"
- **ป้ายปุ่มเปลี่ยนไปบอก "ผลลัพธ์" ไม่ใช่ "ชื่อหน้า"** — คนอ่านไม่ได้อยากไปหน้าสมาชิก เขาอยากออกจากร้าน
  และสิ่งที่ขวางอยู่คือยังไม่มีเจ้าของร้านคนอื่น · ชื่อปุ่มจึงต้องเป็นสิ่งที่เขากำลังจะทำ
- **สาขา (ข) ชี้ไปที่ปุ่มที่มีอยู่จริงบนจอนั้น** ("เปลี่ยนสิทธิ์" บนแถวสมาชิก) — ถ้าในร้านยังไม่มีใครเลย
  รายชื่อด้านล่างจะเป็น `EmptyState` "ตอนนี้มีคุณอยู่คนเดียวในร้านนี้…" + ปุ่ม "เชิญสมาชิก" อยู่แล้ว ⇒ ไม่ตัน

---

## 11. S11 — รับคำเชิญ (`/invite`) — public, มือถือเป็นหลัก

> **ข้อยกเว้นเดียวของ design-system §8:** จอนี้ต้องใช้งานได้ดีบน **เบราว์เซอร์มือถือ** เพราะลิงก์ถูกส่งทาง LINE/แชต
> และผู้รับกดจากมือถือเป็นหลัก (จอ web อื่นยัง desktop-first ตามเดิม)
> **@frontend ★:** ถอด token ออกจาก URL ด้วย `history.replaceState` ทันทีที่อ่านได้ และเก็บไว้ใน memory เท่านั้น (api-spec §3.14)

### 11.1 ก่อนล็อกอิน (คำเชิญยังใช้ได้)

```
┌───────────────────────────────────────┐
│              OmniStock                │
│                                       │
│      คำเชิญเข้าร่วมร้าน                  │
│      ร้านหอมกรุ่นเบเกอรี่                 │
│      ชวนคุณเข้าร่วมเป็น "ผู้ดูแล"          │
│                                       │
│  คำเชิญนี้ออกให้ m***@shop.com           │
│  ลิงก์ใช้ได้ถึง 29 ก.ค. 2026, 14:30       │
│                                       │
│  [   เข้าสู่ระบบเพื่อรับคำเชิญ   ]        │
│  [   สมัครบัญชีใหม่           ]         │
│                                       │
│  ใช้อีเมลเดียวกับที่ถูกเชิญเท่านั้น           │
└───────────────────────────────────────┘
```

- ปุ่มบน = ไปหน้า login ของ F-001 (พา `token` ไปด้วยใน memory/state) → กลับมาที่ `/invite` อัตโนมัติหลังล็อกอิน
- ปุ่มล่าง = ไปหน้า signup → หลังสมัคร + ล็อกอิน กลับมาที่ `/invite` แล้ว **กดรับเอง** (ไม่ auto-accept — ผู้ใช้ต้องเห็นว่าตัวเองกำลังเข้าร้านอะไร)

### 11.2 ล็อกอินแล้ว (บัญชีถูกต้อง)

```
┌───────────────────────────────────────┐
│      คำเชิญเข้าร่วมร้าน                  │
│      ร้านหอมกรุ่นเบเกอรี่                 │
│      ชวนคุณเข้าร่วมเป็น "ผู้ดูแล"          │
│      ลิงก์ใช้ได้ถึง 29 ก.ค. 2026, 14:30   │
│                                       │
│  [        เข้าร่วมร้านนี้        ]        │
│                                       │
│  คุณกำลังใช้บัญชี malee@shop.com          │
│  ใช้บัญชีอื่น                             │
└───────────────────────────────────────┘
```
- ปุ่ม "เข้าร่วมร้านนี้" → `POST /invitations/accept` · loading "กำลังเข้าร่วม..." + disabled
- ลิงก์ "ใช้บัญชีอื่น" = ออกจากระบบแล้วกลับมาที่ `/invite` (คงลิงก์เดิมไว้ให้)

### 11.3 ล็อกอินผิดบัญชี (`403 INVITATION_EMAIL_MISMATCH`)

```
┌───────────────────────────────────────┐
│  ⚠ บัญชีไม่ตรงกับคำเชิญ                  │
│                                       │
│  คำเชิญนี้ออกให้ m***@shop.com           │
│  ตอนนี้คุณเข้าสู่ระบบด้วย noi@shop.com     │
│  กรุณาเข้าสู่ระบบด้วยบัญชีที่ถูกเชิญ         │
│                                       │
│  [ ออกจากระบบแล้วเข้าด้วยบัญชีนั้น ]        │
│  [ กลับหน้าแรก ]                        │
└───────────────────────────────────────┘
```
- ใช้ `details.emailMasked` จาก server เป็นความจริง (ไม่เดา/ไม่เทียบเองฝั่ง client) · **ไม่เผยอีเมลเต็มของผู้ถูกเชิญ**

### 11.4 States & error mapping ทั้งหมดของจอนี้

| สถานการณ์ | Copy (หัวข้อ / เนื้อ / ทางไปต่อ) |
|---|---|
| loading (preview) | skeleton การ์ด (หัวข้อ + 3 บรรทัด + 2 ปุ่ม) |
| `404 INVITATION_INVALID` | "ลิงก์คำเชิญนี้ใช้ไม่ได้" / "ลิงก์อาจถูกคัดลอกมาไม่ครบ หรือถูกยกเลิกไปแล้ว" / ปุ่ม "ขอลิงก์ใหม่จากเจ้าของร้าน" (= ข้อความแนะนำ + ปุ่ม "กลับหน้าแรก") |
| `409 INVITATION_EXPIRED` | "ลิงก์คำเชิญหมดอายุแล้ว" / "ขอให้เจ้าของร้านกด "ออกลิงก์ใหม่" แล้วส่งลิงก์ใหม่ให้คุณ" / ปุ่ม "กลับหน้าแรก" |
| `409 INVITATION_CANCELLED` | "คำเชิญนี้ถูกยกเลิกแล้ว" / "ติดต่อเจ้าของร้านถ้าคุณยังต้องการเข้าร่วม" |
| `409 INVITATION_ALREADY_ACCEPTED` | "คำเชิญนี้ถูกใช้ไปแล้ว" / "ถ้าคุณเป็นคนกดรับเอง ให้เข้าสู่ระบบแล้วเลือกร้านนี้ได้เลย" / ปุ่ม "เข้าสู่ระบบ" |
| `409 ALREADY_MEMBER` | "คุณเป็นสมาชิกของร้านนี้อยู่แล้ว" / "เข้าใช้งานได้เลย — สิทธิ์เดิมของคุณไม่ถูกเปลี่ยน" / ปุ่ม "เข้าใช้งานร้านนี้" |
| `409 INVITATION_SUPERSEDED` | "คำเชิญนี้ใช้ไม่ได้แล้ว" / "คำเชิญนี้ถูกออกก่อนที่คุณจะถูกถอดออกจากร้าน — ขอคำเชิญใหม่จากเจ้าของร้าน" |
| `409 INVITATION_ROLE_UNAVAILABLE` | "คำเชิญนี้ใช้ไม่ได้แล้ว" / "สิทธิ์ที่ระบุในคำเชิญถูกเปลี่ยนไปแล้ว — ขอลิงก์ใหม่จากเจ้าของร้าน" |
| `403 INVITATION_EMAIL_MISMATCH` | §11.3 |
| `429 RATE_LIMITED` | `ThrottleBanner`: "เปิดลิงก์คำเชิญถี่เกินไป กรุณารอสักครู่แล้วลองใหม่ · เหลือ {mm}:{ss}" |
| network/5xx | "เปิดคำเชิญไม่สำเร็จ" + ปุ่ม "ลองใหม่" |
| สำเร็จ | เต็มจอ: "เข้าร่วม "{ชื่อร้าน}" เรียบร้อย" + "สิทธิ์ของคุณ: {สิทธิ์}" + ปุ่มหลัก "เริ่มใช้งานร้านนี้" (→ `/o/{orgId}`) + บรรทัดเล็ก "ถ้าคุณใช้แอปบนมือถือ เปิดแอปแล้วสลับมาที่ร้านนี้ได้เลย" |

**Platform note** — F-002 ทำจอนี้บน **web เท่านั้น** (ลิงก์เป็น https ของ web) · deep link เข้าแอป (`omnistock://`) เป็นของ **F-006** — ไม่ประดิษฐ์ล่วงหน้า

---

## 12. S12 — สองหน้าตาของ 403 (ตอบ Q6) + copy ฝากไว้ S13

### 12.1 S12ก — `403 ORG_ACCESS_DENIED` (ถูกถอด / ไม่ใช่สมาชิก)

```
(ผู้ใช้กำลังอยู่ที่ /o/org_1/settings/members แล้วถูกถอด → request ถัดไป 403)
                    │
                    ▼  พากลับทันที + refetch /me/organizations
┌────────────────────────────────────────────────────────────┐
│ ⚠ คุณไม่ได้เป็นสมาชิกของร้าน "ร้านหอมกรุ่นเบเกอรี่" แล้ว        │
│   เลือกร้านอื่นด้านล่าง หรือติดต่อเจ้าของร้านถ้าคิดว่าไม่ถูกต้อง    │
├────────────────────────────────────────────────────────────┤
│              เลือกร้านที่จะเข้าใช้งาน   (= S1)                 │
│   ┌──────────────────────────────────────────────────────┐ │
│   │ [ค] คลังของฝากเชียงใหม่                     พนักงาน  › │ │
│   └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```
- **พฤติกรรม:** ทิ้ง org context ปัจจุบัน → ไป S1 → refetch `/me/organizations` (ร้านนั้นจะหายไปเอง) → แสดงแถบเตือน
- ถ้าไม่เหลือร้านเลย → S1 แสดง empty state + แถบเตือนเดิมค้างไว้ด้านบน
- แถบเป็น **warning (เหลือง) ไม่ใช่ danger (แดง)** — ผู้ใช้ไม่ได้ทำอะไรผิด และยังใช้ระบบต่อได้
- **ห้ามใช้ handler เดียวกับ `FORBIDDEN`** และ **ห้าม logout** (session ไม่ผูกกับร้าน — D-027)

### 12.2 S12ข — `403 FORBIDDEN` (เป็นสมาชิกแต่ไม่มีสิทธิ์)

- **กรณีกดปุ่ม/ส่งฟอร์ม:** toast กลาง **อยู่หน้าเดิม ไม่ redirect ไม่ล้างฟอร์ม**
  > "คุณไม่มีสิทธิ์ทำรายการนี้ ติดต่อเจ้าของร้านให้เปิดสิทธิ์ให้"
  (จุดที่มีเหตุผลเฉพาะ เช่น Owner-only ให้ใช้ copy เจาะจงตาม §8/§10 แทน — ชัดกว่า)
- **กรณีเข้าหน้าที่ไม่มีสิทธิ์ตรงผ่าน URL:** `ForbiddenPanel` เต็มพื้นที่เนื้อหา (ยังอยู่ในร้านเดิม, เมนู/ตัวสลับร้านยังอยู่)

```
┌──────────────────────────────────────────────┐
│                    🔒                        │
│      หน้านี้เปิดให้เฉพาะผู้ที่ดูแลทีมงาน           │
│  ถ้าคุณต้องใช้หน้านี้ ให้เจ้าของร้านเปิดสิทธิ์ให้คุณ   │
│            [ กลับหน้าแรกของร้าน ]              │
└──────────────────────────────────────────────┘
```

### 12.3 S13 — copy ฝากไว้: ตั้งรหัสผ่านใหม่ให้สมาชิก (ตอบ Q15 · D-028/C-2 + **D-030/NEW-1**)

> จอจริงเป็นของ F-004 (admin reset ของ F-001) — ที่นี่ให้ **copy ที่ตั้งความคาดหวังล่วงหน้า** เพราะตอนนี้มี
> **2 เคส** ที่ระบบจะปฏิเสธ และทั้งคู่คืน **`404` รูปเดียวกันที่ไม่อธิบายอะไร** (โดยเจตนา — ห้ามเป็น oracle):
> (1) **D-028/C-2** target เป็นสมาชิก active ของร้านอื่นด้วย (2) **D-030/NEW-1** target เป็นเจ้าของร้าน
> แต่ผู้กดไม่ใช่เจ้าของร้าน · **UI แยกสองเคสนี้ไม่ได้ และไม่ควรพยายามแยก**

```
┌ ตั้งรหัสผ่านใหม่ให้ noi@shop.com ──────────────────────┐
│ รหัสผ่านใหม่                                          │
│ ┌─────────────────────────────────────────────────┐ │
│ │ ••••••••                                    👁  │ │
│ └─────────────────────────────────────────────────┘ │
│ ℹ บางบัญชีระบบจะไม่ให้คุณตั้งรหัสผ่านใหม่ให้ เพื่อความ    │
│   ปลอดภัยของเจ้าของบัญชี — ถ้าเจอกรณีนั้น ต้องให้เจ้าตัว │
│   ติดต่อทีมงาน OmniStock เอง                          │
│                     [ ยกเลิก ]  [ ตั้งรหัสผ่านใหม่ ]   │
└─────────────────────────────────────────────────────┘

           กด → ระบบปฏิเสธ (404 ทั้ง 2 เคส เหมือนกันทุกตัวอักษร)
                                │
                                ▼
┌ ตั้งรหัสผ่านใหม่ให้ noi@shop.com ──────────────────────┐
│ ⚠ ตั้งรหัสผ่านใหม่ให้บัญชีนี้ไม่ได้ เพื่อความปลอดภัยของ    │
│   เจ้าของบัญชี — ให้เจ้าตัวติดต่อทีมงาน OmniStock       │
│   เพื่อขอความช่วยเหลือ                                 │
│                                     [ ปิด ]           │
└─────────────────────────────────────────────────────┘
```

**Copy (ชุดเดียวครอบทั้งสองเคส)**

| จุด | ข้อความ |
|---|---|
| หมายเหตุก่อนกด (ℹ) | บางบัญชีระบบจะไม่ให้คุณตั้งรหัสผ่านใหม่ให้ เพื่อความปลอดภัยของเจ้าของบัญชี — ถ้าเจอกรณีนั้น ต้องให้เจ้าตัวติดต่อทีมงาน OmniStock เอง |
| เมื่อได้ `404` (เคสไหนก็ตาม) | ตั้งรหัสผ่านใหม่ให้บัญชีนี้ไม่ได้ เพื่อความปลอดภัยของเจ้าของบัญชี — ให้เจ้าตัวติดต่อทีมงาน OmniStock เพื่อขอความช่วยเหลือ |
| บรรทัดช่วยเหลือ (เจ้าของร้านลืมรหัสของตัวเอง) | ถ้าคุณเป็นเจ้าของร้านและลืมรหัสผ่านของตัวเอง เวอร์ชันนี้ยังไม่มีการตั้งรหัสใหม่เองทางอีเมล — ติดต่อทีมงาน OmniStock เพื่อขอความช่วยเหลือ |

**กติกาของ copy ชุดนี้ (ห้ามแหก)**
- **ข้อความล้มเหลวเหมือนกันทุกตัวอักษรทุกเคส** — ไม่มีบัญชีนี้ / อยู่ร้านอื่นด้วย / เป็นเจ้าของร้าน ⇒ ผู้กดแยกไม่ออก
  (ถ้าเขียนแยก ผู้ที่มี `manage_members` จะใช้จอนี้ไล่เดาว่าใครสังกัดร้านไหน / ใครเป็นเจ้าของร้าน)
- **ไม่โกหก:** ไม่พูดว่า "เกิดข้อผิดพลาด" หรือ "ลองใหม่อีกครั้ง" ในเคสนี้ (ระบบไม่ได้พัง และลองใหม่ก็ไม่ได้ผล) ·
  พูดสิ่งที่จริงเสมอ = "ระบบไม่ให้ตั้งให้ เพื่อความปลอดภัยของเจ้าของบัญชี" + **ทางไปต่อที่ทำได้จริง**
- **หมายเหตุก่อนกดต้องเป็นข้อความทั่วไป ไม่ระบุเงื่อนไข** — ห้ามเขียนว่า "ถ้าคนนี้อยู่หลายร้าน" หรือ
  "ถ้าคนนี้เป็นเจ้าของร้าน" เพราะข้อความที่ผูกกับ target = คำใบ้ว่าเงื่อนไขไหนตรง
- ห้ามใช้คำว่า "ถูกบล็อก" / "คุณไม่มีสิทธิ์" / "ผู้ใช้นี้อยู่ร้านอื่น" · ห้ามโชว์ `404`/`traceId` ให้ผู้ใช้เห็น
- **ทางไปต่อ = "ติดต่อทีมงาน OmniStock"** (Phase 0 dogfood ยังกู้ด้วยมือได้) — **ห้ามเขียนว่า "ให้เขากดลืมรหัสผ่าน"**
  เพราะระบบยังไม่มีปุ่มนั้นจนกว่าจะมี F-081 (UI ห้ามสัญญาสิ่งที่ยังไม่มี)

**หมายเหตุสำคัญ: ร้านที่มีเจ้าของร้านคนเดียว (ราคาที่ D-030 รับไว้)**
- ผลจริง: ตั้งแต่ D-030 **ไม่มีใครในร้านตั้งรหัสผ่านใหม่ให้เจ้าของร้านได้เลย** (ผู้ดูแลกดแล้วได้ 404) และเวอร์ชันนี้
  ยังไม่มี self-serve reset ⇒ ร้านที่มีเจ้าของคนเดียวแล้วลืมรหัส = ต้องให้ทีมงานกู้ให้
- **ทางแก้เชิง UX คือกันไว้ก่อน ไม่ใช่ไปแก้ตอนล็อกไม่ได้** ⇒ วางข้อความชวน "ตั้งเจ้าของร้านสำรอง" ไว้ **2 จุด**
  ที่ผู้ใช้เจอตามธรรมชาติ ก่อนเกิดปัญหา:
  1. **S4 การ์ด "เริ่มต้นใช้งาน" ข้อ 3** — เคสร้านเพิ่งสร้าง (เจ้าของร้าน + `counts.activeMembers === 1`) · §5
  2. **S6 แถบ info บนสุดของจอสมาชิก** — เคสร้านที่โตแล้วแต่ยังมีเจ้าของคนเดียว · §7 ท้ายหัวข้อ
- **ไม่วางไว้ในฟอร์มสร้างร้าน (S2)** — จังหวะนั้นผู้ใช้ยังไม่มีทีมงานให้ตั้งเป็นเจ้าของ ⇒ เตือนไปก็ทำอะไรไม่ได้
  (ได้แค่ความกังวล) · หลังสร้างเสร็จ 1 คลิกก็เจอข้อความเดียวกันในการ์ด "เริ่มต้นใช้งาน" แล้ว

---

## 13. ความต่าง web ↔ mobile (เฉพาะที่ต่างจริง)

| จุด | Web | Mobile |
|---|---|---|
| org ใน context | อยู่ใน **URL** `/o/{orgId}/…` (2 แท็บ 2 ร้านได้) | `activeOrgId` ตัวเดียวในแอป (มีร้านเดียวที่ active) |
| ตัวสลับร้าน | dropdown บน sidebar (tablet: ใน drawer) | แตะชื่อร้านบน AppBar → **bottom sheet** |
| เลือกร้าน (S1) | หน้าเต็ม + การ์ดกึ่งกลาง 480px | route เต็มจอ (guard: `SessionAuthed(active: null)`) |
| สร้างร้าน / เชิญสมาชิก / ข้อมูลผู้เสียภาษี | **dialog** (ยกเว้นสร้างร้าน = หน้าเต็ม) | **full-screen + AppBar มีปุ่ม back** ทุกตัว (พิมพ์บนคีย์บอร์ดมือถือได้เต็มจอ) |
| ยืนยัน (confirm) | modal `ConfirmDialog` | ใช้ `core/ui/confirm_dialog.dart` **ที่มีอยู่แล้ว** (dialog กลางจอ) — เนื้อหา/คำเหมือนกันทุกตัวอักษร · **ไม่บังคับเปลี่ยนเป็น bottom sheet ใน F-002** (pattern sheet เป็นของ F-006) |
| เมนูของแถวสมาชิก | ปุ่ม `⋯` → dropdown | แตะแถว → bottom sheet รายการการกระทำ |
| แผ่นลิงก์คำเชิญ (S8) | dialog + ปุ่ม "คัดลอกลิงก์" | full-screen + ปุ่ม "คัดลอกลิงก์" เต็มความกว้าง · **ปุ่ม "แชร์…" (native share) = optional** ถ้าทำได้โดยไม่เพิ่ม dependency ที่ต้องอนุมัติ — ตัดออกได้ไม่กระทบ AC |
| จอรับคำเชิญ `/invite` | มีจอนี้ (ต้องดีบนเบราว์เซอร์มือถือ) | **ไม่มีในแอป** (deep link = F-006) |
| toast | มุมบนขวา | ล่างจอ เหนือ safe-area |

---

## 14. Accessibility

- ทุกฟิลด์มี `label` ผูกจริง · error inline อ่านผ่าน `aria-live="polite"`
- **ป้ายสถานะ (ใช้งานอยู่ / รอตอบรับ / ถูกถอดแล้ว) ต้องมีข้อความเสมอ** ห้ามสื่อด้วยสีอย่างเดียว
- Focus order S7: อีเมล → รายการสิทธิ์ (arrow key เลื่อนภายในกลุ่ม) → ปุ่มยกเลิก → ปุ่มหลัก
- ทุก confirm dialog: focus trap + Escape = ยกเลิก + **default focus ที่ปุ่มปลอดภัย** ("ยกเลิก" / "ไม่ยกเลิก") · focus ring แสดงเมื่อ `:focus-visible` เท่านั้น ([ui.md §2.7](ui.md))
- ปุ่มไอคอน (เมนูการกระทำของแถว, ปุ่มตา, ปุ่มคัดลอก) มี label สำหรับ screen reader: "การกระทำเพิ่มเติมของ {email}", "แสดงเลขเต็ม", "คัดลอกลิงก์คำเชิญ"
- **tap target ≥ 44px ทุกองค์ประกอบที่กดได้ ทั้ง web และ mobile** (`size.tap-target.min`) — รวมปุ่มเล็กในแถว/ในกล่องสถานะ และปุ่มข้อความ (`tertiary`) · แถวรายการสูงพอแตะ
- ไอคอนไม่สื่อความหมายเดี่ยว ๆ — มีข้อความไทยคู่เสมอ ([ui.md §2.7](ui.md))
- กล่องลิงก์คำเชิญ: `readonly` แต่ **โฟกัส/เลือกข้อความได้** (ผู้ใช้ที่ clipboard API ใช้ไม่ได้ยังคัดลอกเองได้)

---

## 15. ของฝั่งมือถือที่ ux ตัดสินรอบ 2026-09-05 (ยังไม่ implement — `@frontend`)

> ทั้งสองข้อมาจาก manual pass §12.2 · **ux ไม่แก้โค้ดใน `apps/mobile/**` เอง** — ที่นี่คือสเปกให้ frontend ทำต่อ
> · ข้อ 15.1 มีครึ่งหนึ่งที่ **ต้องให้ @product เคาะ** (ระบุไว้ชัดในข้อ)

### 15.1 การ์ดภาษี: เลขที่เปิดอยู่ ควร "ซ่อนตัวเอง" เมื่อไหร่

**คำตอบ: ซ่อนเมื่อจอไม่ได้อยู่กับผู้ใช้แล้ว — ไม่ใช่เมื่อเวลาผ่านไป**

| เหตุการณ์ | ทำอะไร | ทำไม |
|---|---|---|
| แอปออกจาก foreground (`paused` / `inactive`) รวมถึงเปิด recents / สลับแอป | **ทิ้งค่าทันที** กลับเป็น mask + ปุ่ม "แสดงเลขเต็ม" | `FLAG_SECURE` กันภาพหน้าจอได้จริง (พิสูจน์แล้วบน Android 13) แต่ **กันคนที่ยืนอยู่ข้างหลังไม่ได้** และไม่กันตอนยื่นเครื่องให้คนอื่นดูอย่างอื่น |
| ออกจากจอนั้น (pop route / สลับแท็บ) | ทิ้งค่า | ค่าอยู่ใน memory ของจอ — ไม่ควรมีอายุยืนกว่าจอ |
| ล็อกอินหมดอายุ / สลับร้าน | ทิ้งค่า | เลขผูกกับร้าน ไม่ใช่กับเครื่อง |
| **นั่งดูอยู่เฉย ๆ (idle timer)** | **ยังไม่ทำในเวอร์ชันนี้ — ต้องให้ @product เคาะ** | ดูด้านล่าง |
| กลับเข้าแอป | ขึ้นเป็น mask เสมอ · **ไม่มี toast/คำอธิบายว่า "ซ่อนให้แล้ว"** | ปุ่มที่กลับไปเป็น "แสดงเลขเต็ม" อธิบายตัวเองอยู่แล้ว · การเด้งข้อความบอกทุกครั้งที่สลับแอปคือการรบกวนโดยไม่เพิ่มความปลอดภัย |

- ⛔ **ห้ามใส่คำเตือนล่วงหน้าว่า "ระบบจะซ่อนเลขอัตโนมัติ"** — เพิ่มตัวอักษรบนจอโดยที่ผู้ใช้ไม่ต้องทำอะไรกับมัน
  · บรรทัดความจริงที่มีอยู่แล้ว ("การกดดูเลขเต็มถูกบันทึกไว้เพื่อความปลอดภัยของร้าน") พูดเรื่องที่สำคัญกว่าไปแล้ว
- **ทำไมไม่เอา idle timer (และทำไมต้องให้ product ตัดสิน):** การกดดูใหม่ทุกครั้ง = **ยิง `reveal` ใหม่ + audit event
  ใหม่ + กิน quota 20 ครั้ง/ชม.** ⇒ timer สั้น ๆ จะทำให้ทั้ง log และ quota บวมโดยที่ความปลอดภัยแทบไม่เพิ่ม
  (จอที่เปิดค้างอยู่ = อยู่ในมือเจ้าของ ซึ่งเคสนี้ครอบด้วยข้อ "ออกจาก foreground" ไปแล้ว) ·
  แต่ถ้า **@product** มองว่าเป็นข้อกำหนดเชิง compliance ต้องมี ให้เคาะ 2 เรื่องพร้อมกัน: **กี่วินาที** และ
  **quota 20 ครั้ง/ชม. ต้องขยับตามไหม** — ux ไม่ตัดสินสองข้อนี้ฝ่ายเดียว
- **web ใช้กติกาเดียวกัน** เท่าที่แพลตฟอร์มมีให้: ซ่อนเมื่อแท็บถูกซ่อน (`visibilitychange` → `hidden`) และเมื่อออกจากหน้า

### 15.2 มือถือ: "ต่อ API ไม่ติด" ต้องพูดว่าต่อไม่ติด

**อาการ (B-17):** จอ login รายงานว่า *"เกิดข้อผิดพลาด กรุณาลองใหม่อีกครั้ง"* ทั้งที่ความจริงคือแอปยิงไปหา
`localhost` ของตัวเครื่องเอง ⇒ ไม่เคยถึง server เลย · `errorNetwork` มีอยู่ใน ARB มาตลอดและ**ไม่เคยถูกใช้บนเส้นนี้**
เพราะ `login_controller.dart` มี `catch (_)` ที่กลืน exception ทุกชนิดลงข้อความเดียว

**ตัดสิน:**

| ชั้น | ต้องพูดว่าอะไร |
|---|---|
| ต่อไม่ติด / timeout / DNS / TLS (ไม่เคยถึง server) | **"เชื่อมต่อไม่สำเร็จ ตรวจสอบอินเทอร์เน็ตแล้วลองใหม่อีกครั้ง"** |
| 5xx (ถึง server แล้วฝั่งเราพัง) | "ระบบขัดข้องชั่วคราว กรุณาลองใหม่อีกครั้ง" |
| 401 / 429 / อื่น ๆ | copy เดิมของ F-001 (enumeration-safe 401 · `ThrottleBanner` 429) — **ไม่เปลี่ยน** |

- **ข้อความชั้นบนสุดใช้คำเดียวกับเว็บทุกตัวอักษร** (§1.5) — design-system §1 ห้าม web↔mobile drift และนี่คือ
  ประโยคที่ผู้ใช้คนเดียวกันจะเจอทั้งสองที่ · ⇒ **`errorNetwork` ใน `app_th.arb` ต้องแก้จาก "เชื่อมต่อไม่ได้
  กรุณาลองใหม่" เป็นประโยคของเว็บ** (`@frontend` เป็นคนแก้ ARB — ux ไม่แตะ `apps/mobile/**`)
- **วิธี implement ที่ทำให้มันไม่กลับมาเป็นเหมือนเดิม:** เลิก `catch (_)` แล้วใช้ `failureMessage(t, failure)`
  บน `ApiFailure` ที่ `error_mapping_interceptor` map มาให้แล้ว (`NetworkFailure() => t.errorNetwork` มีอยู่แล้ว
  ใน `core/error/api_failure.dart`) ⇒ ทุกจอได้ taxonomy เดียวกันฟรี ไม่ต้องไล่แก้ทีละจอ
- **นี่เป็นเรื่อง UX ไม่ใช่เรื่อง debug ของ dev:** แม่ค้าที่เน็ตหลุดกับแม่ค้าที่เจอระบบเราล่ม **ต้องทำคนละอย่าง**
  (คนแรกไปเปิด wifi ใหม่ คนที่สองรอ) · ข้อความเดียวที่ครอบทั้งสองกรณีแปลว่า "ไม่บอกอะไรเลย"

---

## ตอบ consult questions → @backend-api

> ตอบครบทุกข้อที่ api-spec §6 ถามฝั่ง ux — พร้อมให้ backend lock contract

**Q1 — จอ "สมาชิก" รวมหรือแยก?**
**จอเดียว 2 ส่วนบนหน้าเดียวกัน (ไม่ใช่ 2 แท็บ) และ *ไม่ต้อง* ทำ endpoint รวม — ใช้ 2 endpoint ตามที่ร่างไว้**
- FE ไม่ merge สองแหล่งเป็น list เดียว → render เป็น 2 ส่วนซ้อนกัน (คำเชิญที่รอตอบรับ ↑ / สมาชิกในร้าน ↓) แต่ละส่วนมี cursor + สถานะโหลด/ผิดพลาดของตัวเอง
- ไม่เลือกแท็บเพราะ "คำเชิญที่ค้าง" คือ **งานที่ต้องตามต่อ** — ซ่อนหลังแท็บแล้วผู้ใช้ลืม (แม่ค้าส่งลิงก์ผิดคน/ลิงก์หายแล้วไม่รู้ว่าต้องมาออกใหม่ที่ไหน)
- ไม่เลือก endpoint รวมเพราะสองแหล่งมี **สถานะและการกระทำคนละชุดจริง ๆ** (ออกลิงก์ใหม่/ยกเลิกคำเชิญ vs เปลี่ยนสิทธิ์/ถอด) รวมแล้วจะได้ row polymorphic ที่ทั้ง FE/BE ต้องแตกเคสอยู่ดี · ทั้งสองเส้นต้องมี `manage_members` เหมือนกันแล้ว จึงไม่มีปัญหาเรื่องสิทธิ์

**Q4 — field ที่จอต้องการเพิ่ม**
- **ไม่ขอชื่อ-นามสกุล** — F-002 แสดง **อีเมล** เป็นตัวระบุคน + ป้าย "(คุณ)" ก็พอสำหรับทุก AC ⇒ **ไม่ต้อง escalate product**, ไม่ต้องแตะโมเดล `User`
- **ขอ 1 field แบบ "มีก็ดี" (ไม่บล็อก):** `key`/slug ของ role (เช่น `owner|admin|staff`) บน `RoleRow` และ `roleKey` บนแถว `MemberRow`/`Invitation`
  - เหตุผล: UI **ห้ามโชว์คำอังกฤษ "Owner/Admin/Staff"** ให้ SME (กฎ jargon) ต้อง map เป็น "เจ้าของร้าน/ผู้ดูแล/พนักงาน" · map จาก `roleName` ก็ทำได้ แต่เปราะเมื่อ F-003 เปิดให้เปลี่ยนชื่อ role
  - **fallback ถ้าไม่ให้:** map จาก `roleName` ของ 3 role ที่ระบบ seed + role อื่น (custom, F-003) แสดงชื่อที่ผู้ใช้ตั้งเองตรง ๆ — ทำงานได้ ไม่บล็อก build
- **ไม่ขอ** `invitedByEmail` (จึงไม่แสดง "เชิญโดยใคร" ใน F-002) · **ไม่ขอ** จำนวน pending ที่เหลือใน `INVITATION_LIMIT_REACHED` (copy เขียนแบบไม่ใช้ตัวเลขได้)
- ใช้ field ที่มีอยู่แล้วครบ: `isMe`/`isOwner` (ซ่อนปุ่ม), `counts` (จอข้อมูลร้าน), `cancelledInvitations` (toast), `acceptedUserCreatedAfterInvite` (ธงเตือนเบา ๆ), `details.limit`/`details.invitationId`/`details.emailMasked`

**Q5 — สร้าง org แล้วพาเข้าเลยไหม**
**พาเข้าร้านใหม่ทันที** — ยืนยันว่า response 201 พอ ไม่ต้องยิง `GET /me/organizations` ซ้ำ · client seed cache ของร้านนั้นจาก response แล้ว **invalidate** รายการร้าน (ให้ตัวสลับร้านถูกต้องรอบหน้า) · ปลายทาง Phase 0 = `/o/{orgId}` (จอ "ข้อมูลร้าน" ทำหน้าที่หน้าแรกจนกว่า F-030 dashboard จะมา)

**Q6 — copy ของ 403 ทั้ง 2 แบบ (ยืนยันว่าแยก handler)**
- (ก) `403 ORG_ACCESS_DENIED` → **พากลับหน้าเลือกร้าน + refetch `/me/organizations`** + แถบเตือนสีเหลือง:
  > "คุณไม่ได้เป็นสมาชิกของร้าน "{ชื่อร้าน}" แล้ว — เลือกร้านอื่นด้านล่าง หรือติดต่อเจ้าของร้านถ้าคิดว่าไม่ถูกต้อง"
  (ถ้าไม่รู้ชื่อร้าน ใช้ "ร้านนี้") · **ห้าม logout**
- (ข) `403 FORBIDDEN` → **อยู่หน้าเดิม** · action = toast:
  > "คุณไม่มีสิทธิ์ทำรายการนี้ ติดต่อเจ้าของร้านให้เปิดสิทธิ์ให้"
  · route-level (เข้าตรงผ่าน URL) = `ForbiddenPanel`: "หน้านี้เปิดให้เฉพาะผู้ที่ดูแลทีมงาน" + ปุ่ม "กลับหน้าแรกของร้าน"
  · เคส Owner-only ใช้ copy เจาะจง: "เฉพาะเจ้าของร้านเท่านั้นที่ตั้งหรือแก้สิทธิ์ของเจ้าของร้าน"
- **ยืนยัน: client จะไม่รวมสอง code นี้เป็น handler เดียว** — และ `422 ORG_MISMATCH`/`ORG_CONTEXT_REQUIRED` ถือเป็นบั๊ก client (toast ทั่วไป) **ห้ามเตะผู้ใช้ออกจากร้าน**

**Q7 — จอ tax profile มีโหมดแก้ไขไหม**
**มี** — S4 = โหมดดู (mask ไว้ก่อน + ปุ่ม "แสดงเลขเต็ม") · S5 = ฟอร์มกรอก/แก้ (PUT ทั้งชุด, โหลดค่าเดิมมาให้ครบ) · ถ้าเคยประกาศแล้ว มี confirm ก่อนบันทึกทับ ("ร้าน 1 ร้านมีข้อมูลผู้เสียภาษีได้ชุดเดียว ข้อมูลใหม่จะแทนที่ของเดิม") · ไม่ต้องเพิ่ม endpoint

**Q13 — Staff เห็นบล็อกภาษี/เมนูสมาชิกยังไง**
- **บล็อกภาษี: ไม่แสดงตัวเลขเลย แม้แต่ 4 ตัวท้าย** — แสดงแค่ "ร้านนี้ประกาศข้อมูลผู้เสียภาษีแล้ว · จดทะเบียน VAT / ไม่ได้จดทะเบียน" หรือ "ยังไม่ได้ประกาศ" + บรรทัดรอง "รายละเอียดเปิดให้เฉพาะผู้ที่ดูแลข้อมูลร้าน"
  (เหตุผล: 4 ตัวท้ายของบุคคลธรรมดา = 4 ตัวท้ายเลขบัตรประชาชน ซึ่งยังใช้ยืนยันตัวตนได้ในหลายบริการ และไม่มีประโยชน์ต่องานของพนักงานเลย — `taxIdMasked` ยังส่งมาได้ตามสัญญาเดิม UI แค่ไม่ใช้)
  · client ใช้ `taxProfileComplete` ตัดสิน "ประกาศแล้วหรือยัง" **ไม่ใช้การมี/ไม่มี field `taxId`**
- **เมนู "สมาชิก": ซ่อน** เมื่อ `capabilities` ไม่มี `manage_members` (ยืนยันทางที่ backend เอนไป) + **มี `ForbiddenPanel` เมื่อเข้าตรงผ่าน URL** (§12.2)

**Q14 — สัญญาณอายุลิงก์ 24 ชม.**
- **ตอนเลือกสิทธิ์ (ก่อนสร้าง):** แถบเตือนเบา ๆ เฉพาะเมื่อเลือกสิทธิ์ระดับสูง — "สิทธิ์ระดับนี้ ลิงก์คำเชิญจะมีอายุสั้นกว่าปกติเพื่อความปลอดภัย" (**ไม่มีตัวเลข** เพราะยังไม่รู้ค่าจริง)
- **ตอนได้ลิงก์ (S8) และบนแถวคำเชิญ (S6):** แสดงจาก `expiresAt` เสมอ 2 ชั้น — "ลิงก์ใช้ได้ถึง 29 ก.ค. 2026, 14:30 (อีกประมาณ 24 ชั่วโมง)"
- **สัญญาบังคับกับ frontend:** ห้ามมีสตริง "7 วัน"/"24 ชั่วโมง" ตายตัวใน copy ทุกจุด — ทุกตัวเลขคำนวณจาก `expiresAt` (คำเชิญที่เลย `expiresAt` แสดงป้าย "หมดอายุแล้ว" ทันทีโดยไม่ต้องรอ server เปลี่ยน status)

**Q15 — copy ตอน admin reset ล้มเหลวโดยไม่เผยความจริง** *(อัปเดตหลัง D-030 — ครอบ 2 เคส)*
- **หมายเหตุตั้งความคาดหวังในกล่องก่อนกด (ทั่วไป ไม่ระบุเงื่อนไข):** "บางบัญชีระบบจะไม่ให้คุณตั้งรหัสผ่านใหม่ให้ เพื่อความปลอดภัยของเจ้าของบัญชี — ถ้าเจอกรณีนั้น ต้องให้เจ้าตัวติดต่อทีมงาน OmniStock เอง"
- **ข้อความเมื่อได้ 404 (ทุกเคส ข้อความเดียวกันทุกตัวอักษร):** "ตั้งรหัสผ่านใหม่ให้บัญชีนี้ไม่ได้ เพื่อความปลอดภัยของเจ้าของบัญชี — ให้เจ้าตัวติดต่อทีมงาน OmniStock เพื่อขอความช่วยเหลือ"
- ห้ามใช้คำว่า "ถูกบล็อก"/"ไม่มีสิทธิ์"/"ผู้ใช้นี้อยู่ร้านอื่น"/"ผู้ใช้นี้เป็นเจ้าของร้าน" (จะกลายเป็น oracle) · ห้ามเขียนว่า "เกิดข้อผิดพลาด ลองใหม่" (โกหก — ระบบไม่ได้พังและลองใหม่ก็ไม่ได้ผล)
- **+ ราคาที่ผู้ใช้ต้องรู้ล่วงหน้า (D-030):** ร้านที่มีเจ้าของร้านคนเดียวกู้รหัสเองไม่ได้ ⇒ ชวนตั้งเจ้าของร้านสำรองที่ S4/S6 · จอจริงเป็นของ F-004 — copy ชุดนี้ฝากไว้ที่ §12.3 พร้อม i18n key ใน [ui.md](ui.md)

---

## UX self-review (ux-heuristic-review) — 2026-07-28

| # | จอ/จุด | Finding | Fix ที่ทำแล้ว |
|---|---|---|---|
| 1 | ทุกจอ | ร่างแรกใช้คำว่า "องค์กร" ตาม spec = ศัพท์ระบบ ไม่ใช่คำที่แม่ค้าใช้ | ตั้ง glossary §1.3 ใช้คำว่า **"ร้าน"** คำเดียวทั้งฟีเจอร์ + ห้ามใช้คำอื่น |
| 2 | S1/S3 | ร่างแรกแสดง `tierLabel` ("Full (comp)") ในตัวสลับร้าน = jargon อังกฤษดิบบนจอ SME | ตัด plan/tier ออกจาก UI ของ F-002 ทั้งหมด (ยกให้ F-007 ที่มี copy ของ tier) |
| 3 | S6 | ร่างแรกทำ 2 แท็บ (สมาชิก / คำเชิญ) → คำเชิญค้างถูกซ่อน ผู้ใช้ลืมตามงาน | เปลี่ยนเป็นจอเดียว 2 ส่วนซ้อน คำเชิญอยู่บน + ซ่อนส่วนเมื่อไม่มีคำเชิญค้าง (= คำตอบ Q1) |
| 4 | S8 | ปิดแผ่นลิงก์ทิ้งโดยยังไม่คัดลอก = ผู้ใช้เสียลิงก์ถาวร (ดูซ้ำไม่ได้ตาม D-027) | ~~เพิ่ม confirm "ยังไม่ได้คัดลอกลิงก์"~~ → **user เคาะ 2026-07-28: ตัด dialog ออก** (2 ชั้นมากเกินไป) · แทนด้วยแถบเตือน tone warning เหนือปุ่มปิด + **ปุ่มปิดเป็นปุ่มรองจนกว่าจะคัดลอกเสร็จ** (§9.1) |
| 5 | S8/S6 | ร่างแรกเขียน "ลิงก์มีอายุ 7 วัน" ตาม AC เดิม → ผิดกับ TTL 24 ชม.ของ role สูง | ทุก copy อ่านจาก `expiresAt` · ห้าม hardcode ตัวเลข · เพิ่มแถบเตือนแบบไม่มีตัวเลขตอนเลือกสิทธิ์สูง (Q14) |
| 6 | S7 | ซ่อนตัวเลือก "เจ้าของร้าน" เมื่อผู้เชิญไม่ใช่ Owner → ผู้ใช้งงว่าทำไมไม่มี | เปลี่ยนเป็น **disable + บอกเหตุผล** "เฉพาะเจ้าของร้านเท่านั้นที่ตั้งเจ้าของร้านคนใหม่ได้" |
| 7 | S7 | เจอ `409 INVITATION_PENDING` แล้วเป็นทางตัน (ได้แค่ error) | เปลี่ยนกล่องเป็นแผงทางเลือก: ออกลิงก์ใหม่ / ยกเลิกคำเชิญเดิม / กลับไปแก้อีเมล (D-027) |
| 8 | S10 | confirm เดิมเขียน "แน่ใจหรือไม่" — ไม่บอกผลจริง | เขียนผลลัพธ์เป็นข้อ ๆ (เข้าไม่ได้ทันที · ประวัติยังอยู่ · คำเชิญค้างถูกยกเลิก · กลับมาได้ต้องเชิญใหม่) |
| 9 | S4 | ร่างแรกให้ Staff เห็น `taxIdMasked` 4 ตัวท้าย = เผยส่วนท้ายเลขบัตร ปชช. โดยไม่จำเป็น | Staff เห็นเฉพาะสถานะ + จด VAT ไหม ไม่มีตัวเลขใด ๆ (Q13) · ผู้มีสิทธิ์ก็ยัง mask ไว้ก่อน + ปุ่ม "แสดงเลขเต็ม" |
| 10 | S11 | `/invite` ถูกออกแบบแบบ desktop-first ตาม design-system §8 แต่ลิงก์ถูกส่งทาง LINE → เปิดบนมือถือแทบทั้งหมด | ประกาศเป็น **ข้อยกเว้นเดียว** ของ §8: จอนี้ต้องดีบนเบราว์เซอร์มือถือ (บันทึกใน ui.md §5 ด้วย) |
| 11 | S11 | ร่างแรก auto-accept หลังสมัครเสร็จ | ให้ผู้ใช้ **กดรับเอง** เสมอ (ต้องเห็นว่ากำลังเข้าร้านอะไร สิทธิ์อะไร) |
| 12 | S12 | ร่างแรกใช้ `ErrorBanner` แดงกับเคสถูกถอด | เปลี่ยนเป็นแถบเหลือง (warning) — ผู้ใช้ไม่ได้ทำผิด และยังใช้ระบบต่อได้ · ย้ำห้าม logout |
| 13 | S2 | ถามเขตเวลา/สกุลเงินในฟอร์มสร้างร้าน = คำถามที่ไม่จำเป็น | ตัดออก เหลือฟิลด์เดียว + กล่องอธิบายว่าระบบตั้งอะไรให้บ้าง (default ฉลาด) |
| 14 | S4 | ออกแบบปุ่มอัปโหลดโลโก้ร้านไว้ ทั้งที่ contract รับได้แค่ `null` จนถึง F-040 | ตัดออก ใช้ตัวย่อชื่อร้านเป็น avatar (UI ไม่สัญญาสิ่งที่ระบบทำไม่ได้) |
| 15 | ทุก list | ไม่ได้ระบุพฤติกรรมเมื่อโหลด 2 แหล่งบนจอเดียวแล้วพังแหล่งเดียว | ระบุ error แยกต่อส่วน + "ลองใหม่" เฉพาะส่วนนั้น (อีกส่วนยังใช้ได้) |

**รอบเพิ่มเติม 2026-07-28 (หลัง amend #4 ของ contract + D-029/D-030):**

| # | จอ/จุด | Finding | Fix ที่ทำแล้ว |
|---|---|---|---|
| 16 | S13 | หมายเหตุก่อนกดเดิมผูกกับเคสเดียว ("ถ้าพนักงานคนนี้ใช้บัญชีกับร้านอื่นด้วย") — พอมีเคสที่สอง (D-030 target = เจ้าของร้าน) ข้อความจะ **ไม่ตรงความจริง** และถ้าเขียนแยกเคสจะกลายเป็นคำใบ้ว่าเงื่อนไขไหนตรง | เขียนใหม่เป็นข้อความ **ทั่วไปชุดเดียว** ("บางบัญชี…เพื่อความปลอดภัยของเจ้าของบัญชี") + ข้อความล้มเหลวเหมือนกันทุกตัวอักษร (§12.3) |
| 17 | S13 | ทางไปต่อเดิม "ให้เขาขอความช่วยเหลือด้วยตัวเอง" = คลุมเครือ (ขอกับใคร?) และเสี่ยงชี้ไปหาปุ่ม "ลืมรหัสผ่าน" ที่ **ยังไม่มีในระบบ** | ระบุปลายทางจริง "ติดต่อทีมงาน OmniStock" + ห้าม UI สัญญา self-serve reset จนกว่าจะมี F-081 |
| 18 | S4/S6 | D-030 ทำให้ร้านที่มีเจ้าของคนเดียว **กู้บัญชีเองไม่ได้** แต่ไม่มีจุดไหนบอกผู้ใช้ **ก่อน** เกิดเรื่อง | เพิ่มการชวนตั้งเจ้าของสำรอง 2 จุด (การ์ดเริ่มต้นใช้งาน §5 · แถบ info จอสมาชิก §7) ด้วย tone info + ปุ่มที่ทำได้จริง · ไม่เตือนตอนสร้างร้าน (ทำอะไรไม่ได้ = ขู่เปล่า) |
| 19 | S4/S5 | ร่างเดิมเขียนว่า "แสดงเลขเต็ม = เผยค่าที่ server ส่งมา" — ผิดกับ contract แล้ว (TIN เต็มมาทาง `POST …/tax-profile/reveal` เท่านั้น + จำกัด 20 ครั้ง/ชม.) | เขียนพฤติกรรมใหม่: ยิง reveal ทุกครั้ง · ซ่อน = ทิ้งค่า · เพิ่ม copy ของ 429/404/403/network · ฟอร์ม S5 ที่ขอเลขเดิมไม่ได้ **ต้องไม่ตัน** (พิมพ์ใหม่ได้) |
| 20 | ทุกจอที่เขียนข้อมูล | `409 CONFLICT` + `details.reason="busy"` (arch §5.2) ยังไม่มี copy → ผู้ใช้จะเห็นข้อความดิบ/คิดว่าระบบพัง | เพิ่มข้อความกลาง §1.4 (ชั่วคราว + ลองใหม่ + ไม่ล้างฟอร์ม) + กติกา fallback เมื่อไม่มี `reason` |
| 21 | S10.3 | copy "ออกจากร้านนี้" ยังอิง endpoint เดิม (`DELETE …/members/{userId}` ที่ต้องมี `manage_members`) ⇒ พนักงานหาไม่เจอ | ผูกกับ `DELETE /orgs/{orgId}/membership` (D-029) + ย้ายทางเข้าหลักไปท้ายจอ S4 ที่ทุกคนเห็น + เพิ่ม copy `409 LAST_OWNER` ของเคสออกเอง |

**รอบที่ 3 — 2026-07-28 (หลัง user รีวิว mockup `design-system/mockup/f002-org.html`):**

> **รากของทั้ง 6 ข้อเป็นข้อเดียวกัน:** ผมประกาศ contribute-back ไว้แค่ component 7 ตัว + token 5 ตัว
> แต่ระหว่างทำ mockup สร้าง variant/ขนาด/สถานะขึ้นใช้อีกชุดโดยไม่ประกาศ (`btn-sm` · `tlink` · `banner.info` · `focusring`)
> ⇒ ค่าที่ใช้กลายเป็นการเดาของ mockup และไม่มีใครเป็นเจ้าของ = ขัด DS §6 ที่ผมเป็นเจ้าของกฎเอง · แก้ที่ต้นเหตุใน [ui.md §7](ui.md)

| # | จอ/จุด | Finding (ค่าที่วัดได้จริง) | Fix ที่ทำแล้ว |
|---|---|---|---|
| 22 | ทุกกล่องสถานะ | ปุ่มใน `ErrorBanner` เป็น outline โทนกลาง (ตัวอักษร `#112320` + ขอบเทา) บนพื้นแดง ⇒ ดูเหมือนของที่ลอยมาจากที่อื่น | **ปุ่มในกล่องสถานะใช้โทนของกล่องนั้น** (ตัวอักษร `<tone>.text` · ขอบ `currentColor` 65%) + กติกาว่าเมื่อไหร่ใช้ปุ่ม fill ได้ — [ui.md §2.6](ui.md) |
| 23 | S8-3 dialog | focus ring ถูกโชว์ค้างถาวรใน mockup ⇒ อ่านแล้วเหมือนมีปุ่มหลัก 2 ปุ่มแข่งกัน (ทั้งที่ค่าขอบปุ่มถูกตาม DS §1.1c) | กำหนด `focus.ring.w/.offset/.color` + **`:focus-visible` เท่านั้น + ห้ามโชว์ค้าง/ห้ามใช้แทนปุ่มเด่น** · mockup เปลี่ยนเป็นภาพ "สถานะพัก" คู่กับ "ภาพจำลอง `:focus-visible`" ที่ติดป้ายกำกับชัด |
| 24 | ทุกแถบ info | `Banner` ประกาศไว้ว่ามี 4 tone แต่ **tone `info` ไม่เคยมี token** — mockup ใช้ `surface.muted`/`color.text` ไปเงียบ ๆ | เพิ่ม role `color.info.bg/.border/.text` ทั้ง light+dark (ค่าคงเดิม แต่**มีชื่อและมีเจ้าของ**) + บันทึกว่าทำไมไม่ใช้สีฟ้า |
| 25 | ทุกจอ | ไอคอนบนจอเป็น **emoji/ตัวอักษร 9 ตัว** และ DS **ไม่มีนโยบายไอคอนเลย** — emoji เรนเดอร์ต่างกันทุก OS, สื่ออารมณ์ผิด, ทำ `currentColor` ไม่ได้, ไม่มีคู่ใน Flutter | เขียนนโยบายไอคอนเต็ม (outline เท่านั้น · grid 24/stroke 2 · 4 ขนาด · `currentColor` · ห้ามฝังสัญลักษณ์ใน i18n string) เป็น **§1.6 ใหม่ของ DS** · mockup เปลี่ยนเป็น inline SVG outline ครบทุกตัว · **ชุดไอคอนจริง = 3 ตัวเลือกรอ user เคาะ** |
| 26 | ทุกจอ | `tlink` สูง **31px** สี `text.muted` ไม่มีขอบ/ไม่มีเส้นใต้ ⇒ ไม่มีสัญญาณว่ากดได้ และไม่ผ่าน tap target | ประกาศเป็น **`Button variant="tertiary"`** สี `color.primary` เสมอ + เส้นใต้ตอน hover/focus + สูง 44px · แยกให้ชัดจาก `Link` ในเนื้อความ (ที่ต้องขีดเส้นใต้ตลอด) |
| 27 | **ทั้งจอ (ของแถมที่ผมพลาดเอง)** | **27 องค์ประกอบที่กดได้ต่ำกว่า 44px**: `btn-sm` 20 ตัว (40px) + `tlink` 8 ตัว (31px) — F-002 tag `both` และ `/invite` ต้องดีบนมือถือ ⇒ เป็นปัญหาใช้งานจริง | นิยาม `Button size="sm"` ใหม่: **แน่นที่ตัวอักษร/ระยะข้าง ไม่ใช่เตี้ยลง — สูง ≥44px เสมอทุก platform** · **ปฏิเสธ**ทางเลือก "ยกเว้นบริบท pointer-only" (web รองรับ tablet ที่เป็น touch + Flutter ใช้ token ชุดเดียวกัน ⇒ ข้อยกเว้นจะรั่ว) · เพิ่ม "นับ tap target" เข้าเช็คลิสต์ก่อน sign-off mockup |

**Persona walkthrough (แม่ค้าไม่เคยใช้ ERP, มือถือเป็นหลัก, ใจร้อน):**
เปิดแอปครั้งแรก → S1 ว่าง → ปุ่มเดียวชัด "สร้างร้านใหม่" → กรอกชื่อร้านอย่างเดียวจบ → เข้าร้านเจอการ์ด "เริ่มต้นใช้งาน"
บอกสิ่งที่ควรทำต่อ (ชวนทีม / กรอกข้อมูลภาษี / **ตั้งเจ้าของร้านสำรอง** — ข้อหลังพูดด้วยน้ำเสียงแนะนำ ไม่ใช่ขู่ ตาม D-030) — ไม่ต้องอ่านคู่มือ · จุดที่ **เคย**สะดุดคือคำเชิญ: เธอคาดว่าระบบจะส่งอีเมลให้
จึงเพิ่มแถบ "ระบบยังไม่ส่งอีเมลให้อัตโนมัติ — คุณจะได้ลิงก์ไว้ส่งเอง" **ก่อนกด** ไม่ใช่หลังกด และแผ่นลิงก์บอกชัดว่าแสดงครั้งเดียว
โดยวางแถบเตือนไว้ **เหนือปุ่มปิด** + ทำให้ปุ่มปิดยังไม่เด่นจนกว่าจะคัดลอกเสร็จ (แทนการเด้ง dialog ถามซ้ำ ซึ่งเธอจะกดผ่านโดยไม่อ่านอยู่ดี) ·
จุดที่สอง: ตอนถูกถอดกลางคัน เธอจะตกใจว่า "แอปเสีย/โดนแบน" → copy บอกตรง ๆ ว่าเกิดอะไรและยังใช้ร้านอื่นได้

**Unresolved (บอกตรง ๆ) — สถานะหลัง D-029/D-030:**
1. ~~"พนักงาน" ออกจากร้านเองไม่ได้~~ → **ปิดแล้วด้วย D-029**: มี `DELETE /orgs/{orgId}/membership` ที่สมาชิก active ทุกคนเรียกได้ ⇒ ทางเข้าหลักย้ายไปอยู่ท้ายจอ S4 (§5) ซึ่งพนักงานเห็น · รายละเอียด §10.3
2. ~~คำว่า "ร้าน" vs "องค์กร"~~ → **ปิดแล้วด้วย D-029 ข้อ 1** (UI ใช้ "ร้าน", identifier ยังเป็น `organization`)
3. ~~`roles` ไม่มี slug/key~~ → **ปิดแล้ว**: contract ให้ `roleKey` คู่กับ `roleName` ทุกที่ (custom role = `null` ⇒ UI ต้อง fallback เป็น `roleName` และ **ห้ามใช้ `roleKey` ตัดสินสิทธิ์**)

---

## รอบที่ 4 — 2026-09-05 (หลัง manual pass §12.2 บนสแตกจริง + COPY GAP ที่ frontend มาร์กไว้)

> **รากของรอบนี้ต่างจากรอบก่อน ๆ:** 3 รอบแรกเจอจากการ*อ่าน* spec/mockup · รอบนี้เจอจากการ **เดินจอจริงด้วยมือ**
> ⇒ ของที่เจอไม่ใช่ "คำผิด" แต่เป็น **ของที่ถูกบนกระดาษแล้วผิดตอนใช้** — ป้ายที่ไม่ตรงกับของข้างใน,
> ปุ่มที่กดแล้วไม่เกิดอะไร, ปุ่มที่กดแล้วกลับไม่ได้ · ทั้งหมดเป็นชนิดที่ review เอกสารจับไม่ได้เลย
>
> **บทเรียนที่ควรติดตัวไปทุก feature:** จอที่มี **filter/toggle** ต้องตอบ 3 คำถามเสมอ —
> (1) ป้ายบอกครบไหมว่าเปิดอะไรออกมา (2) กลับได้ไหม (3) กดแล้วไม่มีอะไรโผล่ จอพูดว่าอะไร ·
> และ **CTA ทุกตัวต้องรู้ว่าตัวเองถูกเปิดมาจากไหน** ก่อนจะสัญญาว่าจะพาไปไหน

| # | จอ/จุด | Finding (จาก manual pass) | Fix |
|---|---|---|---|
| 28 | S6 คำเชิญ | ปุ่ม "ดูคำเชิญที่หมดอายุ/ยกเลิกแล้ว" เปิด `?status=all` ซึ่ง **รวมคำเชิญที่ถูกรับไปแล้ว** — และแถวพวกนั้นคือที่เดียวที่ธง `acceptedUserCreatedAfterInvite` โผล่ ⇒ **คำเตือนความปลอดภัยข้อเดียวของฟีเจอร์อยู่หลังปุ่มที่คนเชิญไม่มีเหตุผลจะกด** | ป้ายปุ่ม → **"ดูคำเชิญทั้งหมด รวมที่รับแล้ว"** (ตรงกับหัวข้อ "คำเชิญทั้งหมด" ที่จะเจอข้างใน) · ส่วนการย้ายธงไปที่แถว*สมาชิก* ต้องมี field ใหม่ ⇒ **escalate @backend-api/@product** (บันทึกในกล่องเตือน §7) |
| 29 | S6 ทั้งจอ | ปุ่มขยาย filter เป็น **ทางเดียว** — กดแล้วปุ่มหายไปเอง กลับไม่ได้ทั้ง session | toggle 2 ทางถาวร + ป้ายขากลับอยู่ใน copy table (§7) · สมาชิกใช้คู่ แสดง ⇄ ซ่อน ที่ wireframe เขียนไว้ตั้งแต่แรก |
| 30 | S6 ทั้งจอ | กดขยาย filter แล้วไม่มีของ = **จอเงียบสนิท** (ส่วนถูกซ่อนเพราะลิสต์ว่าง + ปุ่มก็หายไป) ⇒ อ่านว่า "กดแล้วไม่ทำงาน" | ต้องพูดว่าไม่มี — เพิ่ม empty 2 บรรทัดในตาราง States · เลือกคำตามความจริงของ filter นั้น ("ร้านนี้ยังไม่เคยมีคำเชิญ" เพราะ `all` รวม pending ด้วย) |
| 31 | S10.3 | CTA ของ `409 LAST_OWNER` ชื่อ "ไปหน้าสมาชิก" ชี้ไป `/settings/members` เสมอ — เปิด dialog จากจอสมาชิกเมื่อไหร่ **กดแล้วไม่เกิดอะไร** | CTA รู้บริบท: จาก S4 = ลิงก์ "ไปตั้งเจ้าของร้านคนใหม่" · จากจอสมาชิก = ประโยคชี้ปุ่มที่มีอยู่จริงบนจอนั้น (§10.3) · ป้ายเปลี่ยนไปบอก**ผลลัพธ์** ไม่ใช่ชื่อหน้า |
| 32 | S6 คำเชิญ | สเปกสั่งปุ่ม "เชิญใหม่อีกครั้ง" ไว้เป็นวลีเดียวใน bullet ⇒ **สร้างไม่ได้จริง** (แสดงบนแถวไหน? สิทธิ์เดิมที่ตั้งไม่ได้แล้วทำยังไง?) และ 5 เดือนผ่านไปก็ยังไม่มีของ | เขียนเป็นตารางสเปกครบ 6 บรรทัด (§7) — แสดงเฉพาะ `expired`/`cancelled`, ห้ามแสดงบน `accepted`, pre-fill แบบไม่ pre-fill ตัวเลือกที่กดส่งไม่ผ่าน, ใช้ error ของ S7 ที่มีอยู่แล้วรับเคสข้อมูลเก่า · **ไม่ขอ field ใหม่** |
| 33 | error ทั้งระบบ | ข้อความสำรอง 5 ตัวใน `errors.ts` ไม่มีเจ้าของ (frontend มาร์ก `‡new`) · 2 ตัวใช้ **ประโยคเดียวกัน**สำหรับ "server เราพัง" กับ "client เราส่งมั่ว" ⇒ แยกไม่ออกทั้งฝั่งผู้ใช้และฝั่งเรา · `notFound` เป็นทางตัน (ไม่บอกว่าทำอะไรต่อ) | เพิ่ม **§1.5 ตารางข้อความสำรอง** + กติกา "ต้องบอกให้ได้ว่าเป็นปัญหาของใคร และทำอะไรต่อ" · 5xx = "ระบบขัดข้องชั่วคราว…" (ของเรา) · 404/409 = บอกว่าข้อมูลเปลี่ยนไปแล้ว + ให้โหลดใหม่ |
| 34 | มือถือ (B-17) | จอ login พูดว่า "เกิดข้อผิดพลาด" ตอนที่ความจริงคือ **ต่อ API ไม่ติดเลย** (`catch (_)` กลืนทุกอย่าง) — คนที่เน็ตหลุดกับคนที่เจอระบบล่มต้องทำคนละอย่าง แต่ได้ประโยคเดียวกัน | §15.2: transport failure ต้องพูดว่า "เชื่อมต่อไม่สำเร็จ ตรวจสอบอินเทอร์เน็ต…" **คำเดียวกับเว็บทุกตัวอักษร** + สั่งให้เลิก `catch (_)` แล้วใช้ `failureMessage()` ที่มีอยู่ ⇒ ทุกจอได้ taxonomy เดียวกัน |
| 35 | มือถือ การ์ดภาษี | ค้างจากรอบก่อน: เลขที่เปิดอยู่ควรซ่อนตัวเองไหม | §15.1 — **ซ่อนเมื่อจอไม่ได้อยู่กับผู้ใช้** (ออก foreground / ออกจากจอ / สลับร้าน) ไม่ใช่เมื่อเวลาผ่านไป · **idle timer ยังไม่ทำ** เพราะทุกครั้งที่กดใหม่ = audit event + quota ⇒ ให้ @product เคาะพร้อมกับ "กี่วินาที + ขยับ quota ไหม" |
| 36 | S3 drawer + ออกจากระบบ | copy 4 ตัวไม่เคยมีใน wireframe เพราะของไม่เคยถูกสร้าง (drawer §8.2 · แถว "ออกจากระบบ" B-16) — frontend ต้องเขียนเองแล้วมาร์ก COPY GAP | ทวนแล้ว **ยืนยันทุกคำ** + ย้ายเข้า wireframe §4 พร้อมเหตุผล (ทำไม aria-label ต้องเป็นกริยา, ทำไมล้มแล้วห้ามพาไป `/login`) เพื่อไม่ให้ drawer ตัวถัดไปเดาใหม่ |
| 37 | token ที่ไม่มีชื่อ | frontend เจอ 8 ค่าที่ต้องใช้แต่ DS ไม่มี token (`150/180/280px`, `.62`, `640/360px`, `90%`, `z-100`) แล้ว **ทำถูกตามกติกา §6** คือปล่อยเป็น literal + คอมเมนต์ แทนที่จะตั้งค่าเอง | ตั้งชื่อครบใน design-system §1.3 (+ `size.content.max-w` ที่ประกาศไว้ในเนื้อความ §8.1 มาตลอดแต่ไม่มีตัวแปรจริง) + เพิ่ม namespace `opacity.*` / `z.*` ทั้งในตาราง mapping §1.5 และใน `tokens.css` |

**Persona walkthrough รอบนี้ (เจ้าของร้านที่เพิ่งชวนน้องสาวเข้าร้าน):**
เธอเชิญ → ส่งลิงก์ทาง LINE → น้องสาวสมัครบัญชีใหม่แล้วกดรับ · **คำเชิญหายจากรายการ** (เพราะไม่ pending แล้ว)
และธง "บัญชีที่กดรับถูกสร้างขึ้นหลังจากออกลิงก์" ไปรออยู่หลังปุ่มที่เขียนว่า "หมดอายุ/ยกเลิก" — เธอไม่มีวันกด
· รอบนี้แก้ป้ายปุ่มให้บอกว่ามี "ที่รับแล้ว" อยู่ข้างใน ⇒ **ยังไม่พอ** (เธอต้องอยากกดก่อน) แต่เป็นครึ่งที่ทำได้
โดยไม่แต่งข้อมูลที่ไม่มี · ครึ่งที่เหลืออยู่ในมือ @backend-api/@product แล้ว และเขียนไว้ตรง ๆ ว่ายังไม่ปิด

**Unresolved หลังรอบที่ 4:**
1. **ธงความปลอดภัยยังอยู่ผิดที่** (finding 28) — ต้องมี field บน `MemberRow` ถึงจะย้ายได้ → @backend-api / @product
2. **idle-timer ของการ์ดภาษี** (finding 35) → @product (มาพร้อมคำถามเรื่อง quota)
3. **ทางกู้รหัสผ่านของเจ้าของร้านไม่มีจอ** (M-02) — `/login/help` บอกให้ "ติดต่อเจ้าของร้าน/ผู้ดูแลให้ตั้งรหัสใหม่ให้"
   แต่คนที่ถูกติดต่อ **ไม่มีปุ่มให้กด** ⇒ คำแนะนำบนจอชี้ไปทางที่ยังไม่มีอยู่ · copy ของจอนั้นฝากไว้ที่ §12.3 แล้ว
   ตั้งแต่ Gate 2 — **ที่ขาดคือจอ ซึ่งเป็นของ F-004** → @product ตัดสินว่าเลื่อน `/login/help` ให้พูดตรงกับความจริง
   ตอนนี้ หรือเร่ง F-004 (ux ไม่ตัดสิน scope)
4. **ยังเปิดอยู่ (ไม่บล็อก build) — ร้านที่มีเจ้าของร้านคนเดียวกู้บัญชีเองไม่ได้** (ราคาของ D-030 ที่ user รับแล้ว) · UX ทำได้แค่ **กันไว้ก่อน** (ชวนตั้งเจ้าของสำรอง §5/§7) + **ทางไปต่อที่ไม่ตัน** (ติดต่อทีมงาน §12.3) · ปิดจริงเมื่อมี **F-081** (self-serve reset ทางอีเมล) ⇒ ตอนนั้นต้องกลับมาแก้ copy ของ S13 ทั้งชุด
5. **ต้องให้ user เคาะก่อน `frontend` เริ่ม build (ไม่บล็อก sign-off จอ): เลือกชุดไอคอน** — 3 ตัวเลือกพร้อม trade-off อยู่ที่ [ui.md §7 ข้อ 5](ui.md)
   (A Lucide / B Material Symbols / C Phosphor ⭐) · เกณฑ์ที่ผมให้น้ำหนักสูงสุด = **ต้องมีชุดเดียวกันทั้ง web และ Flutter จากผู้ดูแลรายเดียวกัน**
   ไม่งั้น web↔mobile จะ drift ซึ่ง DS §1 ห้ามไว้ · mockup วาดเป็นรูปกลางที่ทั้ง 3 ชุดมีเหมือนกัน ⇒ เปลี่ยนทีหลังไม่ต้องรื้อจอ
