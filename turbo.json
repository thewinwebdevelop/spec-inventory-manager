{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build", "db:generate"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "db:generate": {
      "inputs": ["prisma/schema.prisma"],
      "outputs": ["generated/client/**"],
      "cache": true
    },
    "lint": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build", "db:generate"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": ["coverage/**"]
    },
    "gen:contracts": {
      "inputs": ["openapi/**"],
      "outputs": ["openapi/openapi.yaml", "src/generated/ts/**", "../../apps/mobile/api_client/**"],
      "cache": true
    },
    "validate": {
      "inputs": ["openapi/**"],
      "outputs": ["openapi/openapi.yaml"]
    },
    "depcruise": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
