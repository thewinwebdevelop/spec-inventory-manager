{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo ok",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo ok",
    "dev": "echo 'apps/web dev placeholder — Next.js shell lands in T-000-09'"
  },
  "dependencies": {
    "@omnistock/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
