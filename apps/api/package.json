{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo ok",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo ok",
    "dev": "echo 'apps/api dev placeholder — NestJS skeleton lands in T-000-08'"
  },
  "dependencies": {
    "@omnistock/core-domain": "workspace:*",
    "@omnistock/db": "workspace:*",
    "@omnistock/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
