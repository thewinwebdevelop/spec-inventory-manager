name: mobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.6.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # R4 (docs/architecture/refactor-plan.md §4, mobile.md §3.7, user-approved
  # new dependency — gen_l10n/intl are pre-approved by the arch doc) — the
  # generated AppLocalizations (lib/l10n/app_th.arb -> lib/l10n/gen/) needs
  # both: flutter_localizations wires MaterialApp's localization delegates
  # (also gives Thai MaterialLocalizations for free, mobile.md §3.7); intl is
  # the runtime gen_l10n's generated code calls into directly.
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Generated OpenAPI client (T-000-07). Lives OUTSIDE lib/ as a sibling
  # package (apps/mobile/api_client) — a Dart package nested inside another
  # package's lib/ makes the CFE resolve the built_value library files and
  # their .g.dart parts at different language versions ("language version
  # override has to be the same in the library and its part(s)"). Wired-only
  # per F-000 scope (architecture.md §4.3); full typed usage lands in F-006.
  omnistock_api_client:
    path: api_client

  # D-023 (mobile architecture refactor, user-approved new dependency) —
  # state management + DI for the feature-first clean-architecture rewrite
  # (docs/mobile-architecture.md §3). Manual providers only (no
  # riverpod_generator/codegen per D-023 item 2) — every provider is
  # overridable via ProviderScope/ProviderContainer so each layer (domain
  # usecase, data repository, application controller) can be swapped for a
  # fake in tests without touching a real platform channel/network.
  flutter_riverpod: ^2.6.1

  # T-001-17 ★ — secure storage for the refresh token (Keychain/Keystore).
  # NEVER SharedPreferences/plaintext (client-security skill). The access
  # token is never persisted here — in-memory only (see lib/features/auth/data/token_store.dart).
  flutter_secure_storage: ^9.2.2

  # Direct deps (were transitive via omnistock_api_client) — lib/features/auth/data/auth_repository_impl.dart
  # catches DioException / builds Options directly to call the generated
  # AuthApi, and deserializes error bodies with the shared Serializers/FullType,
  # so both must be declared, not just relied on transitively
  # (depend_on_referenced_packages lint). Versions match api_client/pubspec.yaml.
  dio: ^5.7.0
  built_value: '>=8.4.0 <9.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter

  # T-001-17 — mocks the generated Dio-based AuthApi in widget/unit tests
  # without a real network call.
  mockito: ^5.4.4
  build_runner: any

  # T-001-17 — deterministic virtual-clock testing for
  # ThrottleCountdownController's real-time ticking (no real Timer/sleep).
  fake_async: ^1.3.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # R4 — turns on gen_l10n codegen from lib/l10n/app_th.arb (config: l10n.yaml
  # at the package root) on every `flutter pub get`/build/test. No
  # build_runner (mobile.md §3.7: "gen_l10n เป็นของ built-in ใน flutter tool").
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
